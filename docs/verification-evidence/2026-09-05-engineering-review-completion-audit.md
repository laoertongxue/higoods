# 工程代码审查与修复闭环（2026-09-05）

## 1. 结论与边界

- 本轮从原始审查报告、当前代码和实际失败中归并出 17 类工程问题；以下 17 类均已有对应实现或治理修复，不以降低断言、放宽 TypeScript、加入假业务数据或隐藏页面差异制造通过结果。
- 最新批次收口了剩余运行时路由包含匹配，以及 `input` / `change` 事件入口中可证明等价的控件判定和后续渲染选择重复；刻意不同的技术资料、唛架与 PDA `select` 规则保持不变。
- 仓库仍有高风险长期债务，列在第 5 节。它们没有被包装成“已修复”，也没有为追求数字一次性大重构。
- 分支为 `main`，基线 HEAD 为 `4f57183684b5e38c7ec27ce202cdb165b50d1b42`；工作树包含用户已授权的连续工程治理改动，未提交、未推送、未部署。

## 2. 已修复的 17 类问题

| 编号 | 问题类别 | 原问题 | 修复与当前证据 | 用户可见边界 |
| --- | --- | --- | --- | --- |
| ENG-01 | 数量解析正确性 | “长度 612.5 米”“卷数 8 卷”被解析为 0 | 修正数值正则并增加合法、小数、空格和非法文本单测 | 恢复既有业务含义，不改页面结构 |
| ENG-02 | 证据文件标识稳定性 | 连续空白未稳定收口，可能生成不一致标识 | `sanitizeId()` 使用连续空白归一规则并有单测 | 文件显示名称不变 |
| ENG-03 | TypeScript 导入配置 | TS5097 共 3325 项，检查入口不可用 | 配置支持现有 `.ts` 导入；TS5097 为 0 | 仅编译配置 |
| ENG-04 | 全量真实类型债务 | 后续仍有 1567/1502 项真实类型错误 | 全量 `npm run typecheck` 当前为 0；未发现 `@ts-nocheck` / `@ts-ignore` | 类型声明与兼容边界，不改有效数据结果 |
| ENG-05 | UI 重复导出 | UI 统一出口同时导出两个 `renderSkeletonCard` | 卡片导出改为准确名称，统一出口无同名冲突 | 页面调用保持原结果 |
| ENG-06 | 默认测试入口 | `npm test` 只有裸 `tsx`，没有明确测试目标 | 默认入口指向 Node + tsx 单测；当前共 15/15 通过 | 仅工程入口 |
| ENG-07 | lint 基线缺失 | 没有可重复执行的静态规则入口 | 新增 Biome 配置与 `lint`/`lint:fix` 命令 | 未全仓机械改写页面 |
| ENG-08 | format 基线缺失 | 没有 format check/write 入口 | 新增 `format:check`/`format:write`；本次 `main.ts` 也通过 Biome | 仅源码排版 |
| ENG-09 | 库位 T1/T2 旧夹具 | 不完整特殊工艺事件被生产门禁正确忽略，旧断言误判投影 | 补齐测试事件事实；T1 完全交出、部分回仓与数量守恒均通过 | 未修改生产投影与 Mock |
| ENG-10 | 特殊工艺旧断言 | 完整来源已自动绑定，专项仍强制要求“待绑定”样例 | 契约改为验证完整来源下待绑定为 0、绑定与发料一一对应；其他状态断言保留 | 未增加假待绑定记录 |
| ENG-11 | 路由前缀误匹配 | `includes`/未封口正则/裸前缀可能误命中相似路径 | 所有运行时 `pathname.includes()` 路由判断已迁移为边界匹配；确认单及四组固定打印入口由精确/边界帮助函数驱动；8 项路由/外壳单测及 175/175 菜单契约通过 | 合法路由集合不变；查询串解析保留 |
| ENG-12 | 浏览器存储异常边界 | 入口与 Store 直接访问存储，浏览器禁用/抛错时不稳定 | 迁入已有存储封装并验证读、写、删及静默降级 2/2 | key、JSON 形态和回退保持不变 |
| ENG-13 | 动态模块加载重复 | `main.ts` 曾有 31 组 Promise 类型、缓存变量、失败复位和加载函数 | 全部 31 个固定入口统一使用 `createRetryableModuleLoader()`；成功复用与失败重试 2/2 | 懒加载时机、路径、导出和调用点不变 |
| ENG-14 | 巨型入口重复代码 | `main.ts` 基础设施样板占用大量入口代码 | 加载、路由、存储与等价事件基础设施分批收口；`main.ts` 相对 HEAD 2127→1770 行，净减 357 行 | 页面 HTML 和业务事件处理器未迁移 |
| ENG-15 | 可重建/运行态文件膨胀 | 构建、浏览器测试、运行状态和历史输出占用仓库/工作区 | 清理约 2.389 GiB 可重建产物；运行状态、历史输出加入忽略或移出源码树 | 不删除真实图片和现行需求证据 |
| ENG-16 | 失效历史与孤立工具 | 禁用工作流副本、154 份历史文档及无调用脚本干扰现行事实源 | 删除 229 个受控旧文件；15 个仍有效孤立脚本接入统一契约，9 个失效/无消费工具移除 | 所有受控删除可从 Git 历史恢复 |
| ENG-17 | 全局输入重绘重复 | `input` / `compositionend` / `change` 重复维护相同控件规则和渲染分支，容易漂移 | 抽取共享控件判定与统一后续渲染函数；保留事件专属分支和原判断顺序；命名页面连续输入、下拉和焦点浏览器矩阵通过 | 字段值、焦点、局部更新和计算结果不变 |

## 3. 本批瘦身审计

`shrink:` `src/main.ts` 的 31 组动态导入缓存样板已归一为 31 个声明和 1 个帮助函数；手写 `*ModulePromise` 与固定模块 `get*Module()` 实现已清零，唯一保留的 `getPdaMainTabModule(pathname)` 是按路径选择模块的分发函数，不是重复缓存。三个事件入口共有的控件判断与渲染尾段也已收口，事件专属差异仍留在各自入口。

`net:` 当前 `src/main.ts` 相对基线 HEAD 从 2127 行降至 1770 行，净减少 357 行；整个受控差异中删除 229 个历史/运行态文件；新增运行依赖 0。

## 4. 当前验证证据

| 层级 | 检查 | 当前结果 |
| --- | --- | --- |
| 类型 | `npm run typecheck` | 0 错误 |
| 工程 | `npm run typecheck:engineering` | 入口及 `domain`、`components/ui`、`state` 均为 0 |
| 单测 | `npm test` | 15/15；包含解析、路径、最终打印外壳、存储和加载器 |
| 格式/静态规则 | `./node_modules/.bin/biome check src/main.ts`、`npm run lint`、`npm run format:check` | 通过 |
| 路由 | `npm run check:menu-routes` | 175/175，遗漏 0、重复 0 |
| 保留契约 | `npm run check:retained-contracts` | 15/15 |
| 构建 | `npm run build` | 2398 个模块，成功 |
| 浏览器 | Playwright CLI：确认单路由边界、后道/毛织四个仓库路由、裁床唛架、PDA 铺布、技术资料、PDA 登录/入驻 | 页面正常；连续输入保持焦点；下拉、实时计算与路由外壳正确；控制台错误 0 |
| Git | `git diff --check` | 通过 |
| CodeGraph | `sync/status` | 已同步；1578 个文件，48419 个节点，无待同步提示 |
| 任务收据 | `npm run workflow:verify -- ...` | `verified`，blockers 0；`/private/tmp/higoods-engineering-route-rerender-receipt-20260905.json` |

## 5. 明确保留、不可冒进的债务

1. 当前仍有 97 个超过 1500 行的 TypeScript 文件。它们含大量页面模板和业务状态分支，必须按命名模块逐个拆分并做页面验收，不能批量切文件。
2. `localStorage` / `sessionStorage` 字面引用当前分布在 81 个文件、376 行；该数字包含合法封装和页面专属持久化，并不等于 376 个缺陷。后续应先按 key、序列化和失败语义分类，再逐模块迁移。
3. 字符串模板的 HTML 转义仍依赖调用方纪律。需要先区分可信常量、属性值、用户输入和已转义片段，再建立面向危险 sink 的检查，不能对所有插值二次转义。

专项检查脚本的两类工程漂移也已修正：生产对象检查重新绑定当前 `pickup-management-list.ts` 与索引访问器，因而能继续执行到真实业务断言；毛织仓检查改为显式选择共享库位、用“仓库 ID + 库位 ID”组合身份核验本次转出/转回记录，并通过 `window.localStorage` 验证当前浏览器存储契约。生产对象专项仍在既有 `PH-20260328-007` 无法回溯生产单处失败；本批没有删除该断言、补假关联或修改页面展示，因此该项不计入已修复类别。

上述三项长期债务是下一批审查候选，不影响本次 17 类已修复项的闭环结论。
