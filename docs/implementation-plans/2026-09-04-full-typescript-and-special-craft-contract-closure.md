# 全量 TypeScript 与特殊工艺契约闭环实施计划

## 1. 目标与完成标准

本轮承接上一轮工程加固，完成两个明确目标：

1. 将当前 `npm run typecheck` 的 1502 个错误清零，使全量严格 TypeScript 检查真实通过。
2. 修复 `check:cutting-special-craft-dispatch-return` 的“缺少待绑定菲票视图”失败，使专项检查与当前业务事实一致。

完成标准：

- `npm run typecheck` 退出码为 0，不使用 `@ts-nocheck`、关闭 `strict`、扩大 `exclude`、降低 compilerOptions 或删除源文件绕过。
- 类型修复优先使用共享类型事实源、泛型约束、判空、精确联合和局部历史兼容边界，不改变业务状态、数量、Mock 展示、HTML、路由或交互结果。
- 特殊工艺契约必须明确“待绑定”是否仍是当前必备 Mock 状态；若已经由现行自动绑定规则淘汰，则修正过时契约并增加反向证据，不能只删除断言。
- 最后一次实质修改后，重新运行全量类型、相关业务专项、构建、治理、CodeGraph 同步和任务收据。

## 2. 当前事实

- 分支：`main`；基线 HEAD：`4f57183684b5e38c7ec27ce202cdb165b50d1b42`。
- 全量类型错误：1502，分布在 243 个文件。
- 主要错误码：TS2322 474、TS2345 270、TS2339 237、TS18047 81、TS2367 76。
- 最大错误文件：`pcs-project-domain-contract.ts` 110、`marker-spreading.ts` 54、`preparation-timing.ts` 44、`runtime-process-tasks.ts` 44、`pcs-tech-pack-review.ts` 40。
- 特殊工艺专项在当前工作树与纯净 HEAD 均于同一断言失败；当前投影存在待发料、已发料、已接收、待确认顺序和异议中，但不存在待绑定 Mock。

## 3. 执行批次

### WP1：特殊工艺契约事实核对与修复

- 核对 `pendingBindingViews` 的产生条件、自动绑定来源和现行设计/检查。
- 若当前完整数据必然自动绑定，将专项改为验证“无来源时产生待绑定、完整来源不产生伪待绑定”；若业务仍要求展示待绑定样例，则补充真实缺失来源场景及其上下游证据。
- 运行特殊工艺专项、裁剪主链路和库位专项。

### WP2：共享类型根因与高级联错误

- 按错误数量与依赖面优先修复共享 domain/data 类型、导出缺失、状态联合、可空性和泛型工具。
- 每批重新生成错误分布，确认错误总数单调下降并避免将错误转移到别处。

### WP3：模块与页面叶子错误

- 依次处理 FCS、PCS、裁剪页面、PDA、打印和其他页面叶子错误。
- 对历史字段读取采用显式兼容类型或精确适配，不用运行时默认值改变页面结果。

### WP4：全量门禁与回归

- `npm run typecheck` 必须真实通过。
- 运行 `typecheck:engineering`、lint、format、单测、构建、裁剪全链、菜单路由、保留契约和原型治理。
- CodeGraph 同步后生成任务收据；记录任何仍存在但不属于 TypeScript/特殊工艺的独立业务问题。

## 4. 风险控制

- 共享接口改动可能放大页面影响：优先修正类型声明以匹配已有运行对象，并用业务专项证明输出不变。
- 非空断言只允许用于已有前置门禁可证明的值；无法证明时增加显式失败或保持既有回退语义。
- 不进行全仓格式化，不顺手重构页面，不更换状态名称，不修改数量算法。
- 当前工作区包含上一轮用户已授权变更；不回退、不覆盖、不吸收无关差异。

## 5. 完成结果与当前证据

- WP1 已完成：当前 Mock 的上游菲票来源完整且全部存在绑定记录；专项不再要求制造不存在的“待绑定”样例，改为同时验证“待绑定数量为 0”和“发料视图与绑定记录一一对应”。既有待发料、已发料、已接收、待确认顺序和异议断言全部保留。
- WP2-WP3 已完成：`npm run typecheck` 从 1502 个错误降为 0，未使用 `@ts-nocheck`、`@ts-ignore`、扩大排除范围或降低严格配置。
- WP4 中本任务直接门禁已完成：全量类型、工程类型、特殊工艺、库位、裁剪主链、车缝分配、加工厂 Tab/后道、菜单路由、15 项保留契约、lint、format、10 项单测、Vite 构建、原型治理和 `git diff --check` 均通过。
- 首次项目级任务收据状态为 `implemented`：它另外发现毛织全流程两个独立历史失败——部位毛织任务可分配明细数量断言失败，以及 PDA 交出记录折叠区内接收回写入口不可见。两处问题已在后续毛织闭环任务中修复，没有修改毛织业务数据或降低断言。
- 后续项目级任务收据 `/private/tmp/higoods-wool-fix-receipt-final-20260905.json` 状态为 `verified`，毛织专项、全量类型、构建、治理和毛织 E2E 13/13 均通过，阻塞项为 0。
- CodeGraph：`codegraph sync` 返回 `Already up to date`；任务收据确认同步前后 `pendingCount=0`、`worktreeMismatch=false`。
- 用户可见影响按简版治理记录登记：`docs/prototype-review-records/2026-09-04-full-typescript-and-special-craft-contract-no-visible-impact.md`。
