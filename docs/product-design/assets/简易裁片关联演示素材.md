# 简易裁片关联演示素材与调用

本组数据为原型演示，不代表实际生产。

- 初始化：`ensureSimpleCutPieceHandoverFixtures()`，应在现有应用数据初始化完成后调用一次，不能在运行时任务列表构建内部调用，避免循环。
- 订单：`PO-DEMO-SIMPLE-0916`，从 `PO-202603-082` 的技术资料复制。雾霾灰 M 100 件和 L 80 件分配给两个有效 PPIC 不同的工厂，使用现有 runtime SKU 分区与分配快照；未接单也可打印。
- 款式：`SPU-HOODIE-082`，连帽拉链卫衣；效果图没有裤装，所有本次资料名称统一为卫衣。
- 产品图：`/production-confirmation-demo/grey-zip-hoodie.png`，imagegen 生成的正式原型效果图；来源声明见同目录 `sources.json`。
- 面料图：`/materials/fei-ticket/fog-grey-sweatshirt-fleece.png`，对应雾霾灰卫衣抓绒面料；不用于其他颜色或材质。
- 纸样 SVG、DXF、RUL、PRJ：同目录 `grey-zip-hoodie.*`，纸样是同一演示几何的预览与下载。RUL/PRJ 明确为演示格式，不声称兼容厂商生产 CAD。
- 实际部位：左前片 1、右前片 1、后片 1、袖片 2、帽片 2、口袋片 2；合计 9 片/件。
- 第一批：通过既有“完成裁剪”事件生成对应实际产出菲票，帽片未裁。M 可交 700 片、仍欠 200 片；L 可交 560 片、仍欠 160 片。没有预造简易交出或工厂接收事实。
- 第二批：显式调用 `appendSimpleCutPieceDemoCuttingBatch(2)`，新增帽片完裁事实。它的固定幂等键防止重复生成；不会在刷新时自动补齐。
- 初始化不清除现有交出事件、接收记录或用户的当前分配。重新加载保留实际分配快照和已写入事件。

本文件只记录本组可运行基础场景。改派、取消、袋占用、特殊工艺、超量、无 PPIC 等边界由专项契约/其他关联场景验证，不能仅凭此 fixture 存在就标记通过。

## 其他可见打印场景

- `PO-DEMO-SEW-IRON-0916`：车缝+烫包已分配，使用独立裁剪任务的实际首批菲票；任务 `MERGED-SEW-IRON-PO-DEMO-SEW-IRON-0916-12NDM62`。
- `PO-DEMO-TRIPLE-0916`：裁剪+车缝+烫包已分配，任务 `MERGED-CUT-SEW-IRON-PO-DEMO-TRIPLE-0916-0WPGX2R`；接收面辅料，简易裁片交出不适用。
- `PO-DEMO-UNASSIGNED-0916`：独立车缝未分配，任务 `TASK-PO-DEMO-UNASSIGNED-0916-SEW__ORDER`，用于固定打印按钮灰态。
- 独立 M 任务：`TASK-SIMPLE-0916-PROC_SEW-A01`；独立 L 任务：`TASK-SIMPLE-0916-PROC_SEW-R02`。编号读取当前分配动态生成，不在页面硬编码。
- `appendSimpleCutPieceDemoBoundaryCutting('OVER_REMAINING')`：显式追加 M 左前片 101 片的异常完裁，验证整票超过应交上限被排除。
- `appendSimpleCutPieceDemoBoundaryCutting('UNMATCHED_PART')`：显式追加不在任务冻结部位中的 1 片完裁，验证归属不明阻断。
- 第二批函数可传第二参数订单号，为车缝+烫包演示补齐帽片；三合一不调用中央裁床完裁场景。

## 正式生成的辅料图

- `HG-DEMO-ZIPPER-GRY-60`：雾霾灰开尾拉链，60cm、银色细齿、灰布带；`/production-confirmation-demo/fog-grey-open-end-zipper.png`，每件 1 PCS。
- `HG-DEMO-RIB-GRY`：雾霾灰棉氨纶 2×2 罗纹袖口和下摆用料；`/production-confirmation-demo/fog-grey-rib-knit.png`，每件 0.35 米，损耗 3%。
- 两张为 imagegen 正式原型物料效果图，已目视检查灰色、对象和纹理，不复用牛仔裤铜拉链照片；原始生成路径及用量演示来源登记在 `sources.json`。
- PO082 和上述演示订单共用同款资料；任务单和生产确认单从同一 BOM 读取两条辅料及对应图片，单位分别为 PCS、米。
- 尺寸表同步演示 RUL/PRJ 已有数据：后中长、胸围（平铺衣宽×2）、袖长；文件未提供的肩宽、袖口、脚围保留未维护，不编造来源。
- `cancelSimpleCutPieceDemoTaskSheet(当前纸号)`：明确、主动取消本次 `PO-DEMO-*` 分配，返回旧纸号供失效验证；不允许操作非演示生产单，不删除历史交出。
