# 最终增量对抗式审查

2026-09-19；审查者：只读子代理 wool_final_adversarial；主代理复核当前完整 diff。基准 HEAD 与源码摘要见 [最终清单](./final-source-manifest.json)。

## 首轮发现与修复

| 问题 | 修复与直接证据 | 复审 |
|---|---|---|
| 关键词测量早于列表刷新 | 等待实际新列表与空结果，不以输入值替代结果 | 通过 |
| 隐藏破图未被判为失败 | 两份旧脚本检查可见失败容器；image-gate-negative-main.json 与 image-gate-negative-extra.json 共10个反例均正确失败 | 通过 |
| 缺1024性能 | 补12条主管路由与九组主管操作 | 测量逻辑通过，执行结果见最终索引 |
| 设备/仓库入口缺测 | 补实际筛选、分页、列设置、调整、转出转回；不存在的独立查询/导出按钮不伪造 | 测量逻辑通过，执行结果见最终索引 |
| KOL懒初始化等价证据先初始化 | 完全撤回KOL懒初始化；最终等价脚本先读任务，578任务/239进度/267入库/91出库/29厂默认库位逐项相等 | 通过 |

## 复审结论

冻结源码及测量逻辑通过。未发现新的数量、权限、库存或持久化缺陷。已撤回无明显收益的 head 快路径、局部投影及构建试验。默认500ms，只有用户授权的PDA任务队列冷进入允许不超过1000ms；刷新、切换、操作没有扩大例外。

主代理复核了接收窄范围防御性复制、印花演示批处理回滚与调拨文档读取、仓库通用写入后的毛织派生恢复、进度一次快照读取、PDA路由及真实图片、打印分页。没有用减少演示数据、跳业务动作或假占位换取性能。

本记录不独立证明运行验收完成。45项最终浏览器用例及266单元测试已通过；最终五轮性能、构建一致性、矩阵登记和治理结果由最终证据索引统一决定。

## 最终实测暴露的仓库竞态

375项操作及1024补测暴露分页偶发被迟到刷新覆盖。仓库文字input保留180ms延迟，切换/重置取消旧timer；SELECT input只消费，由change应用。初次修复复审发现迟到SELECT input可能取消新文字刷新，已将其提前返回放在清timer之前。

确定性fake-clock测试包含四种事件顺序。第一组旧代码在181ms后替换table/pager节点，修复后不再替换；第四组旧代码使NO-MATCH筛选丢失、仍显示8行库存，修复后正确显示空结果。两轮红绿原文见本目录 `debounce-red.log` / `debounce-green.log` 与 `debounce-late-select-red.log` / `debounce-late-select-green.log`。没有通过等待或跳过迟到事件掩盖问题。

成衣工艺的未计时开发夹具曾被Vite文档保存重载打断；只在该夹具上下文禁用HMR WebSocket，实际操作计时仍使用独立、没有拦截的预览上下文。两份原失败报告 `remaining-action-performance-before-debounce-fix.json` 和 `remaining-action-performance-1024-before-debounce-fix.json` 原样保留。

最终小增量复审：SELECT input在取消timer之前返回后，P2关闭；第四序列红绿日志已由只读审查者核查，未发现其他剩余问题。A08仅补真实UI验收测试，无业务代码变更。最终运行结果与矩阵由主代理收口。
