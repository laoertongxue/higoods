# 生产与履约时效 V2 页面重整与逐行审阅回执

2026-09-17。此次范围为七个二级菜单的信息层级、共用筛选与统计密度，以及原方案逐行复核。未提交、未推送、未部署外网，未改动线上业务数据。

## 本次结果

| 页面 | 当前信息结构 |
|---|---|
| 时效总览 | 运行概况、分布分析、数据待补互斥；概况最多三条关注卡，完整任务带筛选下钻 |
| 生产任务 | 紧凑查询、48px摘要、独立任务列表；详情先选全程、工作、数量、实发订单、公式或跟进 |
| 我的跟单 | 沿用统一查询与任务列表，范围按跟单身份收敛，不叠加全局分析长表 |
| 工作项监控 | 独立工作列表；工作抽屉分时效数量、前后依赖、来源证据 |
| 团队与工厂 | 团队目录进入工作台；可执行、需协调、交付记录互斥 |
| 订单履约分析 | 发货行、完整订单、时钟对照分别查看；口径收在对应说明内 |
| 规则与配置 | 七类配置互斥；左侧阶段目录、右侧该阶段工作；单项编辑、试算、校验、发布使用二级弹窗 |

六个业务查询页默认四个高频条件，展开后的全部条件使用同一等列网格。查询、重置、导出、更多/收起始终在全部条件下方同一行；控件30px、统计卡48px。配置页使用自身配置目录，不添加不适用的任务筛选。

任务甘特保留九阶段入口，默认收起完成阶段，支持按工作、团队、预算版本、时标查看；独立规则算例从任务正文移到专门示例页，不参与任务统计。

## 当前版本与证据

- 分支 `codex/dds-production-fulfillment`，基础HEAD `4804328a822eec3c77eee1ffa5b10911bb77c9fe`。验收对象为当前未提交工作树，精确文件哈希见 `implementation-evidence-v2/build-manifest-v2.json`。
- 服务PID79543，工作树 `/Users/laoer/Documents/higoods`，4179读取同工作树构建 `/tmp/higoods-dds-pf-build`。当前局域网配置页 `http://192.168.5.2:4179/dds/supply-chain/production-fulfillment/configuration` 返回200；旧192.168.5.12地址已失效。
- 构建通过，模块规则测试46条通过；构建中的全套单测169条通过。范围类型检查0错误，范围外61项既有类型错误未纳入此次修改。

| 当前浏览器验证集 | 分组 | 原始样本 | 最大耗时 | 结果 |
|---|---:|---:|---:|---|
| `--production --layout-v2` | 6 | 1214 | 184.69999998807907ms | 全部低于200ms，页面错误0 |
| `--production --deep --regression-v2` | 21 | 698 | 168.90000000596046ms | 全部低于200ms，页面错误0 |
| 新LAN地址 `--metrics-v2` | 1 | 33 | 110.09999999403954ms | 全部低于200ms，页面错误0 |

隔离Chromium149.0.7827.55。布局在1366×768、1280×720、1024×768验证；相邻回归另含1920×1080大屏。冷进入、刷新与站内切换各五次；操作按脚本具名分组重复五次。原始耗时全部保存，不以平均值或剔除慢样本判断通过。首次预检的布局与选择器失败未作为最终验收结果。

保留操作回归包括查询空结果、日期与更多条件、导出、列显示顺序冻结拖拽、排序分页、甘特操作、跟进与人工预计、打印摘要、未知路线、配置发布与在途重算、原始基线保留、主管范围、防越权、图片关闭及失败反馈、时区与大屏。配置编辑、事件试算、校验与发布分层按当前弹窗结构验证。

统计口径另外验证：8个在途任务中，当前逾期可判7/8，预测可判6/8；仅查MOCK-PT-010时分别1/1、0/1。两项不再用同一个“非待判定”数量替代。

## 方案逐行复核

原方案741行中的538个非空行，逐行保留原文、分类与问题绑定，见 `review-v2-line-audit.csv`；15项发现见 `review-v2-findings.md`；源码核对、此次处置及剩余能力见 `review-v2-resolution.md`。

撤回旧“259条适用项全部已验证”的整体结论，原矩阵保留在 `requirements-matrix-v1-audit-snapshot.csv`。当前矩阵274条，仅依据本轮直接证据将14条新增页面/筛选/指标要求标为已验证；其他未重新证明的页面与业务要求继续保留已实现待验证。旧已验证行仍带V1版本，不冒充当前版本全量验收。

仍需补充的业务实现包括逐类来源事件识别、运输交付与工厂实收责任拆分、跨批跨来源订单实例、多SKU物料分配、组合动作叶子预算、阶段整体历时统计、独立数据事项恢复、预测关键路径与数量里程碑，以及事件驱动的完整分批任务甘特。

## 结论

此次七菜单页面分层、筛选对齐与紧凑化已通过当前版本验证。方案逐行审阅已完成，发现的问题已公开登记；**这不代表整个模块的全部业务能力已实现或已验收**。产品接受仍以用户查看当前原型后的反馈为准。
