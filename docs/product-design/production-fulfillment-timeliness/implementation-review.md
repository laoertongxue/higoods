> V1历史记录：以下证据属于V1构建。用户现场指出信息结构问题后，V2重新审阅并撤回“259项全部已验证”的总体结论；当前状态见V2审阅处置和验收记录。

# DDS「生产与履约时效」原型实施与验收

本次按已确认V1.0方案在当前HiGood原型实现。菜单为 **DDS → 供应链域 → 生产与履约时效**，与物料监控与决策并列。正式业务规则值、线上系统接入和真实鉴权没有被包装成已完成能力。

## 实现范围

| 工作包 | 实际位置 | 可观察结果 |
|---|---|---|
| WP01 业务对象与来源 | model.ts、fixtures.ts、mock-data.json、catalog.ts、evidence.ts、boundary-evidence.ts | 1:1任务起点；准备显式关联；合并生产单共享明确主跟单但保留来源T0；77候选动作目录 |
| WP02 时效与数量 | calculations.ts、scenarios.ts、rule-recalculation.ts | 自然日依赖正推/倒排；基线与预测分离；数量分配、批次能力、取消、去重、未知区间 |
| WP03 任务详情 | tasks.ts、task-detail.ts、timeline.ts、styles.css | 九阶段31计时工作、21组内动作；日/小时甘特、依赖、数量阶梯图、详细公式、工作抽屉 |
| WP04 团队协作 | dashboards.ts、index.ts | 我的跟单、团队可执行/待协调队列；原因责任和协调责任分开；本地跟进/负责人预计 |
| WP05 统计与履约 | dashboards.ts、calculations.ts | 总览大屏、实际发货后才关联客户订单、发货行与全单统计独立 |
| WP06 配置 | configuration.ts、config-model.ts、rule-recalculation.ts | 七区编辑、匹配样例、循环/冲突/缺映射阻断、发布影响、在途重算、历史快照 |
| WP07 现有原型接线 | app-shell-config.ts、routes.ts、main.ts、state/store.ts、events.ts、index.html | 七菜单及两类详情；业务模块懒加载；隐藏详情独立标签页；输入局部处理 |
| WP08 验收 | 三个业务专项单测、check-dds-production-fulfillment.mjs、需求矩阵、治理记录 | 同版本业务断言、截图、所有操作类别五次样本、逐项追踪 |

## 关键业务结果

- 默认10任务中8在途：已逾期1、预计逾期2、待判定2、正常3；有效7000件、实发400、剩余6600。共享工作按实例ID去重：全局124项。
- 主例T0=2026-08-28 10:00，快照2026-09-17 10:00。标准 `max(裁片及工艺14,中国辅料到厂15)+齐套1+车缝与合格回货6+后道质检1+实发1=24` 自然日。当前合格回货400/1000，600÷200=3天，预计D25；守住D22合格回货需300件/日，缺100件/日。
- 已入库不代表到厂。006物料需求1000，现货300到厂；采购700中500入库、200到厂。可用500、仓内待调拨300、尚未入库200；新增采购缺口0。
- 007仅接受正式需求调减100：有效900、实发400、剩余500、44.44%。客户取消而未关联具体生产任务时不改变分母；全取消显示终止。
- 5个实际关联客户订单，3个超时，超时率60%；1400件中900件超时，数量占比64.29%。008生产按期与所发客户订单超时可以同时成立。
- 规则W19从1改3天时，当前理论标准24→26，但原始基线/生效截止仍D24。W19已经实际完成，事实不改变，所以实际产能预测仍D25。未完成工作预算变化的预测传播另有专项测试。

## 图示和角色

日视图自动适配屏幕，小时视图在容器内滚动。每工作显示责任、合格数量、时效要求、实际起止和耗时、逾期；标准/实际/预测分别画条，当前/生效截止/预测终点为带文字的线。阶段是浏览分类，不能按阶段顺序假造串行。

跟单看自己任务；车缝厂A主管可查看相关任务，但只能登记本团队节点的负责人预计。保存时再次检查，不能通过修改表单绕过。这里是原型权限演示，未声称提供服务端鉴权。

## 本地运行版本

- 分支：`codex/dds-production-fulfillment`。
- 基础HEAD：`4804328a822eec3c77eee1ffa5b10911bb77c9fe`；工作区改动未提交/推送。
- 工作树：`/Users/laoer/Documents/higoods`。
- 验收构建：`/tmp/higoods-dds-pf-build`，使用当前源码构建；生产预览4179。
- 源码开发服务4178；已有4177是旧dist预览，不作为本次验收证据。
- 局域网入口：`http://192.168.5.12:4179/dds/supply-chain/production-fulfillment/overview`。
- 用户原有`AGENTS.md`差异保持不动，没有接触线上数据。

## 验收状态

本地Mock原型验收通过。项目构建169项单测通过，其中本模块46项；本次类型检查范围0错误，范围外61项既有错误未纳入本次修改。标准列表静态治理424页及原型设计治理通过。最终浏览器23组场景、1308次原始加载和交互均<200ms，最高135.59999999403954ms。

Chromium 149.0.7827.55；1366×768、1280×720、主管1024×768、大屏1920×1080。冷加载45次最高99.1ms、刷新45次最高70.5ms；所有导航最大135.6ms，点击最大34.2ms，输入最大31.6ms，选项切换最大57.1ms，PDF生成最大48.154ms。是否达标按原始值判定，这里的小数仅用于阅读。

[最终证据摘要](implementation-evidence/summary.json) · [1308条原始样本](implementation-evidence/verification.json) · [版本内容哈希](implementation-evidence/build-manifest.json) · [甘特截图](implementation-evidence/gantt-1366.png) · [打印样张](implementation-evidence/task-report.pdf)。相邻原物料总览5项只读回归也通过。

浏览器测量从导航/事件开始，等待对应路由内容和必要图片完成后经过实际绘制帧；所有原始值保存，不用平均值判断。冷加载清理的是隔离验收上下文缓存；未清用户浏览器数据。PDF打印记录生成文件可用时间。

预验收失败记录保留：开发首载超过200ms；一次跟进空表单点击4130.1ms；跟进文本输入207.6ms；一次首次导航221.6ms。检查发现原跟进表单未标本模块字段，输入会进入全局分发；已给全部表单输入及中文组合输入使用本模块处理路径，并保持原生焦点和表单值。首载另增加仅本模块同一真实图片的HTML预加载。最终修改后完整重测全部通过；先前失败样本独立保留，不删除或以平均值覆盖。

## 需求追踪与边界

260条矩阵按照方案章节逐条绑定实现文件、专项脚本和页面证据：259条原型适用项已验证；OPEN-001正式业务规则批准1条明确不适用（候选不用于正式考核，负责人待确认）。六个独立边界卡覆盖BOM/换算、准备复用与提前加工、返工去重、仅日期、实发缺关联、备货待履约；均不混入10任务统计。候选动作目录是77种可选类型，不能解读为每个任务都必须发生77项；主例按实际路线实例化。

正向核查以“需求→文件→当前同版本证据”，反向核查将新增文件和五处现有接线回到WP01—08。无新增依赖、后端、数据库、消息发送或现有执行流程迁移。正式规则发布前的产品确认继续记录在候选配置及设计第19节，不替代用户的最终业务验收。
