### Ran Playwright code
```js
await (async page=>{const p=page.context().browser().contexts().flatMap(c=>c.pages()).find(p=>p.url().includes("RECEIPT-SIMPLE"));return await p.evaluate(async()=>{try{const mod=await import("/src/pages/pda-handover-detail.ts");return await mod.renderPdaHandoverDetailPage(decodeURIComponent(location.pathname.split("/").at(-1)))}catch(e){return e.stack}})})(page);
```
