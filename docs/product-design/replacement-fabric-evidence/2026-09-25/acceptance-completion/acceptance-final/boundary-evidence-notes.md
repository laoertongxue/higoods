# 产品边界补充验收记录

验收工作树：`/private/tmp/higoods-replacement-fabric-release-20260925`。功能现场为同工作树开发服务 `127.0.0.1:43235`，独立 Playwright 会话 `hpb-business-boundaries`。以下是功能证据，不替代最终生产构建性能验收。

每场景使用独立浏览器 context，禁用 HMR WebSocket，提前扩大 Resource Timing 缓冲，并按页面实际加载 URL 导入源模块。模块操作仅构造明确标注的 Mock 来源资料、未交出票及打印前置；被验收的扫码、装袋、入仓、交出、回收与历史选择均走实际页面控件。

## 已通过：资料、需料与交出

`business-boundaries-agent-r8.json`：ORDER-004、MAT-006、MAT-004、HAND-011 全部通过，浏览器未捕获错误为 0。

- 缺生产技术资料：生产单保留在列表，标记“资料待核对”，明细说明缺少资料；0 票、禁止打印；查询和明细不持久化数据。
- 完整映射但面料名称全含“朴”：显示“无需换片布”，0 票、禁止打印；不误报资料缺失。
- 同名不同编码 A/B：真实第一次交出 A；随后明确 Mock 新增同任务需料 B 和第二裁片批。再次读取任务显示 A“此前已交”、B“还需换片布”。扫描同名另一张 A 后交出被拒绝，所有持久记录不变；改扫 B 后真实交出成功。最终恰有两次交出和 A/B 两张回执，第二次仅 1 张 / 5 Yard。
- Mock 技术资料先通过现有生产来源事务保存，避免后续动作 hydrate 覆盖内存前置。没有插入被测交出事件或回执。

该验收发现并修复同一分配下 B 面料范围被错误替换为 A 的缺陷。`tests/unit/replacement-fabric-repository-scope.test.ts` 覆盖真实来源的同分配同名 A/B 分别新增、一起打印以及错误身份拒绝。`repository-scope-before.txt` 为修复前失败；`repository-scope-after.txt` 为修复后通过。

## 已通过：扫码边界与简易交出占用

`bag-boundaries-agent-r10.json` 为最终完整脚本，前三组场景全部通过、浏览器错误为 0。其中前两组覆盖 BAG-002、BAG-003、BAG-004、HAND-009。`bag-boundaries-agent-r5.json` 保留中间结果。

- 同轮重复扫描不会增加票数、不产生持久写入。
- 不同生产单的票被拒绝；第二个合格生产单来自明确静态 Mock 及其裁床分配。
- 错扫后通过实际页面重开未保存轮次，重扫合法票后确认，唯一装袋事件只包含该合法票。
- 已装袋票扫描到另一袋被拒绝，第二袋为 0 票、既有记录不变。
- 真实装袋后的换片布票不能加入简易裁片交出，页面明确提示袋占用，未产生部分交出记录。

## 已通过：历史袋链、回收与旧周期打印

`bag-boundaries-agent-r10.json` 已完成实际 Web 当前周期及回收再装袋后历史周期的三页货物标签预览和软件打印，保持同一组三个旧票身份、裁片 20 片、换片布 5 Yard、捆条 8 米；历史打印不包含新装袋的第 3 张换片布票。`window.print` 被显式拦截计数，不表示物理打印验收。

原 `mixed.higcut` 中混装袋仅装袋未入仓，脚本补走真实 PDA 入仓并选择实际空闲库位；不伪造入仓结果。之后实际整袋交出、回收、旧已交 HPB 拒扫、新独立 HPB 再装袋均已成功，新旧使用周期不同。

此链发现操作时间混用：入仓使用本地时间，交出却使用 UTC，回收/报废另用 Jakarta 时间。已在直接相关三页统一复用 `localDateTimeText().slice(0,16)`，不修改事件排序、不改变浏览器时区。`bag-time-before.txt` 保留同一瞬间 05:31 与 13:31 的失败复现，`bag-time-and-scope-after.txt` 两项专项通过。

历史诊断 r9 暴露点击旧周期后进入“档案信息待补充”分支的问题；主代理补齐 canonical 周期路由解析与归属校验，并通过专项测试。修复后的 r10 通过实际旧周期按钮进入所选快照和货物打印入口，覆盖 BAG-009/010/011。脚本没有用手工 URL 或直接打印调用绕过历史页面。

## 证据边界

所有失败产物保留原后缀，不覆盖历史失败。截图名为各需求边界图，最终确认以带 `agent-rN` 的 JSON 版本为准。业务结果均为原型浏览器本地 Mock，不能解释为实际工厂生产或物理交接已经发生。

## 最后补充：三合一排除与完整空需料真实交出

`scope-empty-agent-r2.json` 两场景均通过，浏览器错误为 0；脚本为 `check-scope-empty-handover.js`。

- SCOPE-001：使用明确静态 Mock `PO-DEMO-TRIPLE-0916` 的真实任务单号，在 Web 简易裁片交出实际读取。显示“三合一任务请到面辅料仓接收”，确认交出按钮禁用，未展示换片布扫描框，读取前后持久记录完全不变。
- MAT-006：保持完整 SKU/颜色/BOM 映射，将该任务面料名称明确 Mock 为含“朴”，通过既有来源事务保存。真实任务读取显示“无需换片布”，实际确认成功交出 5 张裁片票 / 700 片，换片布 0 张 / 0 Yard；恰有 1 个简易裁片交出事件、0 张换片布回执，无伪造确认结果。

r1 保留测试前置选择错误（MAT-006 误选同单非裁片交出任务）的失败现场，r2 已按任务单 supportsCutPieceHandover 筛选对应 M 任务。该修正不更改任何产品源代码。
