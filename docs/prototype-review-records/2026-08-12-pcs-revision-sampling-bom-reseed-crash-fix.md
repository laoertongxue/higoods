# PCS 改款打样列表 BOM 重建报错修复审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-08-12 |
| 相关需求 / 任务 | 打开“改款打样任务”时不得因已有已确认 BOM 而报错；重新初始化独立打样 Mock 时不得覆盖已有 BOM |
| 记录模式 | 完整产品审查 |
| 涉及系统 | PCS |
| 涉及页面路径 | `/pcs/engineering/revision-sampling` |
| 端类型 | 管理端 |
| 主要角色与任务 | 跟单查看改款打样任务列表；买手已维护或确认的 BOM 与价格必须保留 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：页面结构、字段、文案、图片和路由不变；修复了独立打样 Mock 存储缺失、但关联 BOM 已存在时，列表初始化错误修改已确认 BOM 并导致页面无法打开的问题。修复后已有 BOM 只读取、不覆盖，改款打样列表能够正常展示。

当前审查依据：

- `AGENTS.md` 第 4 节：印尼工厂现场产品设计基线。
- `AGENTS.md` 第 5 节：UI、列表和真实图片专项门禁。
- `AGENTS.md` 第 7 节：分层验证和证据新鲜度。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 跟单仍通过标准管理列表查看改款打样任务；未改变角色和业务动作。 |
| 文案、状态、数量与单位 | 通过 | 页面仍显示 12 条改款任务及原有状态、团队、进度和 BOM 颜色版本数量。 |
| 扫码、真实图片与对象识别 | 通过 | 首行 A/B 款式图片、SPU 和名称同块展示；缩略图可打开对应高清大图。 |
| 防错、危险确认与主管兜底 | 通过 | 已确认 BOM 不再被 Mock 初始化写入；既有 BOM 状态和内容保持不变。 |
| 交接、跨端事实与异常追溯 | 通过 | 本次未改变跨端流程；独立打样与 BOM 仍读取同一关联版本。 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 管理端在 1366×768 下页面主体无横向溢出；PDA、弱网和上传不适用于本次列表加载修复。 |
| 命名路由、交互、图片大图与打印 | 通过 | 命名路由可打开且控制台 0 error；大图可打开并由 Esc 关闭；打印不适用。 |

## 4. 问题标签

- `点错风险`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 独立打样 Mock 重新初始化时无条件保存关联 BOM，遇到已确认版本即抛错并阻断列表加载 | 点错风险 | 跟单、买手 | 初始化前识别该打样任务已有的 BOM 版本；已有版本只读取，只有本次新建版本才写入初始演示用料 | 否 |
| 重复初始化可能覆盖买手已经维护的草稿 BOM | 点错风险 | 买手 | 回归测试校验重复初始化后已有 BOM 的完整内容和状态保持不变 | 否 |

## 6. 最终结论

结论：通过

说明：

- 原报错已由自动化契约稳定复现，并在修复后通过。
- 生产工程专项门禁 23/23 通过。
- 本地真实浏览器按“先生成已有已确认 BOM，再删除独立打样存储并重载列表”的场景验收通过。

## 7. 变更覆盖与验证

### 受管文件

- `src/data/pcs-engineering-master-sampling.ts`

### 页面路由

- `/pcs/engineering/revision-sampling`

### 验证命令

- `./node_modules/.bin/tsx tests/pcs-independent-sampling.spec.ts`：通过。
- `npm run check:pcs-engineering-master`：通过，23/23。
- `npm run build`：通过；保留仓库既有的大分块提示，不影响本次构建结果。
- Playwright 本地命名路由验收：通过；构造独立打样存储缺失、关联 BOM 已确认的场景后重载，列表显示 12 条记录，控制台 0 error、0 warning。
- Playwright 1366×768 验收：通过；页面主体无横向溢出。
- `npm run check:prototype-design-governance`：命令通过；当前为未暂存任务文件，staged 模式报告无受管变更，最终暂存／交付时需再次执行以形成正式门禁证据。

### 真实图片验证

- 图片来源：现有款式档案 Mock 的正式款式图，本次未修改图片资源。
- 对象对应：首行 `STYLE-PRJ-202603-011` 与 `STYLE-PRJ-202603-012` 的图片、SPU 和款式名称同块展示。
- 缩略图位置：改款列表“基于款式 → 做成款式”列。
- 大图弹窗：点击 `Jaket Hoodie Unisex 连帽夹克` 缩略图后显示对应大图，替代说明为“Jaket Hoodie Unisex 连帽夹克高清大图”；Esc 可关闭。
- 加载失败态：沿用现有页面能力，本次未改变。

### 例外

- 无。
