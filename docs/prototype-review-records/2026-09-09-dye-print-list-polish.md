# 染色、印花列表样式统一补充审查

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 日期 | 2026-09-09 |
| 记录模式 | 完整产品审查 |
| 相关需求 | 用户5点截图反馈；docs/染色印花列表样式统一补充验收.md，POL-01～POL-10 |
| 系统及端 | PFOS 管理端桌面 |
| 角色任务 | 主管、跟单筛选加工单，核对投入与产出并使用行操作 |
| 分支和工作树 | codex/process-route-full-stage-incremental；.worktrees/process-route-full-stage-incremental |
| HEAD | 8d8f8ffd43d1000d9c852a1cffbb4c73e16bee12 加本次文件改动 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：两页筛选、选择表头、统计布局、操作展示、列宽与图片尺寸统一。
- 依据：用户最新确认优先于AGENTS.md第5节原48px单行统计，采用上下两段且可换行；其他第4、5、7节规则适用。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 管理端标准分页列表，七个业务列加选择列 |
| 文案、状态、数量与单位 | 通过 | 摘要移除待补录句子；明细未知量保留；统计单位不拆开 |
| 扫码、真实图片与对象识别 | 有条件通过 | 本次缩略图及大图通过；前次正式素材缺失仍阻塞完整图片验收 |
| 防错、危险确认与主管兜底 | 通过 | 不可用操作置灰，批量打印空选禁用，无新增危险写入 |
| 交接、跨端事实与异常追溯 | 不适用 | 本次未变更事实源、交接规则或PDA |
| 低分辨率 | 通过 | 1366×768和1280×720，页面无横向溢出，表内滚动 |
| 命名路由、交互、图片大图与打印 | 通过 | 筛选、选择、分页、图像、行操作及导出；打印模板本次未改 |

## 4. 问题标签

- 视觉干扰
- 组件误用
- 字段过载

## 5. 主要问题与处理

| 问题 | 处理方式 | 剩余影响 |
| --- | --- | --- |
| 更多筛选位置及计数 | 与导出同排紧邻，已选条件即时计数 | 无 |
| 选择范围在页头分散 | 固定选择列表头，复选框加范围菜单 | 无 |
| 摘要挤压与补录文案 | 统一名称上数据下，不混入补录句子 | 原明细缺数据继续明确显示 |
| 更多隐藏行操作 | 所有操作直接列出，不可用项置灰 | 无 |
| 两页样式不统一 | 共用统计、选择表头和筛选展开组件；统一输入、列宽、徽章及缩略图 | 业务专属字段和染色专项导出保留 |

## 6. 最终结论

结论：有条件通过

本次样式与交互调整通过；不代表上次整体89项已全部通过。

原有正式素材及历史资料5项阻塞继续有效，本次不制造素材或改写原型数量。

## 7. 变更覆盖与验证

### 受管文件

- `src/pages/process-factory/dyeing/work-orders.ts`
- `src/pages/process-factory/printing/work-orders.ts`
- `src/components/ui/list-table.ts`
- `src/components/ui/process-order-list-presentation.ts`

### 页面路由

- `/fcs/craft/dyeing/work-orders`
- `/fcs/craft/printing/work-orders`

### 验证命令

- `npx --offline tsc --noEmit --pretty false`：通过。
- `npm run build`：通过，既有打包体积警告。
- `node --import tsx scripts/check-dye-list-unified.ts`：通过。
- `node --import tsx scripts/check-printing-two-end-list.ts`：通过，旧七列表头契约按用户新增选择列更新为八列；仍保留七个业务列。
- `npm run check:standard-list-page-template`：通过，包括真实Chromium列拖拽、冻结及DOM稳定性；最初用tsx启动产生__name运行器错误，已改用项目原命令重跑通过。
- 标准列表静态治理、原型治理及任务文件覆盖：见证据目录实际结果。
- 两页浏览器：`output/playwright/process-order-polish/browser.log`；查看、编辑、日志及导出：`actions.log`。
- CodeGraph：已同步，状态见`codegraph-status.log`。

### 真实图片验证

- 沿用已有物料／商品对应资源，未替换素材；缩略图在对象同列展示，统一40×40；两页点击大图和Esc关闭已验证。
- 图片失败态沿用原实现；前次正式色样、正式花型、部分实际投入与产出素材缺失继续显示缺失／待核验。

### 例外

- 统计上下布局和可换行由用户最新明确要求，覆盖旧48px单行约定。
- 工作区还有其他既有改动；不暂存、不合并或吸收。全量任务收据无法限定本次文件，未运行workflow:verify；保留本次四个源码文件的版本指纹和专项证据。
- `check:list-page-governance`组合脚本含整个工作区审计，本次使用其静态检查、标准表格检查和精确四文件覆盖验证，避免把无关工作区改动纳入结论。
