# 后道回货逐 SKU 确认与废弃原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-09-03 |
| 相关需求 / 任务 | `RETURN-SKU-*`、`RETURN-CFM-*`、`RETURN-AUTH-*`、`RETURN-QC-*`、`RETURN-VOID-*`、`RETURN-LEGACY-*` |
| 记录模式 | 完整产品审查 |
| 涉及系统 | PFOS / FCS PDA |
| 涉及页面路径 | `/fcs/craft/post-finishing/wait-process-warehouse`、`/fcs/pda/handover/sewing-self-return`、`/fcs/pda/post-finishing/return-confirm`、`/fcs/pda/handover/:id` |
| 端类型 | 管理端 / 员工执行端 |
| 主要角色与任务 | 车缝送货员登记逐 SKU 回货；后道回货确认员逐 SKU 点数；上级仅在超 5%时授权；相关人员在最终确认前废弃错误登记 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：现有回货列表、详情和 PDA 卡片增加逐 SKU 明细、废弃动作、回货方式与正确的 5%提示；旧 PDA 后道详情移除通用差异/驳回/直接确认入口。菜单、路由、页面主结构、PDA 底部导航和下游后道流程不变。

审查依据：`AGENTS.md` 第 4 节员工执行端、数量和交接基线，第 5 节局部交互与真实图片门禁，第 7 节分层验证。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | Web 保持管理列表；PDA 保持“识别单号—逐 SKU 点数—确认”现场任务，不新增模块导航 |
| 文案、状态、数量与单位 | 通过 | 所有数量均带“件”；明确逐 SKU 5%和整单合计仅供核对；新增“已废弃” |
| 扫码、真实图片与对象识别 | 通过 | Web/PDA 每个 SKU 均展示对应图片、编码、颜色、尺码和数量；大图弹窗及 Esc 关闭已验证 |
| 防错、危险确认与主管兜底 | 通过 | 超 5%才复点/授权；废弃要求原因与二次确认；已确认记录不能废弃 |
| 交接、跨端事实与异常追溯 | 通过 | Web/PDA 调用同一确认和废弃事实；废弃保留人、时间、原因与明细 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 未新增上传或弱网能力；PDA 400×786 及相邻尺寸验收无横向溢出 |
| 命名路由、交互、图片大图与打印 | 通过 | 四条命名路由实际运行；点数、废弃、大图、Esc、旧入口收口均验证；打印不适用 |

## 4. 问题标签

- `算不准`
- `点错风险`
- `协作断裂`
- `追溯不足`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 已确认页面只剩整单信息，无法追溯颜色尺码数量 | 追溯不足 | 后道回货确认员、主管 | 增加确认后和废弃后的只读 SKU 表 | 否 |
| PDA 提示容易理解成“任何差异都要授权” | 算不准 | 后道回货确认员 | 逐 SKU 实时显示 5%内/超 5%结果，整单不参与抵消 | 否 |
| 旧通用交接页仍有差异和驳回分支 | 协作断裂、点错风险 | 后道回货确认员 | 后道类型只读展示并跳专用确认页，处理器同步阻断旧动作 | 否 |
| 错误回货登记无法退出主链 | 追溯不足 | 送货员、后道确认员 | 新增确认前废弃，保留明细与审计，不入仓不建质检 | 否 |

## 6. 最终结论

结论：通过

说明：领域规则、页面源码契约、构建和当前分支浏览器验收均通过。Web 1366×768 和 PDA 400×786 已实际验证逐 SKU 明细、5%边界、超 5%复点与授权、自动生成质检单、废弃只读结果、旧入口收口和图片大图。

## 7. 变更覆盖与验证

### 受管文件

- `src/data/fcs/post-finishing-full-flow.ts`
- `src/pages/process-factory/post-finishing/warehouse.ts`
- `src/pages/process-factory/post-finishing/shared.ts`
- `src/pages/pda-post-finishing-flow.ts`
- `src/pages/pda-sewing-self-return.ts`
- `src/pages/pda-handover-detail.ts`

### 页面路由

- `/fcs/craft/post-finishing/wait-process-warehouse?tab=returns`
- `/fcs/pda/handover/sewing-self-return`
- `/fcs/pda/post-finishing/return-confirm`
- `/fcs/pda/handover/PKH-POST-WOOLHO202605004`

### 验证命令

- `npm run check:post-finishing-full-flow`：通过（3 个生产单、15 次回货、75 条 SKU 明细）
- `npm run check:post-finishing-full-flow-surface`：通过（Web/PDA/共享事实与旧入口负向契约）
- `npm run check:post-finishing-cross-terminal-ui`：通过（15 条跨端链、14 个 UI 阶段）
- `node --import tsx scripts/check-post-finishing-sewing-self-return.ts`：通过（0 数量、5%边界、超限复点授权和旧入口收口）
- `npm run build`：通过（Vite 2396 个模块转换完成）
- `npm run check:list-page-governance:static`：通过（379 个列表页、17 个历史基线）
- Playwright 命名页面验收：通过（4/4：自助回货明细与废弃、Web 废弃后只读明细、旧入口收口、PDA 超 5%复点授权与自动质检）
- `npm run check:prototype-design-governance -- --all`：通过（6 个用户可见文件、1 个关联治理记录）
- CodeGraph 同步/状态：通过（1,581 个文件，待同步 0）
- 浏览器错误控制台：0 条 error、0 条 warning
- `npm run workflow:verify -- --output /private/tmp/higoods-post-return-final-20260903/task-receipt.json --task-boundary "后道回货逐SKU确认、5%授权、自动质检与废弃"`：通过，收据状态 `verified`，无阻塞项

### 浏览器证据

证据目录：`output/playwright/post-finishing-return-sku-20260903/`

- `web-pending-sku-detail.png`：Web 待确认的 5 行 SKU 点数表。
- `web-confirmed-auto-qc.png`：恰好 5% 不授权，最终确认后自动生成质检单及只读明细。
- `web-discarded-readonly.png`：废弃原因、操作人、时间和 5 行明细保留，无点数或送检动作。
- `pda-self-return-sku-detail.png`：自助回货最近登记展开显示 5 行颜色、尺码和数量。
- `pda-return-confirm-five-skus.png`：PDA 400×786 逐 SKU 点数页。
- `pda-over-five-authorization.png`：单个 SKU 二次点数仍超 5% 后自动进入授权。
- `pda-legacy-entry-converged.png`：旧交接详情仅保留明细和专用逐 SKU 入口，无“数量有差异”和“驳回”。

### 真实图片验证

- 沿用现有每个 SKU 对应的正式款式图片和统一大图组件，没有新增占位素材。
- 浏览器已确认图片与 SPU/SKU、颜色、尺码在同一个信息块。
- 大图弹窗、关闭、遮罩和 `Esc` 已实际验证；加载失败态沿用既有公共组件契约。

### 例外

- 当前完成原型代码和本地浏览器验证，不代表物理 PDA、真实扫码枪或生产部署验收。
- 两项跨全交接模块的历史检查仍分别报告“中转袋缺少移除菲票”和“印花备货详情缺少备货物料标签”；不属于本次后道回货改动文件与需求，但不得把它们写成通过。
