# 印花染色加工单与合并染色方案审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-07-16 |
| 相关需求 / 任务 | 染色、印花加工单来源统一为正式生产单或备货；删除按加工需求创建及独立染色、印花需求单页面；批次生成失败不得留下半完成状态；建立合并染色固定顺序分配规则；生产单变更后按稳定 BOM constituent 身份自动同步未执行加工单并保护真实执行与合并染色快照；缺少稳定物料映射时阻断执行，正式替代物料名称同步到当前事实，加工单提交异常时生产事实与加工单侧共同回滚 |
| 涉及系统 | FCS |
| 涉及页面路径 | 删除 `/fcs/process/dye-requirements`、`/fcs/process/print-requirements`；保留 `/fcs/production/demands`、`/fcs/process/dye-orders`、`/fcs/process/print-orders`、PFOS 染色/印花加工单列表与详情、PDA 执行详情、交出列表与交出详情页 |
| 端类型 | 管理端 |
| 主要角色 | 生产计划、生产跟单、加工跟单、工厂执行人员 |
| 主要任务 | 正式生产单自动生成加工单；无生产单时由业务人员按备货创建；工厂继续通过同一任务执行 |

## 2. 参考规范

- `docs/higood-indonesia-factory-product-design-guidelines.md`
- `docs/higood-indonesia-factory-prototype-review-checklist.md`

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色匹配 | 通过 | 加工单页面保持 FCS 管理端定位；新增合并染色页面明确面向染厂主管，手工选择和结果登记由主管完成，未向一线员工暴露批次判断。 |
| 任务清晰度 | 通过 | 加工单页面仅保留“按备货创建”；合并染色页面突出创建、一次完成、更正和删除，生产单来源与平台加工单号只读展示。 |
| 信息架构与导航 | 通过 | FCS 菜单、路由、renderer 与 handler 已删除独立染色/印花需求单入口，不保留重定向；“合并染色”紧跟“染色加工单”并使用独立路由，PFOS 与 PDA 继续使用同一加工单和任务标识。 |
| 页面模式 | 通过 | 管理端和主管端使用标准列表、筛选、摘要、分页、抽屉与确认弹窗；合并染色主列表、创建候选、成员分配、执行更正历史和删除历史均使用统一分页组件，少量或空数据也保留总数与页码口径；PDA 执行方式未新增层级。 |
| 信息负荷 | 通过 | 来源压缩为“生产单自动生成 / 按备货创建”两类，删除关联需求选择和关联需求列表。 |
| 文案 | 通过 | 页面均使用中文业务文案，不显示英文来源码；备货创建明确要求物料、数量、工厂和工艺。 |
| 数量与状态 | 通过 | 生产单加工数量继续按技术包/BOM 计算；备货数量人工填写并校验正数且不超过所选库存可用数量，等于可用数量时允许创建，单位只读继承真实库存；合并染色数量合同固定为最多 3 位小数且乘 1000 后不超过安全整数，合法十进制按 `Number.toString()` 结构精确转为千分之一整数，只有固定 `4 × Number.EPSILON` 内的机器尾差允许归并；分配全程使用最小单位整数运算，确保分配量、未满足量和超出量守恒；产出按生产单下单时间、生产单号固定顺序分配，只满足剩余需求并单列超出数量。合并任务创建时不填实际投入、产出；完成和更正都先校验原始输入去空格后非空，空值不会被转换成 0 或调用领域函数，用户明确输入 0 才交给领域规则处理；完成只登记一次，投入必须大于 0、产出允许为 0；更正保留全部历史版本且只让最新版本参与有效满足累计；软删除保留历史事实并释放活动占用。生产单变更只自动覆盖尚无真实开始、完成、交出或审核事实且未被合并任务保护的加工单；已执行或有合并分配的加工单保留原数量、单位和物料快照并追加影响记录。加工单保留业务计划完成时间，联合水溶原状态流保持不变。 |
| 扫码与识别 | 通过 | 本次不涉及扫码入口或识别方式。 |
| 防错 | 通过 | 来源字段在加工单、PDA 任务、移动索引、PDA 交出单头/记录、仓库、打印卡与执行写回中保持互斥；染色、印花旧 Mock 均在 seed 阶段一次性归一，不再依靠列表查询修改来源；备货创建只展示同工厂、同工序、已入待加工仓且零差异的真实面料库存，领域层再次复核库存归属、工序、状态、差异、身份、单位、正数数量、可用数量上限和计划完成时间；生产单物料替换的原面料下拉只展示具备稳定 `canonicalMaterialId` 的面料事实，执行层和加工快照 helper 再次校验 origin identity，旧表单或伪造 fact ID 均以明确中文错误阻断，不能以空 target map 返回成功；合并任务创建只接收染色加工单 ID，由只读 canonical registry 回读平台加工单事实后快照，调用方不能覆盖工厂、稳定面料 ID、目标颜色、工艺、数量或生产单身份；拒绝不存在、重复、备货来源、非单一生产单来源和缺稳定面料 ID。候选不增加状态、可投缸或“已全部满足”门槛，只校验生产单来源、未被未删除合并任务占用，以及同染厂、同稳定面料 ID、同目标颜色和同染色工艺。合并分配继续要求加工单与生产单 ID/单号 trim 后非空并按规范值防重，拒绝非法数量、混合单位和非法下单时间，且不接受人工分配值覆盖。下单时间只接受无时区业务格式并逐字段校验，排序不依赖运行环境时区。 |
| UI 样式 | 通过 | 复用现有企业后台标准列表、宽表、卡片、筛选、分页、按钮、徽标、表单、抽屉、弹窗和 Toast 样式。 |
| 组件交互 | 通过 | 备货与合并染色创建错误均在抽屉就地展示；合并列表排序、列显隐、列冻结、列拖拽顺序、分页和所有任务弹层只局部刷新 workspace/overlay/toast，输入不触发整页重绘。列偏好默认值每次创建深拷贝；恢复默认会清除当前路由本地偏好并恢复默认列、顺序、冻结列、每页 10 条和未排序状态。四类弹层分页状态相互独立，候选翻页不清除已选加工单。 |
| 协作关系 | 通过 | 生产单、加工单、PFOS、PDA 交出事实和厂内待交出仓共享同一来源；厂内仓库按交出单来源输出真实备货物料或真实生产单，不再把任务 ID 当作生产单 ID。当前及后续生产单物料变更先从当前 UI 事实取得稳定 `canonicalMaterialId`，再逐生产单从真实物料事实精确反查该单自己的正式版本、BOM 行、fact ID 和旧 snapshot material ID；只按各单 `sourceBomItemId` 命中 constituent，保留 BOM 行并替换实际物料，不跨单复用当前单版本或 BOM。正式替代物料从物料档案结构化取得 ID、编码和中文名称，facts identity、正式版本和当前展示名称同步更新，UI 继续使用同一 fact ID 展示新名称。版本关系型结果使用逐单预测同步 current facts、正式版本关系与加工单快照；无加工单时也同步 facts、relation 和 history；事件显式传入稳定变更记录 ID。整批同步候选在生产变更持久化前完成校验和准备；染色、印花、PDA 与合并染色 impact 的 prepared 结果都保存深克隆 before state，批次逐项提交，后项异常时逆序回滚已提交项，再把生产 facts/history/relation/record 落为整批回滚，且始终保留原加工单 ID、加工单号、任务 ID 与任务号；内部加工需求明细仅作追踪，不再作为创建入口。 |
| 异常与追溯 | 通过 | 生产单保留下单时间和单号，备货保留物料标识；底层产物 ID 仅用于系统追踪，不冒充业务来源。正式快照以 `materialItems` 保存 BOM 行、实际物料 ID 和名称，兼容 `materialId/materialName` 只由结构化构成确定性派生；已执行加工单和合并染色任务使用追加式 `changeImpact` 历史记录变更前后快照、保护原因、记录时间和建议动作；自动同步使用独立历史记录，重复变更事件按记录 ID 去重。缺少 origin 稳定映射时整单阻断；canonical 不匹配的后续单保持原事实。第二个 prepared commit 前发生异常的真实表单回归已逐项验证 facts/history、relation、染色/印花加工单、PDA 任务、合并染色 impact 全部恢复，变更记录只保留 `ROLLED_BACK`，不残留 `DONE`。 |
| 现场设备可用性 | 通过 | PDA 继续读取同一任务；备货任务在交出列表和详情中直接显示备货物料名称与编码，不展示或伪造生产单号。 |

## 4. 问题标签

- `协作断裂`：已通过整批预校验修复。
- `追溯不足`：已通过保留正式生产单与加工单唯一来源关系修复。

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 批次中后续生产单校验失败时，先前生产单可能已进入页面状态 | `协作断裂` | 生产计划、生产跟单 | 先转换并缓存整批工艺快照，全部成功后再一次更新生产单与需求状态，最后生成加工单 | 否 |
| 快照转换与加工单注册使用不同校验，部分无效快照会在页面状态写入后才失败 | `协作断裂` | 生产计划、生产跟单 | 抽取唯一无副作用校验函数，快照转换返回前与加工单注册前共同调用 | 否 |
| 小写工艺码校验通过后未生成加工单 | `追溯不足` | 生产计划、生产跟单 | 运行时仅接受大写 `DYE`、`PRINT`，其他值使用中文错误明确阻断 | 否 |
| 加工单可从需求单选择创建，造成来源语义重复 | `协作断裂` | 加工跟单 | 删除按需求创建 API、需求选择 UI 与 `sourceDemandIds/linkedDemands`，正式来源统一为生产单或备货 | 否 |
| 独立染色、印花需求单页面仍作为菜单和路由暴露，造成业务对象重复 | `协作断裂`、`读不懂` | 生产计划、加工跟单 | 删除两页及其菜单、静态路由、异步 renderer、事件 handler 和无人消费的创建意图桥；不保留兼容跳转，生产准备产出中的内部需求记录改为进入对应加工单列表，内部加工需求明细继续作为追踪数据存在 | 否 |
| 备货加工单可能借用假生产单号进入下游 | `防错不足` | 加工跟单、工厂执行人员 | 备货来源使用独立物料标识；下游按来源类型展示，不创建虚假生产单关联 | 否 |
| 备货任务和仓记录以空字符串占位生产单 / 需求字段 | `追溯不足` | 工厂执行人员、仓管 | PDA、移动索引、待加工 / 待交出仓及执行写回统一使用来源联合；备货只保存备货物料，生产单只保存生产单 | 否 |
| PDA 交出单头固定读取生产单号，备货来源进入交出页后丢失 | `防错不足` | 工厂执行人员、仓管 | PDA 交出单头和记录透传来源联合；页面按来源展示“生产单号”或“备货物料”，不渲染空生产单号 | 否 |
| PDA 交出详情固定渲染生产单号，备货点击详情后显示空生产单 | `追溯不足` | 工厂执行人员、仓管 | 详情主身份、任务关联和来源追溯统一按来源分支；备货显示物料名称与编码，生产单保留可追溯单号 | 否 |
| 备货创建允许自由填写库存身份，可能创建仓库中不存在或单位不一致的加工单 | `选不对`、`追溯不足` | 加工跟单、仓管 | 抽屉改为选择真实待加工仓面料库存；名称、编码、单位只读带出，领域提交再次按库存 ID 复核全部事实 | 否 |
| 备货计划数量可超过真实库存可用数量，错误请求可能提前创建加工单或 PDA 任务 | `算不准`、`协作断裂` | 加工跟单、工厂执行人员、仓管 | 库存选项与摘要显示可用数量，数量输入提供 `max` 辅助；染色、印花领域入口在创建任何事实前校验上限，超量返回中文错误并保持加工单、PDA 任务和仓库事实数量不变 | 否 |
| 创建失败只在页面顶部提示，抽屉内看不到原因且输入触发整页重绘 | `看不懂`、`操作成本高` | 加工跟单 | 错误放到提交按钮上方，任一字段纠正即清除；所有创建字段和抽屉动作跳过整页重渲染 | 否 |
| 联合水溶染色删除旧需求建单后缺少完整状态机回归 | `协作断裂` | 染厂操作员、主管、仓管 | 回归改用正式生产单染色加工单注册入口，覆盖单位、暂停决策、重复动作、数量回退、零产出、PDA 同步和唯一最终交出 | 否 |
| 旧印花 Mock 在列表查询时才转成备货来源，seed 已创建的交出单头和记录仍保留伪生产单 | `协作断裂`、`追溯不足` | 印花工厂执行人员、仓管、加工跟单 | seed 完成加工单创建后一次性写回加工单、PDA 任务、既有交出单头和交出记录；列表查询改为纯读取，6 条旧备货 Mock 全量断言跨端来源一致 | 否 |
| 厂内待交出仓把任务 ID 填入生产单 ID，备货和生产来源均可能失真 | `追溯不足` | 仓管、加工跟单 | pending 与已交出库存生成链都按交出来源映射；备货只输出物料 ID/名称，生产来源只保留交出事实中的真实生产单 ID/单号，缺少真实 ID 时不再伪造 | 否 |
| 待加工仓只按“面料且有数量”提供候选，裁床差异库存可能被染色或印花误选 | `选不对`、`协作断裂` | 加工跟单、仓管 | 候选保留工厂、工序、库存状态与差异数量，只返回当前工厂对应 DYE/PRINT、已正常入仓且零差异的库存；领域层独立复核，切换工厂时清除失效选择 | 否 |
| 默认演示仓库没有合格染色/印花备货，抽屉在真实 seed 下无数据可选 | `看不懂`、`操作成本高` | 加工跟单 | 在工厂内部仓库 seed 中增加同一可派工厂的染色 360 米和印花 280 米合格入库事实，页面仍只从仓库领域动态读取 | 否 |
| 正式生产单的联合水溶语义只在直接注册染色加工单时传入，生产单统一生成入口可能漏掉水溶节点 | `协作断裂` | 生产计划、染厂操作员 | 从 DYE 工艺实际绑定 BOM 的 `waterSolubleRequirement` 推导快照标记，并经统一 ensure 入口传入染色加工单，验证执行路线包含水溶后染色 | 否 |
| 同一生产单的染色工艺同时绑定需水溶与不需水溶 BOM，使用任一命中会把整张染色单错误升级 | `算不准`、`协作断裂` | 生产计划、染厂操作员 | 快照转换阶段要求同一 DYE 绑定 BOM 的水溶属性完全一致；混合时用中文提示先统一正式 BOM 工艺属性，并在生产单、需求、加工单和 PDA 写入前整批阻断 | 否 |
| 染色列表/get 查询在读取时改写 PDA 任务来源，导入顺序可能影响跨端事实 | `协作断裂`、`追溯不足` | 染厂操作员、加工跟单 | 参照印花做 seed 时一次性归一加工单与 PDA 任务，list/get 改为纯读取；重复查询前后任务来源快照保持不变 | 否 |
| 备货来源任务流转卡和任务交货卡仍固定渲染生产单号或“按备货创建”占位 | `看不懂`、`追溯不足` | 工厂执行人员、仓管 | 卡片模型补充生产单/备货来源联合，备货卡显示物料名称与 ID，不渲染空生产单号或占位生产单号 | 否 |
| 合并染色实际产出若由人工拆分，可能因输入顺序或覆盖值导致生产单分配不稳定 | `算不准`、`协作断裂` | 生产计划、加工跟单、染厂主管 | 使用无存储副作用的领域函数，按生产单下单时间升序、同时间生产单号升序自动分配；每单最多满足剩余需求，超出数量单独返回，输入顺序与外部人工分配字段不影响结果 | 否 |
| 合并染色任务若允许修改成员、重复完成或覆盖更正历史，会破坏生产单满足量事实 | `算不准`、`协作断裂`、`追溯不足` | 生产计划、加工跟单、染厂主管 | 创建即快照并锁定至少两张同染厂、同面料、同颜色、同工艺的生产单来源加工单；任务只完成一次，更正递增版本且旧版本永久保留，读取时只累计每个任务当前版本 | 否 |
| 删除合并任务若直接撤回已完成分配，会让历史产出与后续剩余需求失真 | `算不准`、`追溯不足` | 生产计划、加工跟单 | 待染色和已完成均采用软删除；删除释放未删除任务占用，但已完成任务当前版本的有效分配继续累计，后续任务只处理剩余需求 | 否 |
| 合并任务若信任调用方提交的完整加工单对象，可伪造工厂、面料、颜色、数量或生产单身份 | `选不对`、`协作断裂`、`追溯不足` | 生产计划、加工跟单 | 创建 API 仅接收加工单 ID；通过窄只读 registry 回读 canonical 加工单并返回克隆，合并领域只比较平台稳定 `materialId`，不以展示名称或备注回退身份 | 否 |
| 生产单变更直接覆盖已执行或已加入合并染色的加工单，会让现场实物、PDA 与合并成员快照失真 | `协作断裂`、`追溯不足` | 生产跟单、加工跟单、染厂/印花厂执行人员 | 明确列出染色、印花尚未执行状态，并以真实执行节点、完成、交出、审核及合并分配事实作为保护依据；保护时只追加变更影响，不修改成员、分配版本、加工单 canonical 身份或 PDA canonical 身份 | 否 |
| 生产单变更失败、事实过期、批次后项无效或持久化回滚后仍同步加工单，会形成半生效状态 | `协作断裂`、`追溯不足` | 生产跟单、加工跟单 | `executeProductionChange` 在调用持久化前解析、全量校验并准备全部染色、印花和合并影响候选，不产生 store 写入；任一快照无效时不调用持久化，只有全部生产单 facts/history/relation 持久化返回 `DONE` 后才执行一次幂等纯内存提交。第二张生产单发生重复变更记录冲突时，恢复整批 facts 原顺序、全部 relation 和加工单快照；回滚、异常和过期事实均不触发加工单提交 | 否 |
| 当前及后续生产单分别绑定不同正式版本和 BOM，或一张加工单聚合多个 BOM 时，物料替换误复用当前单身份并覆盖错误物料 | `协作断裂`、`选不对` | 生产跟单、加工跟单、染厂/印花厂执行人员 | UI 继续提交当前单真实物料事实 ID；当前事实只提供跨单稳定 `canonicalMaterialId`，系统逐 affected order 精确反查目标事实保存的 `sourceTechPackVersionId/sourceBomItemId/snapshotMaterialId`。正式快照保存 `materialItems`，只替换各单命中的 `sourceBomItemId` constituent，legacy singular 只用目标事实显式旧 snapshot ID；缺映射、版本不一致或未命中时该单 facts/relation/workorder 完整保持原事实 | 否 |
| 版本关系型生产变更仍向加工单同步旧技术包身份，或 helper 与持久化各自猜测版本号 | `追溯不足`、`协作断裂` | 生产跟单、加工跟单 | 共享无副作用的正式版本身份预测函数；版本关系型结果稳定选当前关系中的最新已发布 ID/标签，生产补丁保持当前 ID/标签；事件在持久化前生成同一预测结果，同时用于 post-change 加工快照和持久化 current facts/关系，重复变更记录 ID 幂等 | 否 |
| 缺少 stable canonical identity 的旧面料事实仍可被下拉选中，空 target map 使物料替换假成功 | `选不对`、`协作断裂` | 生产跟单 | 下拉只返回具备 canonical identity 的面料事实；表单校验、执行准备和加工快照 helper 三层校验 origin，缺映射用中文错误阻断。`PO-202604-0018 / FAB-P02` 真实 seed 明确验证不可选且伪造输入不进入 DONE | 否 |
| 替换后当前事实继续显示旧面料名称，identity 与跟单看到的下拉文案不一致 | `读不懂`、`追溯不足` | 生产跟单、加工跟单 | `applyProductionOrderMaterialFactReplacement` 接收正式物料档案结构化快照，同时更新 canonical/snapshot/version 和 `${materialCode} ${materialName}`；真实 UI 断言同一 fact ID 只显示新名称 | 否 |
| 生产 facts 已保存 DONE 后，加工单批次中途提交异常被吞掉并返回成功 | `协作断裂`、`追溯不足` | 生产跟单、加工跟单、工厂执行人员 | Dye/Print/Combined prepared 统一提供幂等 rollback；AUTO_SYNCED 深克隆恢复加工单与 PDA，PROTECTED 恢复 impact，Combined 恢复原 `changeImpact`（包括原 undefined）；batch 记录已提交项并在异常时逆序回滚。执行编排随后调用 `persist(ROLLED_BACK)` 恢复生产事实侧，绝不返回已保存的 DONE | 否 |
| 合并染色领域能力缺少染厂主管独立工作入口，现场只能依赖底层数据操作 | `操作成本高`、`协作断裂` | 染厂主管、加工跟单 | 在“染色加工单”后新增独立“合并染色”菜单和标准列表页，接入列表、创建、详情、一次完成、更正、软删除及已删除查看；平台加工单号全程只读，成员创建即锁定 | 否 |
| 主管手工选择时可能把不同染厂、面料、颜色或工艺的加工单误选进同一任务 | `选不对`、`防错不足` | 染厂主管 | 创建抽屉只列生产单来源加工单；首张确定四项兼容条件，其他不兼容项显示具体原因并禁选；不增加“可投缸”等额外状态限制，提交仍由领域层复核 | 否 |
| 合并染色宽表和抽屉交互若整页重绘，会造成输入丢失、滚动跳动和主管误操作 | `操作成本高` | 染厂主管 | 页面采用标准列表页、标准宽表和统一分页；排序、列显示、列顺序拖拽、冻结、分页、筛选及抽屉/弹窗均按区域局部刷新，图标仅 hydrate 新区域，列偏好按路由持久化 | 否 |
| 列偏好默认对象与运行时状态共享引用，恢复默认后可能保留已修改顺序、冻结列或每页条数 | `操作成本高`、`防错不足` | 染厂主管 | 默认偏好改为每次生成独立数组；加载、修改和恢复均返回新对象，恢复同时清理路由 localStorage、排序和页码 | 否 |
| 创建候选、成员分配和历史记录未分页，数据增长后难以定位且不符合列表治理 | `操作成本高` | 染厂主管、加工跟单 | 候选、成员、执行更正版本和删除历史均复用统一表格分页，默认 10 条；选择跨页保留，兼容禁选理由按当前页持续展示 | 否 |
| 空投入或空产出被 `Number('')` 转为 0，可能绕过必填语义并提前调用领域写入 | `算不准`、`防错不足` | 染厂主管 | 完成与更正共用原始字符串解析 helper，先 trim 非空再转数值；空投入、空产出四种提交路径均断言领域调用次数为 0 | 否 |
| 合并染色页面在 render 时注册演示加工单，会让 canonical 与 PDA 数据取决于导航顺序 | `协作断裂`、`追溯不足` | 染厂主管、加工跟单、PDA 操作员 | 两张兼容演示生产单加工单迁入染色领域统一 seed，页面删除注册入口并保持纯读取；中央 seed 按生产单 ID 防重并同步同 ID PDA 任务，重复 render 和同生产单重复注册均不改变 store | 否 |

## 6. 最终结论

结论：有已知基线问题的通过（DONE_WITH_CONCERNS）

说明：

- 本次调整管理端加工单来源、备货创建抽屉和下游来源展示，并删除独立染色/印花需求单菜单与路由；不改变加工单主状态流与现场执行动作。
- 正式生产单自动生成与按备货创建互斥；内部加工需求详情继续保留用于追踪，但不再提供创建加工单入口。
- 动态检查覆盖加工单、PDA 任务、移动执行索引、PDA 交出单头/记录、真实交出详情渲染和仓库记录的双向来源互斥；备货链路不再以空字符串伪装生产单或需求，交出列表和详情均按真实来源显示。
- 6 条历史印花备货 Mock 已逐条覆盖：007、008、010 固定校验交出单头、记录及厂内仓库条目数量，009、011、012 明确校验三类数量均为 0；加工单、PDA 任务、移动索引和实际存在的库存条目逐项校验同一备货来源。列表查询不再产生来源归一副作用。
- 厂内待交出仓的待交出条目与已交出条目均按来源联合生成；库存来源无生产单字段，生产来源不再使用任务 ID 冒充生产单 ID。
- 备货抽屉复用工厂待加工仓真实库存，成功列表和详情同时显示真实物料、原始单位与计划完成时间；提交失败在抽屉内就地提示，字段输入不重建整页。
- 备货数量等于当前可用库存时可建单，超过一个最小数量单位时由领域层阻断；页面 `max` 仅作输入辅助，超量失败不会创建加工单、PDA 任务或仓库事实。
- 默认演示数据已提供合格染色与印花备货；裁床工序、差异待处理、跨工厂或跨工序库存均不会进入候选或通过领域提交。
- 联合水溶状态机回归已从正式生产单技术包快照构建入口进入统一加工单确保入口，未恢复需求单创建器或伪需求数据。
- 同一生产单仍坚持一个同类工艺只生成一张加工单：全部需水溶生成一张联合水溶染色单，全部不需生成一张普通染色单，混合属性不拆单且不整单升级；修正后重试只新增一张染色单和一个 PDA 任务。
- 染色加工单 list/get 不再承担 seed 来源修复；备货任务流转卡与交货卡统一显示备货物料，生产单来源继续显示真实生产单号。
- 已用真实页面状态检查覆盖缺 BOM、混合数量单位、无效数量、空技术包版本、空物料名、重复失败重试以及修正后单次成功落库。
- 合并染色分配继续覆盖 0、部分、足量和超量产出，三位小数安全合同、整数域数量守恒、单位与身份校验、固定排序、非法时间、跨时区一致性及输入不可变性；本阶段进一步覆盖真实生产来源与真实备货来源工单、同染厂/面料/目标颜色/工艺合并约束、成员快照和占用、一次完成、短产、零产出终止、更正版本、待染色/已完成软删除、删除后释放、有效满足累计、深克隆和加工单只读投影。未生成投缸、补染、继续染或后续任务记录。
- 合并任务 canonical 回读对抗覆盖伪造/不存在/重复 ID、旧完整对象输入、同名不同 `materialId`、同 `materialId` 不同展示名、缺失稳定面料 ID，以及调用方修改已返回加工单对象无法覆盖平台工厂、面料、颜色、工艺、数量和生产单身份；所有校验完成后才占用任务序号，失败后首个成功任务仍为 `HBRW-000001`。完成备注可省略并规范为空字符串，更正原因继续必填。
- 生产单变更后的加工单同步已覆盖未执行染色/印花自动更新、真实执行快照保护、活动合并任务双边影响留痕、未完成合并任务删除后释放同步、已完成分配继续保护、重复事件幂等和多次不同变更历史保留；STOCK 来源不参与生产单同步。事件层数量变更按当前数量到确认数量的比例派生加工数量；物料替换从真实 UI 事实 ID 解析稳定 BOM constituent，替换为已确认的真实面料档案 ID 与名称，并显式传入变更记录 ID 和执行时间，不再依赖旧生产单默认快照或拆分展示字符串。
- 0004、0101、0102 显式共享同一跨单业务物料 `canonicalMaterialId`，但各自保留不同正式版本、BOM 行和旧 snapshot material ID；当前 UI 仍提交 0004 的 fact ID，系统逐单反查 target identity。三单只替换各自命中 constituent，其他 constituent 不变；canonical 不匹配的 0007 facts、relation 和 workorder 保持 unchanged。`materialId/materialName` 由 `materialItems` 确定性重建，三单 current facts、relation、history 与加工单最终快照一致；0102 无加工单的 CURRENT_ONLY 仍完成 facts/relation/history 持久化。第二单持久化冲突会回滚整批 facts/relation/workorder，CURRENT_ONLY 不生成后续快照。VERSION_RELATION / VERSION_AND_PATCH 使用逐单最新正式版本，PRODUCTION_PATCH 保留该单当前版本。
- 缺少正式加工快照和 stable canonical identity 的 `PO-202604-0018 / FAB-P02` 不补造 BOM：原面料下拉不展示，旧表单或伪造 fact ID 在执行层明确阻断。正式替换后的 facts 保留原 fact ID/source BOM 行，并以物料档案编码和中文名称刷新当前展示；旧名称不再出现在当前下拉。
- 加工单侧提交现在具备最小 prepared rollback 协议，不引入通用事务框架。批次重复 commit 幂等，重复 rollback 不破坏旧状态；真实表单在第二个 prepared commit 前注入异常后，第一项已提交状态会逆序恢复，染色/印花加工单、PDA 任务、合并染色 impact 与生产 facts/history/relation 均回到执行前，变更记录为 `ROLLED_BACK`。
- 合并染色主管工作台已新增：菜单紧跟染色加工单，列表默认每页 10 条并支持宽表列治理；创建抽屉以平台加工单号作为唯一只读身份，只允许生产单来源，首项锁定同厂、同面料、同目标颜色和同工艺，其余候选即时说明不兼容原因。页面不增加“可投缸”或备料状态限制。
- 任务成员创建后无增删入口；详情按生产单下单时间和生产单号展示系统顺序，完成只登记一次实际投入/产出，短产直接显示未满足并终止；更正只改投入/产出并保留版本，待染色和已完成任务都可软删除并永久展示执行、分配和删除历史。
- 合并染色列表的筛选、分页、排序、列显隐、列冻结、列顺序拖拽、创建选择、详情、完成、更正和删除均采用 workspace/overlay/toast 局部 DOM 更新，输入事件不触发整页重绘，新增区域单独 hydrate 图标。当前分支已正常合并主线完整列表治理，不维护局部治理副本；新页面直接通过实际列表页静态治理和标准模板检查。
- 创建候选、成员分配、执行更正版本和删除历史均默认每页 10 条并始终展示统一分页口径；候选跨页选择保留。列偏好恢复使用全新默认对象，清除当前路由 localStorage，并恢复默认顺序、显示列、冻结列、每页条数及排序。
- 完成与更正的投入、产出先校验原始输入非空，空字符串不再等同于 0；用户明确输入 0 时仍按领域规则处理。已全部满足的生产单来源加工单不再因满足状态禁选或被领域拒绝，仍受活动任务占用和四项合并身份校验约束。
- 两张合并染色兼容演示加工单已从页面 render 移入 `dyeing-task-domain` 中央 seed；首次读取染色加工单时同时建立 canonical 与同 ID PDA 任务。页面首次和重复 render 前后加工单/PDA ID 集合不变，同生产单重复注册返回实际已有 canonical ID，不制造第二张加工单。
- PDA 仅在未执行时同步必要展示事实，不重建任务或改号。批量 prepare 阶段不写 store，第二条失败不会留下第一条半同步且不会调用生产变更持久化；持久化成功后 commit 幂等且不再执行业务校验。
- 未发现需要偏离设计规范的例外；既有“送货交出”文案统一问题不属于本次来源迁移范围。
- 已知基线：`npm run check:fcs-handover-domain` 仍会报告 `TASKGEN-202603-082-002__ORDER` 的二维码值串到 `003`。该 TASKGEN seed 串号在本任务开始前已记录，且本次 diff 未修改对应 runtime/task seed；按本次边界不扩大修复。
