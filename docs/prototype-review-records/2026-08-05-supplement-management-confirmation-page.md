# 补料管理筛选、对象选择与页面确认原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-08-05 |
| 相关需求 / 任务 | 按《补料业务调整方案》压缩列表筛选区、精简新增补料对象选择区，并将长确认弹窗改为同页确认总表 |
| 记录模式 | 完整产品审查 |
| 涉及系统 | PFOS |
| 涉及页面路径 | 补料管理列表、新增补料、确认创建补料单 |
| 端类型 | 管理端 / 主管端 |
| 主要角色与任务 | 裁床业务人员筛选补料单、选择补料对象、核对补料构成和供应节点后确认创建补料单 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：补料管理的十二个筛选条件及两个操作按钮改为固定两行；新增补料对象选择区删除重复标题、说明和最外层线框；提交补料后不再打开长弹窗，而是在同一页面进入第三步，使用一张横向总表合并补料构成、物料需求、各仓库存、采购、染色、印花、中转仓配料和本次处理；取消独立风险提示和“已知悉风险”按钮；返回修改保留已选对象、原裁片单、数量、原因和说明。

当前审查依据：

- `AGENTS.md` 第 4 节印尼工厂现场产品设计基线。
- `AGENTS.md` 第 5 节 UI、标准列表和真实图片门禁。
- `AGENTS.md` 第 7 节分层验证与证据新鲜度。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 页面仍面向裁床管理与主管人员；创建入口、业务对象和最终落单动作不变，仅压缩信息并将确认改为可完整浏览的页面步骤。 |
| 文案、状态、数量与单位 | 通过 | 总表逐行保留补料件数、物料需求及各节点数量和单位；同时需要染色和印花时明确“先染色，再印花”；不展示整体预计可配料时间。 |
| 扫码、真实图片与对象识别 | 通过 | 本次未改变款式与物料图片来源；对象选择、确认总表仍将真实缩略图与物料名称、编码放在同一信息块，并沿用高清大图和失败态。 |
| 防错、危险确认与主管兜底 | 通过 | 无库存且无采购在途时仅在物料行显示“不建议创建”和确认后的采购缺口，不增加独立风险提示；最终按钮防重复点击，业务可返回修改。 |
| 交接、跨端事实与异常追溯 | 通过 | 确认总表只汇总既有补料供应事实，最终确认仍形成同一补料单、采购/印染单和独立中转仓配料需求，未改变下游引用。 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 1366×768、1280×720 的列表筛选均为两行且页面无横向溢出；确认宽表仅在表格容器内横向滚动。本次未修改 PDA、上传或弱网行为。 |
| 命名路由、交互、图片大图与打印 | 通过 | 补料列表和新增补料命名路由已验收；确认、返回修改、最终创建均可操作且不使用弹窗。本次未修改打印。 |

## 4. 问题标签

- 视觉干扰
- 字段过载

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 筛选条件宽度过大、占用三行 | 视觉干扰 | 裁床业务人员 | 将十二个筛选条件与筛选、重置按七项一行固定为两行，并在两种标准分辨率校验 | 否 |
| 对象选择区重复表达且外框层级过重 | 视觉干扰 | 裁床业务人员 | 删除重复标题、说明和最外层框，保留页签、搜索、结果、选中结果和下一步 | 否 |
| 二次确认弹窗内容过长，无法连续浏览 | 字段过载 | 裁床主管 | 改为同页第三步，用一张按物料分组的总表合并展示全部核对信息，底部固定返回和确认动作 | 否 |

## 6. 最终结论

结论：通过

说明：三项页面调整均已形成当前代码、专项自动化、标准分辨率和真实交互证据；未改变补料数量、采购判断、先染后印、中转仓配料需求及最终创建规则。

## 7. 变更覆盖与验证

### 受管文件

- `src/pages/process-factory/cutting/supplement-management.ts`

### 页面路由

- `/fcs/craft/cutting/supplement-management`
- `/fcs/craft/cutting/supplement-management?mode=create`
- `/fcs/craft/cutting/supplement-management?mode=create&releaseSnapshotId=cpr-target-po-14671-v9`

### 验证命令

- `npm run check:cutting-supplement-supply-decision`：通过。
- `npm run check:cutting-supplement-node-facts`：通过。
- `npm run check:cutting-supplement-process-work-orders`：通过。
- `npx playwright test tests/supplement-management-list-template.spec.ts --grep '补料管理筛选条件|新增补料对象选择区|补料确认改为页面总表'`：通过，3 项。
- `npx playwright test tests/supplement-management-list-template.spec.ts --grep '补料确认改为页面总表' --workers=1`：通过，返回修改保留草稿且不再出现错误提示。
- `npx playwright test tests/supplement-management-list-template.spec.ts --workers=1`：通过，38 项完整页面回归全部通过。
- `npx playwright test tests/cut-order-supplement-linkage.spec.ts --grep '补料详情只读|操作栏一次' --workers=1`：通过，2 项。
- `npm run build`：通过。
- `npm run check:list-page-governance`：通过；首次执行因本记录仍有“待补充最终结果”未通过，补齐实际结果后重新执行通过。
- `npm run check:prototype-design-governance -- --all`：通过；首次执行因本记录仍有“待补充最终结果”未通过，补齐实际结果后重新执行通过。

### 浏览器证据

- `output/playwright/supplement-list-1280x720.png`：列表页十二个筛选条件连同筛选、重置固定为两行，页面主体无横向溢出。
- `output/playwright/supplement-create-source-picker-1280x720.png`：对象选择区无重复标题、重复说明和最外层线框，所有款式与物料均有图片。
- `output/playwright/supplement-confirm-page-1280x720.png`：确认内容位于同一页面的一张总表中，不存在确认弹窗或独立风险提示。
- 返回修改实测：原裁片单 `CUT14671-B`、原因“尺码齐套不足”、说明“页面确认总表验收”均保留，且无“未找到对应的补料对象”误提示。

### 真实图片验证

- 款式和物料读取现有正式本地素材；为对象选择数据补齐了款式与正式素材的身份映射。
- 新增补料对象选择页逐条检查后无“图片未提供”；确认总表继续将物料缩略图、名称和编码放在同一信息块，加载、失败和高清大图能力保持不变。

## 8. 第二次核查清单

| 调整项 | 实现证据 | 自动化证据 | 页面证据 | 结论 |
| --- | --- | --- | --- | --- |
| 十二个筛选条件连同筛选、重置只占两行 | 两个固定筛选行，每行七项 | 两种标准分辨率用例通过 | 列表页 1280×720 截图 | 通过 |
| 删除对象选择区重复标题、说明和最外层线框 | 对象选择页仅保留页签、搜索、结果和下一步 | 对象选择区用例通过 | 对象选择页完整截图 | 通过 |
| 长确认弹窗改为同页第三步 | 页面确认总表 | 确认页用例通过且无对话框 | 确认页完整截图 | 通过 |
| 一张总表合并补料构成与全部供应节点 | 按物料分组的十列总表 | 列名、行合并和节点文案断言通过 | 确认页完整截图 | 通过 |
| 不展示独立风险提示 | 仅在物料行表达本次处理 | 风险提示及风险按钮不存在断言通过 | 确认页完整截图 | 通过 |
| 返回修改保留全部已填内容 | 确认草稿回填 | 原裁片单、原因、说明回填及误提示回归用例通过 | 浏览器返回修改实测 | 通过 |
| 最终创建及既有补料业务规则不变 | 复用既有供应决策和创建落单 | 供应、节点、印染加工单及裁片联动专项检查通过 | 确认页最终动作可见 | 通过 |
| 对象图片完整可识别 | 款式素材身份映射与既有物料图片 | 对象选择区无缺图断言通过 | 对象选择页逐条加载完成 | 通过 |

### 例外

- 无
