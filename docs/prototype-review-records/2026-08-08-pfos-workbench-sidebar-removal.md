# PFOS 工作台侧栏分组删除原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-08-08 |
| 相关需求 / 任务 | 删除 PFOS 侧栏中重复的“工作台 → 工作台 → 总览”导航结构 |
| 记录模式 | 完整产品审查 |
| 涉及系统 | PFOS |
| 涉及页面路径 | `/fcs/craft/workbench/overview` |
| 端类型 | 管理端 / 主管端 |
| 主要角色与任务 | 中央工厂管理人员、工艺工厂主管从 PFOS 侧栏识别并进入各工厂业务模块 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：PFOS 侧栏不再显示外层“工作台”分组、内层“工作台”入口和“总览”子项；侧栏直接从“裁床厂管理”开始。PFOS 默认落地页、右侧总览页面、顶部页签、路由、业务数据和页面交互均保持不变。

当前审查基线：

- `AGENTS.md` 第 4 节：印尼工厂现场产品设计基线。
- `AGENTS.md` 第 5 节：UI、列表和真实图片专项门禁。
- `AGENTS.md` 第 7 节：分层验证和证据新鲜度。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 删除重复导航层级后，侧栏直接展示各工厂管理模块，不改变管理端和主管端任务。 |
| 文案、状态、数量与单位 | 通过 | 只删除指定侧栏文案和导航层级，业务状态、数量和单位不变。 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 本次只改变桌面侧栏结构，不影响 PDA、弱网或上传能力。 |
| 命名路由、交互、图片大图与打印 | 通过 | 当前分支已在 1366×768 验收 `/fcs/craft/workbench/overview`；侧栏中指定三层入口均不存在，总览页仍可正常打开，控制台 0 错误、0 警告。本次不涉及图片和打印。 |

## 4. 问题标签

- 视觉干扰

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 侧栏“工作台”在分组、入口和子项三层重复，占用导航空间 | 视觉干扰 | 中央工厂管理人员、工艺工厂主管 | 删除整个 PFOS 工作台侧栏分组，保留默认总览页和路由 | 否 |

## 6. 最终结论

结论：通过

说明：

- 指定侧栏分组已完整删除，默认总览页和路由保留；文件契约、命名页面与菜单路由检查通过。

## 7. 变更覆盖与验证

### 受管文件

- `src/data/app-shell-config.ts`

### 页面路由

- `/fcs/craft/workbench/overview`

### 验证命令

- `npx playwright test tests/fcs-process-factory-shell-restructure.spec.ts --workers=1 --reporter=line --grep '顶部系统新增 PFOS'`：通过，1/1。
- `npm run check:menu-routes -- --systems pfos`：通过，157 个菜单地址无重复、无未覆盖路由。
- `npm run check:prototype-design-governance -- --all`：通过。
- 命名页面浏览器验收：通过；当前分支在 1366×768 验收 `/fcs/craft/workbench/overview`，侧栏直接从“裁床厂管理”开始，右侧总览页保留，控制台 0 错误、0 警告。

### 例外

- 完整运行 `tests/fcs-process-factory-shell-restructure.spec.ts` 时 4 个用例中 3 个通过、1 个失败；失败项是旧契约将“生产单总览”页标题预期为“生产单进度”，与本次删除 PFOS 工作台侧栏无关。本次命中的侧栏删除及默认总览入口用例已单独通过。
