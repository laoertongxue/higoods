# 来料异议数量解析与项目清理原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-09-04 |
| 相关需求 / 任务 | `BUG-001`、`UI-001`、项目工程与历史文件清理 |
| 记录模式 | 完整产品审查 |
| 涉及系统 | PFOS / FCS |
| 涉及页面路径 | `/fcs/craft/cutting/cut-orders` 及裁片单详情“差异处理”页签 |
| 端类型 | 管理端 / 主管端 |
| 主要角色与任务 | 裁床管理人员查看裁片单领取数量、差异和异议结果 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：修复来料异议辅助函数中空白字符与小数点被错误转义的问题。此前“长度 612.5 米”“卷数 8 卷”等合法文本会被静默解析为 0；修复后页面投影及异议预填能读取真实数值。页面结构、文案、状态、Mock 数据、路由、图片和交互入口不变。UI 骨架卡片重命名只消除同名导出，不改变页面输出。

完整产品审查的当前基线：

- `AGENTS.md` 第 4 节：印尼工厂现场产品设计基线。
- `AGENTS.md` 第 5 节：UI、列表和真实图片专项门禁。
- `AGENTS.md` 第 7 节：分层验证和证据新鲜度。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 不调整角色、任务或管理列表模式 |
| 文案、状态、数量与单位 | 通过 | 合法长度、卷数可按原单位解析，小数保留；非法或 0 值仍返回 0 |
| 扫码、真实图片与对象识别 | 通过 | 未修改扫码、图片资源或对象关系；命名页面图片正常渲染 |
| 防错、危险确认与主管兜底 | 通过 | 不改变提交门禁；避免合法数量被误判为 0 |
| 交接、跨端事实与异常追溯 | 通过 | 修复共享辅助函数，状态库和裁床投影继续读取同一数值口径 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 不修改布局、上传或网络处理 |
| 命名路由、交互、图片大图与打印 | 通过 | 当前工作树打开裁片单及详情“差异处理”页签，无浏览器控制台错误；打印未受影响 |

## 4. 问题标签

- `算不准`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 合法中文长度和卷数文本被解析为 0 | `算不准` | 裁床管理人员、异议处理人员 | 修正正则转义并增加长度、卷数和证据文件编号测试 | 否 |

## 6. 最终结论

结论：通过

说明：

- 专项测试覆盖带空格、无空格、小数、非法 0 值和连续空格文件名。
- 当前工作树命名路由可正常渲染裁片单及差异记录，浏览器控制台无错误。

## 7. 变更覆盖与验证

### 受管文件

- `src/components/ui/card.ts`

共享计算文件 `src/helpers/fcs-claim-dispute.ts` 不在第 4.6 节列出的治理目录内，但因其结果会进入页面投影，本记录仍将其纳入验证。

### 页面路由

- `/fcs/craft/cutting/cut-orders`
- `/fcs/craft/cutting/cut-orders/<裁片单标识>?tab=differences`

### 验证命令

- `npm run test:unit`：通过，3/3。
- `npm run typecheck:engineering`：通过。
- `npm run build -- --outDir /private/tmp/higoods-cleanup-build --emptyOutDir`：通过，2396 个模块完成生产构建。
- `npm run check:prototype-design-governance -- --all`：通过。
- Playwright CLI 打开裁片单、进入首条记录详情并切换“差异处理”：通过；页面显示带单位的计划/实际数量，控制台错误数为 0。

### 例外

- 无。
