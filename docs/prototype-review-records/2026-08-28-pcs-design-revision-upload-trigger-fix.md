# PCS 设计改款任务设计稿选择修复审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-08-28 |
| 相关需求 / 任务 | 修复“新建设计改款任务”弹窗点击“选择本地设计稿”不打开系统文件选择器 |
| 记录模式 | 完整产品审查 |
| 涉及系统 | PCS |
| 涉及页面路径 | `/pcs/engineering/design-revision` |
| 端类型 | 管理端 |
| 主要角色与任务 | 跟单创建设计改款任务并从本地选择、校验和保存真实设计稿图片 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：不改变页面结构、业务字段、状态、数量、Mock 数据和路由；修正新建弹窗内部原生文件选择控件被弹窗遮罩事件误拦截的问题。修复后，点击“选择本地设计稿”会立即打开系统文件选择器；选择不支持的文件仍阻断并保留已填表单，选择真实图片后显示文件名并允许创建任务。点击弹窗外遮罩、关闭按钮和取消按钮仍可关闭弹窗。

当前审查以 `AGENTS.md` 第 4、5、7 节为基线。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 仍由跟单在管理端新建设计改款任务并上传设计稿。 |
| 文案、状态、数量与单位 | 通过 | 文案和业务状态未变化；上传成功、失败反馈保持原口径。 |
| 扫码、真实图片与对象识别 | 通过 | 不涉及扫码；使用真实图片文件验证系统文件选择、读取和保存。 |
| 防错、危险确认与主管兜底 | 通过 | 非图片继续阻断，且失败后已填参照款、目标款和要求不会丢失。 |
| 交接、跨端事实与异常追溯 | 通过 | 本次不改变跨端交接；文件信息仍写入设计改款任务。 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 不涉及 PDA 和弱网；管理端弹窗内原生文件选择在当前桌面浏览器可触发，校验失败后可重新选择。 |
| 命名路由、交互、图片大图与打印 | 通过 | 命名路由不变；真实点击文件选择器、非法文件重选、真实图片保存和建单链路通过；不涉及打印。 |

## 4. 问题标签

- `点错风险`
- `组件误用`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 弹窗内部点击被外层遮罩错误判定为关闭弹窗动作，页面总事件分发取消了浏览器原生文件选择行为 | 点错风险、组件误用 | 跟单 | 弹窗内容区域的非业务动作点击不再声明为已处理，仅点击遮罩、关闭或取消时执行关闭；补充真实点击并监听系统文件选择器的回归测试 | 否 |
| 旧自动化直接向隐藏文件框写入文件，未覆盖用户真实点击 | 点错风险 | 产品、测试 | 改为先真实点击“选择本地设计稿”并确认文件选择器打开，再分别选择非法文件和真实图片 | 否 |

## 6. 最终结论

结论：通过

说明：修复范围仅限设计改款新建弹窗的原生文件选择触发；文件读取、校验、失败恢复、文件展示和建单规则均沿用现有业务口径。最终结论以第 7 节当前分支验证全部通过为准。

## 7. 变更覆盖与验证

### 受管文件

- `src/pages/pcs-independent-sampling.ts`

### 页面路由

- `/pcs/engineering/design-revision`

### 验证命令

- `PLAYWRIGHT_REUSE_EXISTING_SERVER=false CUTTING_E2E_PORT=4198 npx playwright test tests/pcs-design-revision-create-upload-ui.spec.ts --workers=1 --reporter=line`：通过（1/1；真实点击文件选择器、非法文件阻断、表单保留、真实图片读取、文件名展示和创建任务）
- `PLAYWRIGHT_REUSE_EXISTING_SERVER=false CUTTING_E2E_PORT=4199 npx playwright test tests/pcs-design-revision-create-upload-ui.spec.ts tests/pcs-design-revision-consolidation.spec.ts --workers=1 --reporter=line`：通过（设计改款专项契约通过；真实浏览器用例 1/1 通过）
- `npm run check:pcs-design-revision-consolidation`：通过
- `npm run build`：通过（Vite 生产构建，2354 个模块完成转换）
- `npm run check:prototype-design-governance -- --all`：通过（1 个用户可见文件、1 份关联治理记录）
- `git diff --check`：通过
- `codegraph sync && codegraph status`：通过（同步 2 个变更代码文件，索引为最新状态）
- 任务收据：`/private/tmp/pcs-design-revision-upload-fix-task-receipt.json`，状态 `verified`，无阻塞项。

### 真实图片验证

- 设计稿验证文件：`public/dress-sample-1.jpg`，作为跟单从本地选择的真实图片文件。
- 选择入口：新建设计改款任务弹窗“设计稿”区域。
- 验证结果：系统文件选择器可由可见按钮真实触发；真实图片读取成功后展示文件名；非法 PDF 被阻断并可重新选择。
- 款式缩略图、加载失败态和大图能力未被本次修复改动，后续专项回归继续核验既有能力。

### 例外

- 无
