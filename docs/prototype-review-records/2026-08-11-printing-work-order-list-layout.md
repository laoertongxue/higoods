# 印花加工单列表单行汇总、固定列与操作列调整审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-08-11 |
| 相关需求 / 任务 | 印花加工单六项汇总改为单行；仅固定“印花加工单”和“操作”；操作列收窄并按每行两个文字动作展示 |
| 记录模式 | 完整产品审查 |
| 涉及系统 | PFOS |
| 涉及页面路径 | `/fcs/craft/printing/work-orders` |
| 端类型 | 管理端 |
| 主要角色与任务 | 工艺管理、计划和跟单人员查看印花加工单全量信息，在横向浏览过程中持续识别当前加工单并执行详情、换料和打印动作 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：印花加工单列表的六项汇总从可换行卡片改为固定单行；列表固定列从“印花加工单、加工投入、加工产出、操作”收口为“印花加工单、操作”；操作列由 330px 收窄为 190px，描边操作按钮改为每行最多两个的文字操作。筛选、字段、状态、数量、图片、Mock 数据、业务动作、打印内容和路由均未改变。

当前审查基线：

- `AGENTS.md` 第 4 节：印尼工厂现场产品设计基线。
- `AGENTS.md` 第 5 节：UI、列表和真实图片专项门禁。
- `AGENTS.md` 第 7 节：分层验证和证据新鲜度。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 保持管理端标准列表；固定加工单识别列和操作列，中间业务信息在表格容器内横向浏览。 |
| 文案、状态、数量与单位 | 通过 | 原有字段、双状态、数量与 Yard 单位全部保留；六项汇总仅调整为同一行。 |
| 扫码、真实图片与对象识别 | 通过 | 商品、加工投入、加工产出和花型图片能力未变；当前验收确认缩略图成功加载并可打开不溢出屏幕的高清大图。 |
| 防错、危险确认与主管兜底 | 通过 | 本次未修改换料、交出、接收和打印动作的门禁或确认逻辑。 |
| 交接、跨端事实与异常追溯 | 通过 | 加工状态、交出状态、接收数量和差异字段完整保留，未改变事实来源。 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 管理端按 1366×768 验收，无页面级横向溢出；PDA、上传和弱网不在本次改动范围。 |
| 命名路由、交互、图片大图与打印 | 通过 | 命名路由通过；横向滚动后左右固定列不位移；图片大图与三类既有打印回归通过。 |

## 4. 问题标签

- `视觉干扰`
- `组件误用`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 第六项汇总单独换到第二行，破坏汇总区阅读节奏 | 视觉干扰 | 工艺管理、计划、跟单 | 页面级使用六等分单行汇总，1366×768 验证六项顶部坐标一致 | 否 |
| 加工投入、加工产出被固定，挤压可浏览区域 | 组件误用 | 工艺管理、计划、跟单 | 仅允许“印花加工单”作为左固定列，操作列继续右固定，其他列全部进入表格内部横向滚动区 | 否 |
| 操作列过宽且描边按钮密集 | 视觉干扰 | 工艺管理、计划、跟单 | 操作列收窄至 190px，改为双列文字动作，每行最多两个 | 否 |

## 6. 最终结论

结论：通过

说明：

- 三项调整均形成静态契约和 1366×768 浏览器证据；横向滚动前后固定列位置、可滚动列位移和操作行密度均由自动化验证。
- 没有删减印花加工单线上已有字段、业务功能、图片能力或打印入口。

## 7. 变更覆盖与验证

### 受管文件

- `src/pages/process-factory/printing/work-orders.ts`

### 页面路由

- `/fcs/craft/printing/work-orders`

### 验证命令

- `npm run check:printing-work-order-redesign`：通过。
- `PLAYWRIGHT_REUSE_EXISTING_SERVER=false CUTTING_E2E_PORT=43211 npx playwright test tests/printing-work-order-input-output.spec.ts --workers=1 --reporter=line`：通过，3/3。
- `npm run check:list-page-governance`：通过，扫描 355 个列表页面，历史基线 18 项；公共列表列拖拽与持久化回归通过。
- `npm run check:prototype-design-governance -- --all`：通过，1 个用户可见文件与 1 份关联治理记录闭环。
- `npm run build`：通过，2338 个模块完成构建。
- `codegraph init -i && codegraph sync && codegraph status`：通过，索引 1,484 个文件、45,511 个节点、177,436 条边，无待同步文件。
- `npm run workflow:verify -- --output /private/tmp/higoods-printing-list-layout-receipt/task-receipt.json --task-boundary "印花加工单列表：六项汇总单行、仅加工单与操作固定、操作列双列文字动作"`：通过，收据状态为 `verified`，无阻塞项。

浏览器证据位于 `/private/tmp/higoods-printing-acceptance/01-printing-list-1366.png`，内容为 1366×768 单行汇总、横向滚动后的固定列与双列文字操作。

### 真实图片验证

- 图片来源继续使用仓库内与商品、面料和花型对象对应的稳定资源；本次未替换图片或对象映射。
- 商品、加工投入、加工产出和花型仍在对应对象信息块内展示可点击缩略图；专项浏览器验收确认首张缩略图自然宽度大于 0，大图可见、保持比例且不超过 1366×768，并可用 `Esc` 关闭。
- 原有加载失败文字态未改变，不以无关占位图冒充对象图片。

### 例外

- 无。
