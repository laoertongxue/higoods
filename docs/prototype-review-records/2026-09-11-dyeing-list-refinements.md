# 染色列表仓库、数量、规格及备注审查

## 1. 基本信息

- 日期：2026-09-11；管理端；需求 REFINE-001～005，来源用户本轮四项反馈。
- 版本：main / f6f3f945a46711265ad6f0eaf7086e9533134fcb 当前累计染色工作区。
- 服务：/Users/laoer/Documents/higoods，PID 59149，0.0.0.0:5188；当前局域网IP为192.168.0.17。
- 范围：仓库归属展示、数量四段、逐SKU规格、备注轻交互和本地保存。

## 2. 影响判定

- 用户可见影响：有
- 判定依据：列表资料、数量布局与备注入口改变；详情和导出同步删除虚构仓库归属。
- 采用 AGENTS.md 第4、5、7节和 HiGood 工厂设计技能，保留当前标准管理列表与真实图片。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 管理端标准列表，原工厂Tab、筛选与分页保留 |
| 文案、状态、数量与单位 | 通过 | 三条横线形成四段，单位按原单据，不混加米与Yard；仓库无所属工厂 |
| 扫码、真实图片与对象识别 | 通过 | 每个投入及产出SKU同块展示名称/图片/编码/规格；加宽SKU160 cm独立 |
| 防错、危险确认与主管兜底 | 通过 | 备注空白/超长/版本冲突/存储失败阻断；失败不重建输入，取消不保存 |
| 交接、跨端事实与异常追溯 | 通过 | 上游发出和本单实收分别读取；备注保存仅备注变更，并写现有日志 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 1366×768、1280×720无主体横向溢出；PDA、上传、真实网络保存不适用 |
| 命名路由、交互、图片大图与打印 | 通过 | 备注新增/查看/编辑/取消/刷新通过，物料大图正常且Esc关闭；打印入口未改 |

## 4. 问题标签

- 用词假设：用户“付款”按前文“幅宽”理解，已提问并告知；尚无相反回复。本轮不增加付款字段。

## 5. 主要问题与处理

- 删除仓库类型中的factoryName及其展示，保留真实仓名中的括号文本；工厂类型分支不变。
- 数量按计划、上游交接、加工、下游交接分区；完成和交出以卷数/长度并列，保留备料、损耗及待接收信息。
- 补齐每个SKU属性，花边明确为辅料，多SKU规格各自读取。
- DWO-002、DWO-003作为无备注演示场景；本轮浏览器验收后DWO-002保留明确标为本地演示的备注，DWO-003仍可新增。
- 备注复用已有字段及日志，当前浏览器按加工单保存备注。只更新操作按钮文本和弹窗；不重绘列表或应用根节点。

## 6. 最终结论

结论：通过。本轮功能和页面已在同工作树验收；技术收据执行结果以最终JSON为准。上述幅宽用词为已告知假设，不表述为用户确认。

## 7. 变更覆盖与验证

### 受管文件

- `src/data/fcs/dye-work-order-demo-details.ts`
- `src/data/fcs/dye-work-order-online-view.ts`
- `src/data/fcs/dye-work-order-online-domain.ts`
- `src/data/fcs/dyeing-task-domain.ts`
- `src/pages/process-factory/dyeing/work-orders.ts`
- `src/pages/process-factory/dyeing/work-order-overlays.ts`

### 页面路由

- `/fcs/craft/dyeing/work-orders`

### 验证命令

- `node --import tsx scripts/check-dyeing-list-sections.ts`：通过；逐SKU、分类、四段、备注仅单字段改变、版本冲突、存储失败、重新初始化读取保存内容。
- `node --import tsx scripts/check-dyeing-parity-20260910.ts`：通过；16条原有染色资料、图片、条码和流程卡契约。
- `npm run typecheck:entrypoints`：通过。
- `git diff --check`：通过。
- `curl http://192.168.0.17:5188/fcs/craft/dyeing/work-orders`：通过，200；进程cwd确认同工作树。
- `npm run workflow:verify`：未运行（记录准备时）；最终结果见 [收据](/tmp/higoods-dyeing-0911/task-receipt.json)。

### 浏览器证据

- 两页10+6条：仓库无所属工厂；加工厂仍有名称、编号、类型；所有行四段数量、所有投入/产出属性齐全。
- 1366×768横向滚动至数量列：前三段底边均1px浅灰色，第四段0；实际截图在本任务工具输出。
- DWO-002空白保存提示“请填写备注内容”；新增后按钮变查看备注；编辑取消仍显示旧内容；再次编辑保存，刷新后仍为新内容，换行保留。
- DWO-003仍显示“备注”；输入保存与取消只更新本地相关弹窗及行按钮。
- 1280×720主体宽1280；1366主体宽1366，表格内滚动，操作列固定。
- 页面错误日志为空，结束时恢复默认视口、全部工厂和第一页。

### 真实图片验证

- 图片沿用前续已登记资源，未新增或替换图片；投入/产出同SKU属性区展示缩略图。
- 两页图像正常加载；蓝白花棉布高清图在1280×720下约789×592，不溢出；Esc成功关闭。

### 例外

- 备注为浏览器本地原型数据，不代表后端持久化；既有日志继续采用原内存演示机制。
- 本轮不涉及PDA、扫描、上传或实物打印；此前打印人工验收例外不扩大为本轮已完成事项。
