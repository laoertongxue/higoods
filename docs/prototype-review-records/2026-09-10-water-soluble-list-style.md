# 水溶管理列表与染色列表统一

## 1. 基本信息
- 日期：2026-09-10
- 分支：codex/process-route-full-stage-incremental
- HEAD：8d8f8ffd43d1000d9c852a1cffbb4c73e16bee12，加本次单文件差异。
- 工作树：.worktrees/process-route-full-stage-incremental；服务5188已核对同一工作树。
- 用户要求：水溶加工单筛选、统计卡片和数据列表与当前染色加工单一致。

## 2. 影响判定
- 用户可见影响：有
- 判定依据：管理端由执行卡片改为标准列表，复用染色列表的列表容器、统计、表格、筛选折叠与分页组件；保留工厂执行权限。

## 3. 自查结论
|检查项|结论|说明|
|---|---|---|
|角色与页面模式|通过|管理端标准列表，可信工厂账号原执行卡片和动作保留|
|筛选与按钮位置|通过|综合查询、三类状态、工厂；更多条件含接收方、下单日期；按钮在全部字段后独占一行|
|统计口径|通过|六项分别为单数、计划投入、已接收、完成、已交出、下游待接收；按单位分别合计，无虚构投入用量|
|表格|通过|加工单/商品、加工投入/上游、加工要求、三类状态、产出/下游、工厂/时间、操作；分页、排序、列设置与路由偏好|
|图片识别|有条件通过|原商品与投入物料对应图、加载/失败与大图入口保留；产出图缺口明确提示|
|操作权限|通过|新增管理只读详情，所有变更状态的动作仍需原工厂与角色检查|
|更新方式|通过|筛选输入不重绘，查询只更新列表区域；详情沿用原抽屉|
|边界|通过|无匹配数据使用标准空态；原水溶业务检查通过|

### 受管文件
- `src/pages/process-factory/dyeing/water-soluble-orders.ts`

### 页面路由
- `/fcs/craft/dyeing/water-soluble-orders`
- 对照 `/fcs/craft/dyeing/work-orders`

### 验证命令
- `node --import tsx scripts/check-water-soluble-pages.ts`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过。
- `npm run check:list-page-governance:static`：通过。
- `Playwright output/playwright/water-list/browser.js`：通过。

### 例外
- 当前任务只统一管理列表样式，不补造产出实物图片；该素材缺口仍保留。
- 工作区有大量原有差异，不运行吸收全工作区的workflow收据；用单文件任务差异和当前页面证据记录。

## 6. 最终结论
结论：有条件通过。样式与管理列表交互统一，缺失产出素材未冒充补齐。依据AGENTS.md第4、5、7节。产品确认人：待用户确认当前版本；未提交推送。
