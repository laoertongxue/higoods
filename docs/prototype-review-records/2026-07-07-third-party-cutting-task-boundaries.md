# HiGood 原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-07-07 |
| 相关需求 / 任务 | 三方裁片与连续任务边界 Task 1 / Task 2 / Task 3 |
| 涉及系统 | FCS |
| 涉及页面路径 | 无页面改动，涉及生产单任务边界 Mock 数据、纯函数规则、裁片单生成数据与我方辅助/特种工艺加工单数据源 |
| 端类型 | 管理端 |
| 主要角色 | 生产计划、跟单、任务分配人员 |
| 主要任务 | 判断整单、含裁片连续任务、独立裁片任务是否生成裁片单和我方加工单，并在裁片单与特殊工艺加工单数据中保持来源、回流与跳过策略一致 |

## 2. 参考规范

- `docs/higood-indonesia-factory-product-design-guidelines.md`
- `docs/higood-indonesia-factory-prototype-review-checklist.md`

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色匹配 | 通过 | 规则服务 FCS 管理端任务分配边界，不暴露给一线员工操作。 |
| 任务清晰度 | 通过 | 边界输出直接说明是否生成裁片单、是否生成我方加工单。 |
| 信息架构与导航 | 通过 | 无导航改动。 |
| 页面模式 | 通过 | 无页面模式改动。 |
| 信息负荷 | 通过 | 无页面字段新增。 |
| 文案 | 通过 | 输出标签使用中文业务语义。 |
| 数量与状态 | 通过 | 不新增数量输入或英文状态展示。 |
| 扫码与识别 | 通过 | 不涉及扫码。 |
| 防错 | 通过 | 检查脚本覆盖整单、含裁片连续任务、不含裁片连续任务、独立裁片任务、独立非裁片任务、旧摘要裁片识别，以及裁片单来源、回流方式、我方加工单策略和特殊工艺加工单跳过 warning 中文标签。 |
| UI 样式 | 通过 | 无 UI 改动。 |
| 组件交互 | 通过 | 无组件交互改动。 |
| 协作关系 | 通过 | 区分三方连续承接与我方内部加工单生成边界。 |
| 异常与追溯 | 通过 | 修复生产单摘要与任务事实源不一致问题。 |
| 现场设备可用性 | 通过 | 无现场设备页面改动。 |

## 4. 问题标签

无。

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| `PO-202603-0007` 被误用为含裁片连续任务样本，破坏现有多任务事实摘要 | 协作断裂、追溯不足 | 生产计划、跟单 | 恢复其多单工序摘要，改用待确认样本承载连续任务预览 | 否 |
| 裁片单生成未按任务边界过滤，且缺少来源 / 回流 / 我方加工单策略标记 | 协作断裂、追溯不足 | 生产计划、跟单、裁床管理 | 消费统一任务边界规则，只为独立裁片任务和含裁片连续任务生成裁片单，并写入中文策略标签 | 否 |
| 验收覆盖未包含不含裁片连续任务、独立非裁片任务，且中文策略标签只做非空检查 | 追溯不足 | 生产计划、跟单、裁床管理 | 在检查脚本中使用最小合成输入覆盖不生成分支，并精确断言来源、回流方式、我方加工单策略中文标签 | 否 |
| 裁片单候选先截断再按任务边界过滤，导致排在截断后的真实裁片订单漏单 | 协作断裂、追溯不足 | 生产计划、跟单、裁床管理 | 候选列表先按正式技术包和任务边界过滤，再生成全部应生成裁片单；检查脚本断言真实应生成订单不得漏单 | 否 |
| 修复漏单后移除了 PDA 多裁片单、菲票链路依赖的稳定裁片单编号 | 协作断裂、追溯不足 | 裁床管理、PDA 操作员、仓库 | 在合法独立裁片任务种子上补回 `CUT-260306-101-01/02`、`CUT-260307-102-01/02` 等稳定场景裁片单，保留来源、回流方式、我方加工单策略标签，并扩展 PDA / 主链路检查 | 否 |
| 我方辅助/特种工艺加工单仍会从整单、连续任务或独立非裁片任务补充生成 | 协作断裂、追溯不足 | 生产计划、跟单、特殊工艺管理 | 特殊工艺生成器先消费统一任务边界；整单、含裁片连续任务、不含裁片连续任务直接返回空任务、空明细、`已跳过` 批次和“不生成我方辅助/特种工艺加工单” warning；数据源兜底 demo 上下文也只取允许回我方链路的独立裁片任务 | 否 |
| 旧检查脚本用数量和过期源码文案判断特殊工艺生成基线 | 追溯不足 | 开发验收 | 调整为合并感知的 runtime 任务覆盖断言，并按当前任务清单页面结构校验稳定入口，避免要求本任务修改页面 | 否 |

## 6. 最终结论

结论：通过

说明：

- 本次只修改数据规则、裁片单生成数据、检查脚本和验收覆盖，不新增页面、路由或交互。
- 规则保持“任务类型决定生成边界”，不把三方连续任务内部工艺拆成我方加工单。
- 我方辅助/特种工艺加工单列表只保留我方内部加工对象；整单任务、含裁片连续任务、不含裁片连续任务均以中文跳过批次说明不生成。
- 本次不新增真实生产单 mock；不含裁片连续任务和独立非裁片任务通过检查脚本里的最小合成输入覆盖，避免破坏既有生产单事实源。
- 裁片单维度演示场景改用合法裁片任务种子，不再依赖已被边界过滤排除的非裁片生产单。
- PDA 多裁片单和菲票主链路继续使用稳定裁片单编号，但这些编号只绑定到真实存在且任务边界合法的裁片单，不重新放开整单或非裁片边界。
