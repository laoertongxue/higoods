# 后道辅料接收测试数据扩充原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-09-12 |
| 相关需求 / 任务 | 在现有 PDA“辅料接收”基础上补充多张可执行的后道辅料调拨测试单 |
| 记录模式 | 完整产品审查 |
| 涉及系统 | PFOS / FCS PDA |
| 涉及页面路径 | `/fcs/pda/handover?tab=pickup`、`/fcs/pda/handover?tab=pickup&materialOrderNo=DB-PF-202608-002`、`/fcs/craft/post-finishing/wait-process-warehouse?tab=materials` |
| 端类型 | 员工执行端 PDA、管理端 Web |
| 主要角色与任务 | 后道仓管扫描完整调拨单号，核对物料实配明细并整单一次性入库 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：页面结构、入口、文案和交互规则不变；默认 Mock 从一张扩充为五张不同生产单的待入库调拨单，PDA 待接收数量、卡片、详情明细和入库后 Web 辅料库存随所选单据变化。已有浏览器只补缺失的 `002` 至 `005`，不覆盖 `001` 的已入库状态、库存和操作历史。
- 当前审查基线：`AGENTS.md` 第 4 节印尼工厂现场产品设计基线、第 5 节 UI 与交互专项规则、第 7 节验证原则。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 仍由后道工厂操作工/仓管从交接待接收进入，主动作仍是精确扫描后整单入库。 |
| 文案、状态、数量与单位 | 通过 | 五张单据均为“待入库”，明细使用调出方实配数量和 `PCS`，未增加手填数量。 |
| 扫码、真实图片与对象识别 | 通过 | 五张完整单号可区分；物料名称、编码、规格、实配数量和正式物料图在同一信息块。 |
| 防错、危险确认与主管兜底 | 通过 | 保留完整单号精确匹配、一次性入库确认、重复入库幂等和不使用成衣差异授权规则。 |
| 交接、跨端事实与异常追溯 | 通过 | PDA 入库与 Web 待加工仓辅料库存继续读取同一调拨事实；各单保留三段状态历史。 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 390×844、480×800 均无横向溢出；本次无上传和弱网能力变化。 |
| 命名路由、交互、图片大图与打印 | 通过 | 列表、`002` 扫描详情、物料大图、Esc 关闭均实测通过；本次不涉及打印。 |

## 4. 问题标签

- 无

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 已有浏览器中的 `001` 可能已入库，单纯替换初始化数据不会出现新测试单 | 追溯不足 | 后道仓管、产品验收人员 | 初始化改为按完整单号只补缺失单据，保留已有状态、库存和历史，并增加专项迁移用例 | 否 |

## 6. 最终结论

结论：通过

说明：

- 这是现有 Mock 数据和幂等补齐的局部迭代，没有重做 PDA 页面、路由或入库流程。
- 五张测试单分别属于不同生产单，符合“一张生产单最多一张主调拨单”；新增单据均可直接用于整单入库测试。

## 7. 变更覆盖与验证

### 受管文件

- `src/data/fcs/post-finishing-full-flow.ts`

### 页面路由

- `/fcs/pda/handover?tab=pickup`
- `/fcs/pda/handover?tab=pickup&materialOrderNo=DB-PF-202608-002`
- `/fcs/craft/post-finishing/wait-process-warehouse?tab=materials`

### 验证命令

- `npm run check:post-finishing-full-flow`：通过
- `npx playwright test tests/post-finishing-pda-role-boundary.spec.ts --workers=1 --reporter=line`：通过，8/8
- `npm run build`：通过
- `npm run check:post-finishing-full-flow-surface`：通过
- `npm run check:prototype-design-governance -- --all`：通过，1 个用户可见变更文件与 1 份治理记录对应
- `npm run workflow:verify -- --output /private/tmp/post-material-transfer-test-data/task-receipt.json --task-boundary "后道辅料接收扩充五张测试调拨单，并保留已有入库历史"`：通过，状态 `verified`、无阻塞项

### 真实图片验证

- 款式图使用项目现有正式样衣图：`/shirt-sample.jpg`、`/cardigan-sample.jpg`、`/dress-sample-1.jpg`、`/jacket-sample.jpg`。
- 物料图使用项目已有、来源登记于 `public/materials/sources.json` 的 `/materials/accessory-label.jpg` 与 `/materials/accessory-button.jpg`。
- PDA 详情中图片与物料名称、编码、规格同块展示；`002` 第一条物料已实测打开大图并由 `Esc` 关闭。
- 截图：`output/playwright/post-material-transfer-test-data/pda-pickup-390x844.png`、`pda-pickup-480x800.png`、`pda-material-002-detail-image-390x844.png`。

### 例外

- 无
