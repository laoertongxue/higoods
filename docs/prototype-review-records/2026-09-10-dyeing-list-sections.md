# 染色加工单列表分区与字段补齐

## 1. 基本信息

- 日期：2026-09-10；完整原型审查；PFOS 管理端标准列表。
- 来源：用户本轮六项列表要求；需求编号 LIST-001～009。设计/实施包及原子追踪见 `docs/dyeing-online-parity-20260910.md` §7 和对应矩阵。
- 版本：main / f6f3f945a46711265ad6f0eaf7086e9533134fcb 的本次工作区差异。
- 运行：Vite PID 98873，cwd `/Users/laoer/Documents/higoods`，监听 0.0.0.0:5188；局域网命名页面已返回200。

## 2. 影响判定

- 用户可见影响：有
- 判定依据：重新组织单据/商品/标签、投入/上游、产出/下游信息；新增时间、数量列；补充命名演示资料。未改执行、接收、交出或仓库库存事实。
- 基线：采用 AGENTS.md 第4、5、7节及 HiGOOD 工厂设计技能；保持既有标准列表组件和局部更新事件。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务、页面模式 | 通过 | 管理端标准宽表，分段灰线，商品/投入/产出图左文右 |
| 文案、状态、数量与单位 | 通过 | 16行字段完整；未发生时间和备货生产单明确说明；三阶段独立；重量注明理论换算 |
| 真实图片、对象识别 | 通过 | 两页共49张缩略图加载成功；多SKU分别标识，高清大图保持比例 |
| 防错、危险确认、主管兜底 | 不适用 | 本轮只变更列表展示；保留原选择/行操作处理器，不新增危险动作 |
| 交接责任、跨端事实、追溯 | 通过 | 上游发出数量来自对应仓库单据；补齐的计划发出量0，不产生本单接收 |
| 低分辨率、PDA、弱网、上传 | 通过 | 1366×768与1280×720无主体横向溢出；PDA/弱网/上传不适用本轮 |
| 路由、交互、图片大图、打印 | 通过 | 命名列表、切厂、搜索、两页、列设置持久化、大图Esc实测；打印版式不在本轮修改范围 |

## 4. 问题标签

- 口径待确认：加工要求“类型”线上为空，已异步询问用户；本地暂沿用“头缸/复染”，尚未取得业务分类确认。
- 实物打印与图片失败态：未在本轮重验，沿用之前矩阵未关闭项；不以本轮列表验证替代。

## 5. 主要问题与处理

- 单据/商品/标签分三段，投入/上游及产出/下游分两段；长SKU换行不截断。
- DWO-002增加同种棉氨针织布加宽SKU并设置为超期补料，支撑多SKU与多标签演示；补齐合并染色演示单售卖类型。
- 未发料预设单使用具名 DB-DEMO 调拨计划展示，不写入仓库业务记录；已发料来源读取原仓库单据状态及发出量，不从本单接收量反推。
- 卷数为命名演示单汇总资料，与现有备料/完成/交出数量的有无匹配；它不代表生成了对应卷条码，也不增加执行或交接数量。动态单按现有卷记录读取。
- 数量列保留原单位，备料重量用现有理论换算；DWO-006损耗尚未确认，显示“尚未完工核算”。未完成/交出的时间不伪造时间戳。
- 新列首次展示时默认可见；已有隐藏/顺序/冻结设置保留，之后手动隐藏时间列并刷新仍然有效。验证后恢复时间列显示。
- 本轮增量审查以 `/tmp/higoods-dyeing-list-20260910/` 下三个 before 文件为基线；其余累计差异均属于此前同一染色对齐任务。没有发布或线上操作。

## 6. 最终结论

结论：有条件通过。列表分区、字段、图片和命名页面交互已本地验证。类型业务分类仍待用户确认；技术检查最终结果以本轮收据为准，收据成功产生前不视为技术闭环。前次打印系统面板与纸张分页待确认项仍保留，不声明整个累计范围已产品接受。

## 7. 变更覆盖与验证

### 受管文件

- `src/pages/process-factory/dyeing/work-orders.ts`
- `src/data/fcs/dye-work-order-online-view.ts`
- `src/data/fcs/dye-work-order-demo-details.ts`

### 页面路由

- `/fcs/craft/dyeing/work-orders`
- `http://192.168.0.21:5188/fcs/craft/dyeing/work-orders`

### 验证命令

- `node --import tsx scripts/check-dyeing-list-sections.ts`：通过，16行资料、图片资源、上游数量、多SKU搜索、多标签、卷数、进度及两列契约。
- `node --import tsx scripts/check-dyeing-parity-20260910.ts`：通过，16行资料、普通/补料/批量卡映射、条码独立于加工交接的相邻回归。
- `npm run typecheck:entrypoints`：通过；最终全部类型与构建结果一并记录于收据。
- `npm run build`：通过，包含工程类型检查、现有单元测试及 Vite 构建。
- `npm run workflow:verify`：失败（前两次因审查记录缺少显式命令结果、结论格式，被文档治理门禁拦截）；补正后最终重跑结果见 `/tmp/higoods-dyeing-list-20260910/task-receipt.json` 与同目录 `verify.log`，包含构建、治理及 CodeGraph 同步状态，绑定本任务累计差异。

### 浏览器证据

- 同服务IAB桌面页：工厂GTG切换返回2行；DWO-002投入2个SKU，同时出现超期/补料标签；第二SKU搜索精确返回1行。
- 全部工厂第一页10行31图、第二页6行18图；图片complete/naturalWidth均通过，无空字段span。
- 1366×768：documentWidth1366，表格容器1044，表格1910；滚到右侧看到时间/数量，首列和操作列保留。1280×720：documentWidth1280。截图保存在本任务工具输出。
- 多SKU第二图打开高清预览，原图1200×1200，预览640×640，Esc后弹窗数量0。
- 时间列隐藏后刷新仍无时间单元格，再通过列设置恢复10个时间单元格；最终恢复原浏览器尺寸、默认全部工厂第一页。
- 输入、切厂、列设置沿用局部更新，没有修改整页重绘或图标扫描入口；本轮未做精确耗时基准。

### 图片来源与对应关系

- 商品使用已有正式商品图片映射；材料来自 `public/materials/process-orders/`、`public/materials/fei-ticket/` 的既有对应图片。
- 新增 `FAB-J180-WHITE-160` 是同种本白棉氨布加宽演示SKU，复用 `white-black-cotton-jersey.jpg`，没有用无关占位图。
- 加工投入可多图，产出每行唯一图；均与名称/SKU处在同一信息块。图片失败提示沿用未修改的现有图片组件，本轮验证加载成功和大图，不声称失败态已实测。

### 例外

- 类型分类暂用头缸/复染，待产品确认；本轮不要求真实后端、库存执行、PDA或实物打印。
