# 生产准备时效多选筛选与标准列表原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-07-17 |
| 相关需求 / 任务 | 生产准备时效准备台账、月度统计和明细统计接入标准列表页治理 |
| 涉及系统 | FCS |
| 涉及页面路径 | `/fcs/production/preparation-timing`、`/fcs/production/preparation-timing-statistics` |
| 端类型 | Web 管理后台 |
| 页面模式 | 标准列表页 + 下拉复选筛选 + 抽屉/弹窗 |
| 主要角色 | 跟单、版师团队、毛织团队、车板团队、花型团队、染色团队、采购团队 |
| 主要任务 | 筛选生产准备记录，查看准备进度和产出；按月查看准备项完成统计并追溯完成明细 |

## 2. 参考规范

- `docs/higood-indonesia-factory-product-design-guidelines.md`
- `docs/higood-indonesia-factory-prototype-review-checklist.md`
- `AGENTS.md` 中标准列表页、交互性能和中文化要求

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色匹配 | 通过 | 页面是 FCS 管理端台账，允许较高信息密度和追溯入口。 |
| 任务清晰度 | 通过 | 标题、筛选、统计、准备台账和行操作按管理任务顺序排列。 |
| 信息架构与导航 | 通过 | 保留查看详情、确认工作项、独立准备项操作和非系统内物料维护入口。 |
| 页面模式 | 通过 | 使用标准列表页、标准表格、标准分页和列设置。 |
| 信息负荷 | 通过 | 可隐藏非必需列；业务防错列和操作列始终可见。 |
| 文案 | 通过 | 页面状态和操作均使用中文业务文案。 |
| 数量与状态 | 通过 | 数量带单位，完成情况和产出状态沿用业务口径。 |
| 扫码与识别 | 有条件通过 | 本页为管理端，不承担现场扫码任务。 |
| 防错 | 通过 | 商品、人员、准备时间、整体状态和操作列不可隐藏。 |
| UI 样式 | 通过 | 标题不放入卡片；宽表只在表格容器内横向滚动。 |
| 组件交互 | 通过 | 排序、分页、列显示、冻结、顺序和恢复默认使用标准组件；轻交互局部更新。 |
| 协作关系 | 通过 | 保留跟单确认和各责任团队独立准备项入口。 |
| 异常与追溯 | 通过 | 详情、完成凭证、下载记录和产出信息入口未删减。 |
| 现场设备可用性 | 通过 | 管理端以 1366×768 为标准验收视口，页面主体不产生横向溢出。 |

## 4. 问题标签

- `字段过载`
- `点错风险`
- `追溯不足`
- `组件误用`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 原台账为手写宽表，缺少列治理 | 字段过载、组件误用 | 跟单、管理人员 | 接入标准列表表格、分页和列设置；允许隐藏非必需列。 | 否 |
| 宽表操作列可能随横向滚动离开视口，或在无按钮时被空数组断言误判 | 点错风险 | 全部管理端角色 | 操作列声明为固定右侧必需列；双视口分别核对未确认行“确认工作项”和已确认行“查看详情、上传毛织基码纸样”的数量、文案、可见性、非零尺寸及矩形不重叠。 | 否 |
| 标准化可能删减原业务入口 | 追溯不足 | 跟单、责任团队 | 保留查看详情、确认工作项、独立准备项操作、产出和物料维护入口。 | 否 |
| 列设置可能触发整页重绘 | 组件误用 | 全部管理端角色 | 表格、分页和列设置使用局部 DOM 区域更新；异步事件链完成后实测响应小于 200ms。 | 否 |
| 页码和排序跨刷新或重新进入保留 | 点错风险 | 跟单、管理人员 | 页码和排序仅保留在当前局部交互；刷新、筛选或 SPA 重新进入时统一回到第 1 页且未排序。 | 否 |
| 同路由业务弹窗导致台账上下文丢失 | 点错风险 | 跟单、责任团队 | 路由进入生命周期与页面渲染分离；查看详情、准备项操作和上传后的重渲染保留当前页与排序。 | 否 |
| 列设置局部渲染后图标不可见 | 组件误用 | 全部管理端角色 | 每次局部更新后只对新插入区域执行图标 hydration，不扫描整页。 | 否 |
| 列设置抽屉在 SPA 重进时自动重开 | 点错风险 | 跟单、管理人员 | 路由重新进入时统一清理页码、排序、抽屉和拖拽瞬时状态。 | 否 |
| 月度统计和明细统计为手写表格，列偏好互相影响 | 字段过载、组件误用 | 跟单、管理人员 | 两个 Tab 分别使用标准表格、标准分页和独立存储键；各自持久化列显示、顺序、冻结和每页条数。 | 否 |
| 统计分页和排序写入 URL 后会在刷新时错误恢复 | 点错风险 | 跟单、管理人员 | 页码和排序只保留在当前 Tab 的内存状态；刷新、Tab 切换和 SPA 重进均重置。 | 否 |
| 月份进入明细时可能丢失多选筛选或传播废弃参数 | 追溯不足 | 管理人员 | 明细链接保留合法重复筛选值，只覆盖 `tab=detail` 和 `detailPage=1`，不传播废弃参数及准备项进度。 | 否 |
| 统计宽表可能造成页面级横向溢出 | 字段过载 | 管理人员 | 页面主体保持无横向溢出，月度和明细宽表只在标准表格容器内滚动。 | 否 |
| 统计处理器响应无关输入时重复聚合 | 组件误用 | 跟单、管理人员 | 事件处理先按路由和统计列表前缀快速返回；性能验收连续触发多个关键词输入事件，并等待各自异步分发、宏任务和下一帧，确认最终输入值保留、列表不刷新且每次响应小于 200ms。 | 否 |
| 台账处理器吞掉统计列拖拽结束事件 | 组件误用 | 管理人员 | 台账和统计处理器分别校验所属路由及列前缀后再清理拖拽状态。 | 否 |
| CSV 仅验证下载入口，未验证重复多选真实生效、完整筛选结果和标准转义 | 追溯不足 | 管理人员 | 浏览器真实选择两个准备项和两个责任团队，将每页条数设为 5；月度导出只允许梭织基码 2 项、辅料下单 4 项为非零，明细页面显示 5 行但导出必须包含准确的 6 条记录；专项纯函数另覆盖逗号、引号和换行转义。 | 否 |
| 辅料下单被普通上传凭证口径误判，或编辑多采购单时覆盖逐单下单时间 | 追溯不足、状态与事实不一致 | 跟单、采购团队 | 完成证据统一使用数据层判定：辅料无需上传，必须有非空采购单号、每张单有下单时间，实际完成时间等于最晚下单时间；弹窗逐行回填和保存单号、下单时间，仅修改目标行。 | 否 |
| 状态标记已完成但缺少有效凭证时仍被 KPI 计入完成 | 算不准、追溯不足 | 跟单、管理人员 | KPI、筛选、串行依赖、统计和产出统一调用有效完成凭证口径；普通项必须有上传人、上传时间和文件名，辅料下单按逐单下单时间判定。 | 否 |
| 重新勾选原“无需”准备项后仍不可开始，或运行态动作后记录状态仍停留在旧值 | 状态抽象、协作断裂 | 跟单、责任团队 | 重新勾选且无完成证据的准备项恢复为“待开始”；确认、上传、染色要求和辅料登记后按有效凭证派生记录状态，未触达记录保留原具体卡点。 | 否 |
| 基码纸样下载改变记录状态，或错误的记录号与准备项编号组合串写其他记录 | 追溯不足、点错风险 | 版师团队、跟单 | 下载只新增下载审计记录，不触发记录状态派生；上传和下载必须同时匹配记录号与准备项编号。 | 否 |
| 已关闭记录被残留运行态重新生成正式产出 | 状态抽象、协作断裂 | 跟单、管理人员 | 已关闭记录保留原产出就绪状态、发布时间、生产需求单号、生产单号和产出列表，不允许运行态补生成正式产出。 | 否 |
| 应用响应探针在最终渲染前结束，或关键动作未纳入 200ms 验收 | 组件误用 | 全部管理端角色 | 在浏览器真实 click/change 事件捕获阶段记录起点，等待最终 DOM/URL 条件后结束；补齐详情、确认、上传、下载、染色要求、重置、分页、每页条数和列显示动作。 | 否 |
| 生产准备专项 E2E 未进入 CI，CSV 可被表格公式前缀解释 | 追溯不足、点错风险 | 管理人员 | CI 使用随机端口单独执行生产准备专项 spec；CSV 对 `= + - @` 和制表符前缀增加单引号中和，同时保留标准逗号、引号和换行转义。 | 否 |

## 6. 对抗式反例验收

| 方向 | 构造反例 | 结果与防错 |
| --- | --- | --- |
| 数据完整性 | 未确认工作项；普通项状态已完成但无完整上传；辅料项无采购单号、任一采购单缺下单时间或完成时间不等于最晚下单时间；依赖项无有效凭证；Mock 状态与确认逻辑冲突 | 均被专项检查拦截。完成项统一通过数据层有效凭证函数；普通项要求完整上传，辅料项不要求上传但要求每张采购单号及逐单下单时间完整，实际完成时间取最大下单时间；每条 Mock 均校验确认、依赖、状态和产出一致性。 |
| 页面可见性 | 准备项与责任团队选择不兼容；筛选无结果；旧筛选字段残留；标题被包入列表卡片 | 已选值即使不兼容仍保持勾选且可见，并展示标准空态；筛选区不出现责任人、是否超时、买手；页面文案为中文；标题位于标准列表页头部而非列表卡片。 |
| 交互可达性 | 台账五类多选缺项；统计四类多选被单值关键词混淆；联动错误取交集；分页、排序、列设置或业务弹窗丢失上下文 | 台账跟单/记录状态/准备项/准备项进度/责任团队均为多选；统计跟单/记录状态/准备项/责任团队为多选，关键词按批准口径保留单值输入且无准备项进度；双向联动按并集；三个列表分页、三态排序、列显示/顺序/冻结/恢复可达；台账操作列固定右侧；业务弹窗保持当前页和排序。 |
| 业务闭环 | 月份进入明细丢重复参数；筛选后 CSV 只导出当前页或转义损坏；准备项/团队/进度跨项命中；无上传已完成进入统计 | 月份和 Tab 链接只保留合法白名单并保留重复多选参数；CSV 在每页 5 条时仍导出准确的 6 条筛选明细，专项覆盖逗号、引号、换行；`PREP-202603-002` 的版衣制作不满足开始条件、但其他准备项已完成，筛选“版衣制作 + 车板团队 + 已完成”时明确排除该记录，证明三类条件在同一准备项匹配；无有效上传的普通完成项不进入月度与明细统计。 |
| 运行态边界 | 无凭证已完成进入 KPI；重新勾选仍为无需；下载重算记录状态；错误记录号与准备项编号串写；已关闭记录生成正式产出；localStorage 暂时不可写 | KPI 只认有效完成凭证；重新勾选恢复待开始；纯下载只记录审计事实；上传下载联合校验记录号和准备项编号；已关闭产出字段保持不变；存储恢复后自动补写会话降级状态并清除缓存。 |

双视口验收：1366×768、1280×720 下 `documentElement` 与 `body` 均无页面级横向溢出，宽表只在 `data-standard-list-scroll` 内滚动，台账固定操作列与按钮保持可见且不重叠。

性能验收：多选联动、台账/月度分页、列设置、台账无关点击和连续快速关键词输入事件均等待最终 DOM 条件后计时；筛选提交、上一页/下一页、每页条数、台账排序、统计 Tab、月份钻取、详情开关、确认工作项、上传开关与真实文件提交、下载记录、染色要求、辅料登记、重置筛选和列显示均从浏览器真实 click/change 事件捕获阶段开始，到最终 DOM/URL 条件满足后结束，应用内响应均小于 200ms。关键词仍在点击“筛选”后才应用到统计结果；文件选择器和下载器等浏览器原生系统界面不计入应用响应。

稳定性复验：交互与交付门禁补强提交完成后，使用随机端口串行执行生产准备专项，结果为 `34 passed (5.5m)`；覆盖本记录所列业务闭环、列表交互、双视口、CSV 安全和应用内性能动作。最终整体验收结果以本任务结束前重新执行的命令为准。

## 7. 最终结论

结论：通过

说明：准备台账、月度统计和明细统计均已接入标准列表治理。统计口径和 CSV 导出仍基于当前筛选后的有效完成凭证数据；统计事件已隔离到所属列表，关键词输入与无关点击响应小于 200ms。没有业务例外，也未修改治理基线。
