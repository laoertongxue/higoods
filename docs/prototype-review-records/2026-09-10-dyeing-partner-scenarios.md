# 染色上下游参与方与演示场景纠正

## 1. 基本信息

- 日期：2026-09-10；PFOS管理端标准列表及详情；PARTY-001～005。
- 依据：用户本轮4张截图与明确说明“不存在雅加达中心仓”；名称、编码、工厂类型采用截图，逐单上下游归属为本地演示分配。
- 设计、实施包、验收：`docs/dyeing-online-parity-20260910.md` §8；追踪见同日矩阵。
- 版本：main / f6f3f945a46711265ad6f0eaf7086e9533134fcb 当前工作区。Vite PID 98873 的cwd为 `/Users/laoer/Documents/higoods`，监听0.0.0.0:5188，复用本地演示服务。

## 2. 影响判定

- 用户可见影响：有
- 判定依据：纠正染色仓库展示资料，补充工厂来源、四类下游及属性/类型；详情与导出跟随同一具名资料；染色调拨种子采用对应仓库名和编号。
- 采用 AGENTS.md 第4、5、7节基线。限定染色模块与染色专用调拨构造，不全局修改其他模块历史仓库资料，不修改接收、加工、交出数量与处理器。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务、页面模式 | 通过 | 标准管理列表，保留上一轮分区和灰线；主体不溢出 |
| 文案、状态、数量、单位 | 通过 | 仓库有归属/仓名/编码/属性，工厂有名称/编号/类型；加工单与调拨单分别显示状态；4个新增工厂来源尚未交出，数量0 |
| 真实图片与对象识别 | 通过 | 沿用既有对应图片，两页无加载失败；辅料中央仓示例为花边，未把面料作为辅料演示 |
| 防错、危险确认、主管兜底 | 不适用 | 本轮展示及资料调整，未新增保存/危险操作 |
| 交接责任、跨端事实、追溯 | 通过 | 保留现有数量；仓库单据名称在染色专用种子源纠正；未把已调拨记录改成工厂加工单 |
| 低分辨率、PDA、弱网、上传 | 通过 | 1366×768和1280×720主体宽度等于视口；PDA/弱网/上传不适用 |
| 命名页面、交互、大图、打印 | 通过 | 两页、工厂切换、编号搜索、详情及截图实测；打印映射跑相邻专项，实物打印不适用本轮 |

## 4. 问题标签

- 无本轮实现阻塞。历史“加工要求类型”业务分类及真实打印待确认项仍按此前矩阵保留。

## 5. 主要问题与处理

- 旧染色调拨构造统一指向WH-JKT-01，导致所有已发料行展示旧仓库。现在染色专用构造按具名场景读取面料中央仓/中转仓名称和编码，单据ID、行ID、单位、数量、接收事实均保留。
- DWO-002、DWO-003、DYE-COMBINED-DEMO-001、DYE-WATER-PO-202603-081分别展示berys konveksi、PT CIK INTERNUSA、SPF - 特种工艺、TMF - 辅料厂。它们原本没有已接收仓库记录，新增工厂演示单号和加工状态不会伪造接收。
- 下游包含Sea Cutting/新裁床中转仓、GKP面料中央仓、GTP辅料中央仓及印花厂。GKP/GTP/Sea Cutting/新裁床归属依据截图仓名括号中的标识作本地演示，不声称逐单关系是线上真实派单。
- 列表、详情、查询和CSV读取同一参与方信息；未建立真实工厂派单、库存、完整后端或迁移框架。非预设单不套用16行的具名分配。
- 新增信息仅在已有信息块内，复用原渲染与局部更新，不修改排序、列设置、选择或图标扫描机制。
- 本轮源码增量以 `/tmp/higoods-dyeing-parties-20260910/` 中before文件核查；此前累计差异属于同一染色对齐任务，均保留。

## 6. 最终结论

结论：通过。本轮名称、类别覆盖、单据分类和页面展示已本地验证；技术检查最终状态见收据，不把本地演示关系表述为真实业务交接。不关闭此前实物打印等未验收条目，不声明远端交付。

## 7. 变更覆盖与验证

### 受管文件

- `src/data/fcs/dye-work-order-demo-details.ts`
- `src/data/fcs/dye-work-order-online-view.ts`
- `src/data/fcs/warehouse-material-execution.ts`
- `src/pages/process-factory/dyeing/work-orders.ts`
- `src/pages/process-factory/dyeing/work-order-overlays.ts`

### 页面路由

- `/fcs/craft/dyeing/work-orders`
- `http://192.168.0.21:5188/fcs/craft/dyeing/work-orders`

### 验证命令

- `node --import tsx scripts/check-dyeing-list-sections.ts`：通过，16行资料、4个工厂来源、4类下游、参与方属性、仓库种子同源、未发出数量0、搜索和CSV。
- `node --import tsx scripts/check-dyeing-parity-20260910.ts`：通过，资料/图片、打印映射及条码独立于加工/交接回归。
- `npm run typecheck:entrypoints`：通过；最终工程类型与构建以收据为准。
- `npm run workflow:verify`：未运行（记录准备时）；最终累计任务检查和CodeGraph同步结果见 `/tmp/higoods-dyeing-parties-20260910/task-receipt.json` 与同目录 `verify.log`。

### 浏览器证据

- 当前服务IAB，全部工厂第一页10行：2个工厂来源、8个仓库来源，旧名称不存在；第二页6行旧名称不存在，图片加载失败0。
- GTG2行同屏展示：GKP面料中央仓到Sea Cutting中转仓；berys konveksi印花厂到PT CIK INTERNUSA印花厂。折叠菜单后保留双方完整截图于本任务工具输出。
- 点击DY-20260328-002详情：上游berys名称/编号/印花厂类型及YH-DEMO单号，下游PT CIK名称/编号/类型与列表一致。
- 搜索WH-FITTING-001返回唯一花边单：上游TMF - 辅料厂、加工单、加工完成待交出、交出量0；下游GTP/辅料中央仓/对应编码。
- 1366×768文档宽1366；1280×720文档宽1280。宽表内部滚动；本轮截图重点为伙伴名称和属性，未新增滚动实现。
- 验证结束恢复菜单展开、全部工厂第一页和原视口尺寸。

### 图片来源与对应关系

- 没有新增或替换图片。投入、商品、产出继续使用原有正式物料/商品映射。
- 全部16行两页检查没有破图，辅料示例使用15 mm水溶花边图片。大图行为未改，本轮不重复故障注入或实物打印。

### 例外

- 本轮不修改其他模块旧仓库，也不建立真实工厂上下游派单；明确限定为原型演示资料。
