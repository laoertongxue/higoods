# 水溶工序及加工单原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-07-11 |
| 相关需求 / 任务 | 水溶工序及独立加工单；按 BOM 物料拆解准备阶段产物并生成稳定执行事实 |
| 涉及系统 | PCS（技术包 BOM 与工序预览）；FCS（生产单拆解产物、独立水溶加工单与染厂能力） |
| 涉及页面路径 | 技术包详情 / 物料清单、技术包详情 / 工序工艺；既有 PDA 任务列表与任务详情仅调整通用数量显示契约，未新增水溶执行页面 |
| 端类型 | 管理端、员工执行端通用显示契约 |
| 主要角色 | 技术资料维护人员、生产跟单、染厂主管、查看任务数量的一线员工 |
| 主要任务 | 维护每条 BOM 物料的水溶要求；由物料级任务产物生成独立水溶加工单，分配具备水溶能力的染厂并处理数量不足与收货差异 |

## 2. 参考规范

- `docs/higood-indonesia-factory-product-design-guidelines.md`
- `docs/higood-indonesia-factory-prototype-review-checklist.md`

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色匹配 | 通过 | 管理与主管领域负责分厂、异常和数量批准；PDA 本轮只准确展示原 BOM 单位，不增加水溶操作。 |
| 任务清晰度 | 通过 | BOM 行直接维护“水溶要求”，工序页自动预览水溶工序及实际关联物料。 |
| 信息架构与导航 | 通过 | 沿用技术包既有 BOM 与工序页，不新增路由或跨系统入口。 |
| 页面模式 | 通过 | 管理端表格同时提供只读展示与编辑选择，符合技术资料维护场景。 |
| 信息负荷 | 通过 | 仅新增单位、水溶要求和组合顺序短提示，未引入额外配置层。 |
| 文案 | 通过 | 数据字典使用“水溶”“BOM物料”“任务单”等中文业务语义。 |
| 数量与状态 | 通过 | 单位缺失安全回退“米”，水溶要求缺失安全回退“否”，只读与编辑口径一致。 |
| 扫码与识别 | 通过 | 未新增扫码或水溶 PDA 动作；任务列表与详情继续复用既有任务识别。 |
| 防错 | 通过 | 所有物料类型均可选择；同一物料同时水溶和染色时提示并锁定“先水溶、后染色”。 |
| UI 样式 | 通过 | 复用既有表格和选择框样式，提示短且克制。 |
| 组件交互 | 通过 | 行内选择和新增/编辑弹窗均沿用现有事件模式，未引入整页框架或新状态层。 |
| 协作关系 | 通过 | BOM 行 ID 传递到物料级任务产物和独立水溶加工单；交出与收货先保留加工单内占位事实，不冒充通用交接已接入。 |
| 异常与追溯 | 通过 | 数量不足转主管处理，收货不一致转差异处理；加工单保留稳定来源产物、任务二维码和轻量操作记录。 |
| 现场设备可用性 | 通过 | PDA 列表与详情沿用原布局，仅将“公斤、卷”等精确单位用于数量短句，不增加字段或操作层级。 |

## 4. 问题标签

本次未遗留固定问题标签。当前范围只调整 PDA 通用数量显示契约，后续实现水溶现场操作时仍需另行复审。

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 规格审查发现公共产值模板未识别“水溶” | - | 跟单、染厂主管 | 将“水溶”纳入既有批次型模板 D，并由专项检查锁定公共模板和产值字段。 | 否 |
| 类型契约原先仅由 Node 擦除类型后运行，存在假绿风险 | - | 技术包维护与后续开发 | 新增独立类型契约文件，由项目本地 `tsc --noEmit` 先编译，再执行运行时检查。 | 否 |
| 加工单领域已建立，但完整页面与跨端执行链路不在当前范围 | - | 跟单、染厂主管、染厂一线员工 | 独立水溶移动任务已接入 PDA 统一执行任务入口并复用既有任务列表/详情；仍不宣称独立菜单、水溶完整执行交互或真实通用交接已实现，后续任务另行实现和复审。 | 否 |
| 普通染厂可能只有染色能力而没有水溶能力 | `选不对`、`缺主管兜底` | 生产跟单、染厂主管 | 分配时读取统一工厂能力，只有 `WATER_SOLUBLE` 能力才可承接；Mock 同时覆盖可承接与必须拦截的染厂，水溶能力仍归类为染厂能力。 | 否 |
| 实际完成量不足或收货数量不一致时不能静默推进 | `算不准`、`缺主管兜底`、`追溯不足` | 染厂主管、后续收货方 | 不足计划量必须填写原因并暂停，主管可继续加工、按实际量交出或退回返工；收货差异进入独立状态，确认后才完成。 | 否 |
| 字典覆盖演示产物原先只有 BOM ID，领域会把成衣数量和虚构物料当作水溶加工事实 | `算不准`、`追溯不足` | 生产跟单、染厂主管 | 字典覆盖演示产物改为读取确定性的生产单技术包 BOM，按 BOM 单耗、损耗和适用 SKU 计算计划量，并携带物料与单位；旧快照缺物料编码或单位时，由生成器沿用现有 BOM 规范化口径使用 BOM ID 或“米”，领域只接收字段完整的物料级任务产物，不再自行兜底。演示产物不等于实际业务单。 | 否 |
| 待交出状态可提交任意较小数量，绕过系统或主管批准 | `算不准`、`缺主管兜底` | 染厂主管、交出人员 | 本期不支持部分交出；提交数量必须与加工单批准交出量一致，正常完工和主管按实际量继续均使用同一口径。 | 否 |
| 继续补做时累计完成数量可能倒退 | `算不准`、`追溯不足` | 染厂主管 | 完工上报明确为累计完成数量；不得少于已有完成量，继续不足再次暂停，补足后才进入待交出；只有退回返工可清零。 | 否 |
| 工厂旧入驻能力可能覆盖正式能力禁用或不可派单 | `选不对` | 生产跟单 | 分厂同时校验工厂启用状态与派单资格；存在正式 `processAbilities` 时作为唯一真相源，仅无正式能力数据时兼容历史能力。 | 否 |
| 领域内存加工单与当前产物集合可能漂移 | `追溯不足`、`协作断裂` | 生产跟单、染厂主管 | 每次读取和动作前按 generationKey 对账：新增有效产物、移除消失产物，并保留既有执行态、工厂、数量与日志。 | 否 |
| BUNDLE 映射会把公斤、卷等原 BOM 单位显示成“打” | `读不懂`、`算不准` | 染厂一线员工 | `ProcessTask` 增加可选精确显示单位；水溶移动任务写入原 BOM 单位，PDA 列表与详情优先显示该单位，`QtyUnit` 枚举保持不变。 | 否 |
| PDA 统一入口仍保留错误通用 TASKGEN，领域水溶任务不可见且数量使用成衣件数 | `算不准`、`协作断裂` | 染厂一线员工、染厂主管 | 将领域水溶任务接入统一移动任务聚合；按 `coveredProcesses.sourceArtifactIds` 精确替换同一独立水溶产物的单工序 TASKGEN，保留普通任务和组合任务。运行时检查覆盖统一入口、索引、按 ID 查询和数量展示。 | 否 |
| 不同 BOM 行分别选择水溶、染色时可能被误当成同一物料组合 | `选不对`、`追溯不足` | 技术资料维护人员 | 按工序逐行计算并覆盖 `linkedBomItemIds`，新增、编辑、删除都会重新联动。 | 否 |
| 同一物料水溶与染色的先后顺序可能被手工调整破坏 | `点错风险` | 技术资料维护人员 | 页面显示“固定顺序：先水溶、后染色”，路线动作拒绝交换或并行为同一步。 | 否 |
| 规格审查发现预置倒序或同一步并行草稿可绕过页面动作保护直接确认 | `点错风险`、`追溯不足` | 技术资料维护人员 | 将共享 BOM 物料的“先水溶、后染色”约束下沉到通用路线归一化；确认、保存和运行时归一化统一生效。仅拆分共享水溶；染色仍有并行伙伴时保留原组语义，拆分后降为单项时清除失效并行标记。 | 否 |
| 代码审查发现多组与交叉并行组可能形成互换循环，达到次数上限后仍静默保留非法路线 | `点错风险`、`追溯不足` | 技术资料维护人员 | 先从含染色的混合组拆出参与共享 BOM 依赖的水溶子组，再按步骤组依赖图做稳定拓扑排序；返回前强校验全部共享 BOM 不变量，失败时明确阻断。增加多组、首个不共享、交叉组、仅两项并行及连续承接反例。 | 否 |
| legacy 与页面工序联合类型未直接继承正式技术包水溶字段 | `协作断裂` | 技术资料维护人员、后续生产单读取方 | legacy `TechPackProcessEntry` 与页面 `TechniqueItem` 直接引用 `TechnicalProcessEntry` 对应属性，专项 tsc 构造正式与 legacy 契约并检查页面单一来源。 | 否 |
| 生产单准备产物原先按工序汇总，可能把不同 BOM 行的水溶与染色要求误组合，且沿用成衣总数量 | `选不对`、`算不准`、`追溯不足` | 生产跟单、后续染厂主管 | 生产拆解改为读取生产单引用的正式快照，按生产单、快照、工序、BOM 行生成稳定产物；计划数量由适用 SKU 数量、BOM 单耗和损耗率自动计算并保留 BOM 单位。同一物料同时水溶和染色时只生成带固定路线的染色需求。 | 否 |
| BOM 产物 ID 只使用生产单固定快照号，切换正式技术包版本后可能错误复用旧产物身份 | `追溯不足`、`协作断裂` | 生产跟单、后续染厂主管 | BOM 产物版本键同时包含生产单快照号和正式技术包版本 ID，版本 ID 缺失时才回退版本编码或快照号；专项检查固定 snapshotId、只切换正式版本 A / B，锁定产物 ID 必须变化。 | 否 |
| 非准备阶段、CRAFT 或错误单据类型的染色工序可绕过门槛，多条路线还会为同一 BOM 重复生成产物 | `选不对`、`追溯不足` | 生产跟单、后续染厂主管 | 真实生产入口统一校验工序类型、阶段、单据类型和启用状态；BOM 产物按单据类型、工序和 BOM 行稳定去重，保留第一条路线 entry。PRINT、无 BOM 染色和其他工序继续走原兼容路径。 | 否 |
| BOM 产物 ID 直接清洗后用连字符拼接，不同 entry / BOM 标识可能碰撞 | `追溯不足`、`协作断裂` | 生产跟单、后续染厂主管 | BOM 身份段改为 URI 编码加长度前缀，无歧义组合生产单、快照、正式版本、entry 和 BOM；legacy 非 BOM ID 保持不变。 | 否 |
| 适用 SKU 无匹配或数量、单耗、损耗异常时仍可能生成 0、负数或非有限计划数量单据 | `算不准`、`追溯不足` | 生产跟单、后续染厂主管 | 计划数量纯函数统一校验 SKU 数量、适用数量、单位用量、损耗率及三位小数结果；异常时抛含 BOM ID、物料和原因的中文错误，阻断正式拆解，0% 损耗仍允许。 | 否 |
| 已无需水溶或染色的 NONE BOM 若残留工序关联，会在确认不生成前错误校验数量并阻断其他合法产物 | `选不对`、`协作断裂` | 生产跟单、后续染厂主管 | 每条 BOM 先按当前 WATER / DYE 工序判断是否确实生成产物，仅命中生成条件后才计算和校验数量；NONE 与组合场景的 WATER 跳过分支不再误阻断，真正生成的产物仍执行完整数量校验。 | 否 |

## 6. 最终结论

结论：通过

说明：

- 本阶段已完成 PCS 技术包 BOM 与工序预览；草稿只更新技术包工序预览，不生成加工单。
- 水溶按 BOM 物料触发，适用于所有 BOM 物料类型，目标对象不收窄为面料。
- 水溶归准备阶段、归属染厂、排序早于染色，默认生成任务单，不新增需求单、分厂、仓库、结算或批次能力。
- 规格审查发现的公共产值模板缺口已修复；水溶复用既有批次型模板 D，并已补齐专项运行时检查。
- BOM 与技术包工序类型契约已移入独立 `scripts/check-water-soluble-types.ts`，由项目本地 `tsc --noEmit` 真实编译，不再依赖 Node 的类型擦除结果。
- 所有 BOM 物料类型均可维护单位与水溶要求；同一物料同时水溶、染色时固定先水溶后染色。
- 正式技术包快照、运行时克隆和评审差异保留物料编码、单位、水溶要求及工序关联 BOM 行。
- FCS 生产单拆解已按正式快照中的具体 BOM 行生成准备产物：仅水溶生成独立水溶任务，仅染色生成普通染色需求，同一物料水溶加染色只生成带“先水溶、后染色”路线的染色需求，均未选择则不生成。
- 计划数量由系统按适用 SKU 生产数量 × BOM 单耗 ×（1 + 损耗率）计算，最多保留 3 位小数并保留 BOM 单位；产物 ID 包含生产单、正式快照、工序和 BOM 行，重复拆解不累加。
- BOM 产物 ID 已明确纳入正式技术包版本 ID；同一生产单固定 snapshotId 下切换正式版本会生成新身份，同 snapshotId 与同 version id 重复拆解保持幂等，legacy 非 BOM 产物 ID 不变。
- FCS 真实生产入口已锁定 WATER / DYE 资格门槛并对同一 BOM 稳定去重；BOM 产物 ID 使用无歧义分段编码，数量异常会明确阻断，不再静默生成错误单据。
- 数量校验只对当前工序确实会生成的 BOM 产物执行；残留关联的 NONE BOM 保持 0 产物且不影响其他合法产物，组合 BOM 的 WATER 分支也会直接跳过。
- FCS 已建立独立水溶加工单唯一领域来源：仅消费字段完整的 `WATER_SOLUBLE` 且 `TASK` 物料级产物，既包括真实物料级生成产物，也包括携带真实技术包 BOM、系统计算数量与单位的字典覆盖演示产物；演示产物仅服务原型状态覆盖，不等于实际业务单。`sourceDemandIds` 始终为空，不生成水溶需求单；重复读取保持稳定且不累加。
- 加工单已覆盖待分配染厂、待原料、待水溶、水溶中、生产暂停、待交出、交出待收货、收货差异和已完成；非法状态、重复动作、非有限数、非正数与超量交出均以中文原因阻断。
- 工厂分配已接入统一工厂能力：水溶能力归类为染厂能力；只有染色而无水溶能力的工厂不能承接。产能档案可投影水溶能力，使用批次型“批/分钟”口径与通用设备类型。
- 数量不足由系统进入主管处理，支持继续加工、按实际完成量交出、退回返工；交出/收货事实暂保存在加工单内，尚未创建或接入通用交接对象。
- 稳定的 `PROC_WATER_SOLUBLE` 领域移动任务已接入 PDA 统一执行任务入口；既有任务列表、详情、索引和 taskId / 扫码入口读取同一任务，数量使用 BOM 计划量和原单位。同一独立水溶产物的错误通用 TASKGEN 已按稳定 artifact 标识替换。
- 本任务仍未新增独立 PDA 菜单或水溶完整执行交互，现阶段只复用统一任务入口与通用任务详情；真实交接仍未接入。
- 加工单会在读取与动作前对账当前物料级产物：新增、移除按 generationKey 收敛，既有加工单执行状态、数量、工厂和日志不会被重复读取重置。
- 分厂资格以正式工厂能力为准，同时校验工厂启用状态和派单资格；交出必须等于批准数量，继续补做按累计完成数量上报且不得倒退。
- 规格审查补充的倒序、同一步并行、直接确认、无共享物料、重新同步、取消水溶和快照隔离反例均已纳入专项检查。
- 代码质量审查补充的多组共享关系、首个染色不共享、拆分后单项染色并行元数据及连续承接反例均已纳入专项检查。
- 质量复审补充的交叉并行组依赖反例已纳入专项检查，路线不再通过迭代互换和次数上限收敛。
- 本次完成 FCS 拆解产物、独立水溶加工单领域与染厂能力约束；不宣称水溶加工单 FCS 页面、PFOS 页面、PDA 页面或真实通用交接已实现，后续现场执行链路仍需按角色、动作、防错与异常兜底另行复审。
