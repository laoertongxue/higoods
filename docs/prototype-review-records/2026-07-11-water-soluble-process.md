# 水溶工序及加工单原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-07-11 |
| 相关需求 / 任务 | 水溶工序及加工单实现计划 |
| 涉及系统 | PCS（技术包 BOM 与工序预览）；FCS 仅冻结正式技术包快照字段 |
| 涉及页面路径 | 技术包详情 / 物料清单、技术包详情 / 工序工艺 |
| 端类型 | 管理端 |
| 主要角色 | 技术资料维护人员 |
| 主要任务 | 维护每条 BOM 物料的单位和水溶要求，并预览自动生成且绑定实际物料的准备工序 |

## 2. 参考规范

- `docs/higood-indonesia-factory-product-design-guidelines.md`
- `docs/higood-indonesia-factory-prototype-review-checklist.md`

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色匹配 | 通过 | 当前仅服务技术资料维护人员，不混入染厂主管或一线 PDA 操作。 |
| 任务清晰度 | 通过 | BOM 行直接维护“水溶要求”，工序页自动预览水溶工序及实际关联物料。 |
| 信息架构与导航 | 通过 | 沿用技术包既有 BOM 与工序页，不新增路由或跨系统入口。 |
| 页面模式 | 通过 | 管理端表格同时提供只读展示与编辑选择，符合技术资料维护场景。 |
| 信息负荷 | 通过 | 仅新增单位、水溶要求和组合顺序短提示，未引入额外配置层。 |
| 文案 | 通过 | 数据字典使用“水溶”“BOM物料”“任务单”等中文业务语义。 |
| 数量与状态 | 通过 | 单位缺失安全回退“米”，水溶要求缺失安全回退“否”，只读与编辑口径一致。 |
| 扫码与识别 | 通过 | 当前管理端资料维护不涉及扫码；未虚构 PDA 动作。 |
| 防错 | 通过 | 所有物料类型均可选择；同一物料同时水溶和染色时提示并锁定“先水溶、后染色”。 |
| UI 样式 | 通过 | 复用既有表格和选择框样式，提示短且克制。 |
| 组件交互 | 通过 | 行内选择和新增/编辑弹窗均沿用现有事件模式，未引入整页框架或新状态层。 |
| 协作关系 | 通过 | BOM 行 ID 传递到工序 `linkedBomItemIds`，正式技术包快照继续保留来源关系。 |
| 异常与追溯 | 通过 | 新增、编辑、删除后重新计算实际关联行；旧数据缺字段时有明确默认值。 |
| 现场设备可用性 | 通过 | 本阶段为管理端技术资料页，不涉及现场 PDA；现有弹窗布局保持不变。 |

## 4. 问题标签

本次未遗留固定问题标签。当前范围没有现场 PDA 操作，后续若实现工厂执行页需另行复审。

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 规格审查发现公共产值模板未识别“水溶” | - | 跟单、染厂主管 | 将“水溶”纳入既有批次型模板 D，并由专项检查锁定公共模板和产值字段。 | 否 |
| 类型契约原先仅由 Node 擦除类型后运行，存在假绿风险 | - | 技术包维护与后续开发 | 新增独立类型契约文件，由项目本地 `tsc --noEmit` 先编译，再执行运行时检查。 | 否 |
| 加工单与跨端执行链路不在当前范围 | - | 跟单、染厂主管、染厂一线员工 | 当前不生成加工单，也不宣称 FCS / PFOS / PDA 页面已实现；后续任务另行实现和复审。 | 否 |
| 不同 BOM 行分别选择水溶、染色时可能被误当成同一物料组合 | `选不对`、`追溯不足` | 技术资料维护人员 | 按工序逐行计算并覆盖 `linkedBomItemIds`，新增、编辑、删除都会重新联动。 | 否 |
| 同一物料水溶与染色的先后顺序可能被手工调整破坏 | `点错风险` | 技术资料维护人员 | 页面显示“固定顺序：先水溶、后染色”，路线动作拒绝交换或并行为同一步。 | 否 |

## 6. 最终结论

结论：通过

说明：

- 本阶段已完成 PCS 技术包 BOM 与工序预览；草稿只更新技术包工序预览，不生成加工单。
- 水溶按 BOM 物料触发，适用于所有 BOM 物料类型，目标对象不收窄为面料。
- 水溶归准备阶段、归属染厂、排序早于染色，默认生成任务单，不新增需求单、分厂、仓库、结算或批次能力。
- 规格审查发现的公共产值模板缺口已修复；水溶复用既有批次型模板 D，并已补齐专项运行时检查。
- BOM 与技术包工序类型契约已移入独立 `scripts/check-water-soluble-types.ts`，由项目本地 `tsc --noEmit` 真实编译，不再依赖 Node 的类型擦除结果。
- 所有 BOM 物料类型均可维护单位与水溶要求；同一物料同时水溶、染色时固定先水溶后染色。
- 正式技术包快照、运行时克隆和评审差异保留物料编码、单位、水溶要求及工序关联 BOM 行。
- 本次不宣称加工单、FCS 页面、PFOS 页面或 PDA 页面已实现；后续现场执行链路仍需按角色、动作、防错与异常兜底另行复审。
