# PDA 交接待接收三入口紧凑布局原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-09-03 |
| 相关需求 / 任务 | `PDA-HANDOVER-ENTRY-001`：待接收页一行展示回货接收、自助回货、辅料接收三个紧凑入口 |
| 记录模式 | 完整产品审查 |
| 涉及系统 | PFOS / FCS PDA |
| 涉及页面路径 | `/fcs/pda/handover?tab=pickup` |
| 端类型 | 员工执行端 |
| 主要角色与任务 | 后道工厂操作员接收回货；后道管理员开启车缝自助回货；后道仓管接收待入库辅料 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：待接收页原“后道回货接收”大面板和常驻辅料扫码框收口为一行三个紧凑入口；辅料扫码改为点击入口后打开底部弹层。原回货接收、自助回货、辅料整单入库、待入库列表、数量与状态规则不变。

审查依据：`AGENTS.md` 第 4 节员工执行端、交接和 PDA 基线，第 5 节局部交互规则，第 7 节分层验证和证据新鲜度。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 三个入口按回货操作员、管理员、仓管的现场动作命名；待接收仍为当前页主任务 |
| 文案、状态、数量与单位 | 通过 | 辅料入口展示待入库单数；下方列表保留单号、物料种数、调出方实配数量和目标库位 |
| 扫码、真实图片与对象识别 | 通过 | 回货接收仍进入送货单扫描页；辅料接收弹层仍只接受完整调拨单号；本次不改变对象图片展示 |
| 防错、危险确认与主管兜底 | 通过 | 自助回货仍仅管理员可开启；错误辅料单号提示后弹层保留，可直接改正 |
| 交接、跨端事实与异常追溯 | 通过 | 三个入口仍分别读取原送货、车缝自助回货和辅料调拨事实，不新建平行数据 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 390×844、400×786、480×800 下三个卡片同排、单卡 68px、无横向溢出；本次不涉及上传与弱网规则 |
| 命名路由、交互、图片大图与打印 | 通过 | 三个入口均完成点击回归；辅料弹层打开、关闭、错误后恢复和正确单号进入详情均通过；打印不适用 |

## 4. 问题标签

- `视觉干扰`
- `字段过载`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 回货与辅料入口纵向占用过高，首屏只能看到少量待接收记录 | 视觉干扰 | 后道操作员、仓管 | 三个入口改为一行三卡，单卡固定 68px；辅料扫码按需弹出 | 否 |
| 辅料扫码框常驻在列表前方 | 字段过载 | 后道仓管 | 扫码框放入“辅料接收”底部弹层，下方待入库列表保持直接可见 | 否 |

## 6. 最终结论

结论：通过（本地原型范围）

说明：本次只对现有待接收页入口区做迭代收紧，没有改变页面结构主线、业务数据、单据状态或数量规则。物理 PDA 和生产环境仍需现场验收。

## 7. 变更覆盖与验证

### 受管文件

- `src/pages/pda-handover.ts`

### 页面路由

- `/fcs/pda/handover?tab=pickup`
- `/fcs/pda/post-finishing/return-confirm`
- `/fcs/pda/handover/sewing-self-return`
- `/fcs/pda/handover?tab=pickup&materialOrderNo=DB-PF-202608-001`

### 验证命令

- `PLAYWRIGHT_REUSE_EXISTING_SERVER=false CUTTING_E2E_PORT=4202 npx playwright test tests/post-finishing-pda-role-boundary.spec.ts --workers=1 --reporter=line`：通过（7/7）
- `npm run check:post-finishing-web-pda-fallback`：通过
- `npm run check:post-finishing-cross-terminal-ui`：通过（3 个生产单、每单 5 个 SKU、每单 5 次回货的静态跨端门禁）
- `git diff --check`：通过
- `npm run build`：通过（Vite 2396 个模块转换完成）
- `npm run check:prototype-design-governance -- --all`：通过（本记录补齐明确结果后最终复跑）

### 真实图片验证

- 本次没有新增款式或物料对象，也未改变原对象图片映射、加载失败态和大图能力；不新增图片素材。

### 例外

- `npm run check:pda-handover-pages` 的全模块历史检查仍报“移动端交接缺少中转袋闭环字段：移除菲票”，与本次后道工厂三入口布局无关；本次命名路由和交互由专项浏览器测试覆盖。
- 本地浏览器证据不代表物理 PDA、真实扫码枪或生产环境验收。

### 页面证据

- `output/playwright/pda-handover-compact-entry-20260903/390x844-pending-three-entries.png`
- `output/playwright/pda-handover-compact-entry-20260903/390x844-material-receive-dialog.png`
