# PCS 改款打样任务 BOM 补种热修复审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-08-12 |
| 任务边界 | 修复打开“改款打样任务”时，任务 Mock 重新补种并错误回写已有已确认 BOM 的异常 |
| 记录模式 | 完整产品审查 |
| 涉及系统 | PCS |
| 页面路径 | `/pcs/engineering/revision-sampling` |
| 端类型 | 管理端 |
| 主要角色 | 跟单查看改款打样任务；买手已维护或确认的 BOM 必须保持不变 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：浏览器保留既有 BOM、但独立打样任务存储需要重新补种时，修复前打开列表会抛出“只有草稿 BOM 与价格可以修改”，页面无法正常加载。
- 修复后：既有 BOM 在补种过程中只读取、不回写；列表正常展示，已确认 BOM 的内容和状态不变。
- 页面结构、字段、文案、路由、图片和正常业务操作均未改变。

当前审查依据：

- `AGENTS.md` 第 4 节：印尼工厂现场产品设计基线。
- `AGENTS.md` 第 5 节：UI 与交互专项规则。
- `AGENTS.md` 第 7 节：验证原则。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色与任务 | 通过 | 跟单仍在改款打样列表查看任务，未改变角色权限。 |
| 文案、状态、数量与单位 | 通过 | 列表仍显示 12 条改款任务，原有任务状态和 BOM 颜色版本数量保持不变。 |
| 对象识别与图片 | 通过 | 页面继续使用既有款式档案图片，并与 SPU、款式名称同块展示；本次未改变图片能力。 |
| 防错与数据保护 | 通过 | 已有 BOM 不再被 Mock 初始化覆盖；已确认状态和确认时间在页面重载前后保持一致。 |
| 交接、跨端事实与异常追溯 | 通过 | 本次只修复 PCS 管理端列表的数据初始化，不改变 PDA、打印和跨端事实。 |
| 低分辨率与交互 | 通过 | 页面结构未变化；命名路由真实重载后正常渲染，控制台 0 error、0 warning。 |

## 4. 问题标签

- `点错风险`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| Mock 补种错误回写已有 BOM，导致列表加载失败并可能覆盖买手事实 | 点错风险 | 跟单、买手 | 补种前识别任务已有 BOM；已有版本一律只读，只有本次新建且仍为草稿的版本可以写入演示用料 | 否 |

## 6. 最终结论

结论：通过

说明：该热修复只阻止 Mock 补种覆盖既有 BOM，不放宽真实 BOM 的草稿编辑校验。

## 7. 变更覆盖与验证

| 需求 | 实现位置 | 验证证据 | 结果 |
| --- | --- | --- | --- |
| 已有 BOM 只读 | `src/data/pcs-engineering-master-sampling.ts` | 补种前记录任务已有 BOM ID；只有本次新建且仍为草稿的版本可以写入演示用料 | 通过 |
| 已确认 BOM 不被覆盖 | `tests/pcs-independent-sampling.spec.ts` | 先确认种子 BOM，再重新补种；断言不抛错且序列化快照完全一致 | 通过 |
| 改款列表可打开 | `/pcs/engineering/revision-sampling` | 浏览器构造已确认 BOM 并删除独立打样存储后重载：显示“共 12 条”，控制台 0 error、0 warning | 通过 |
| 关联 BOM 状态保留 | BOM Mock 存储 | 重载后状态仍为 `COMPLETED_CONFIRMED`，确认时间保持 `2026-08-12 13:58:00`，记录数仍为 48 | 通过 |

### 受管文件

- `src/data/pcs-engineering-master-sampling.ts`

### 页面路由

- `/pcs/engineering/revision-sampling`

### 验证命令

- `./node_modules/.bin/tsx tests/pcs-independent-sampling.spec.ts`：通过。
- `npm run check:pcs-engineering-master`：通过，23/23。
- `npm run build`：通过；仅保留仓库既有的大分块提示。
- `Playwright 命名路由重放`：通过；原始异常场景不再报错。

### 真实图片验证

- 本次未改变图片资源和图片交互；命名路由重放确认既有款式图仍与 SPU、款式名称同块展示。

### 例外

- 任务收据未生成：热修复使用的临时 `main` worktree 未初始化独立 CodeGraph；项目根 CodeGraph 索引状态健康，专项测试、生产工程门禁、构建和命名页面浏览器验收均已完成。
