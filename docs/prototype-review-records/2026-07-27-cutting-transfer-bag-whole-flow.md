# 裁床中转袋整袋流转原型累计审查记录

> 本记录覆盖裁床中转袋整袋流转整体方案，随任务推进持续更新。当前已审查任务 1、任务 2；任务 3、任务 4 完成后在同一记录中补充。

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-07-27 |
| 相关需求 / 任务 | 裁床中转袋整袋流转快速原型；当前覆盖任务 1、任务 2 |
| 涉及系统 | FCS |
| 涉及页面路径 | `/fcs/pda/warehouse/wait-handover?scope=cutting`；`/fcs/pda/cutting/inbound/<taskId>`；`/fcs/pda/cutting/inbound/<taskId>?action=inbound-location` |
| 端类型 | 员工执行端（PDA） |
| 主要角色 | 印尼裁床仓务操作员 |
| 主要任务 | 从五个明确入口进入现场动作；完成菲票装袋；完成中转袋入仓 |

## 2. 参考规范

- `docs/higood-indonesia-factory-product-design-guidelines.md`
- `docs/higood-indonesia-factory-prototype-review-checklist.md`
- `AGENTS.md` 中 PDA 页面执行优先、中文化、轻交互与性能治理要求

## 3. 当前实施范围

### 3.1 任务 1：待交出仓五入口

已实现：

- 裁床待交出仓根页统一展示「菲票装袋」「中转袋入仓」「中转袋交出」「特殊工艺回仓」「菲票打编号」五个现场动作入口。
- 五个入口直接进入对应操作页，不在根页继续展示候选列表或说明性流程卡。
- 旧 `action` 地址自动进入新的对应操作页，避免旧入口形成死路。
- 「中转袋交出」使用独立入口名称和路由语义，并可返回五入口根页。

### 3.2 任务 2：菲票装袋与中转袋入仓

已实现：

- 「菲票装袋」固定为「1 扫中转袋、2 扫菲票、3 确认装袋」。
- 菲票扫码识别成功后立即加入当前袋，可连续扫描，不再要求第二次点击「加入菲票」。
- Enter 扫描立即校验；无 Enter 的扫码枪输入停顿约 150ms 后统一校验，输入过程中不重复构造候选数据。
- 扫码等待按任务、执行单和模式状态键分轮次管理；确认装袋前同步完成待处理扫描，成功清空或离开当前模式/路由时取消等待，旧轮次回调不得写回新一轮。
- 未知、未打印、作废或跨生产单菲票在扫描完成时立即局部提示，清空扫码框便于重扫，并保留袋码和已扫菲票。
- 同一袋首张菲票确定生产单，后续不同生产单菲票禁止加入，符合一个唛架和中转袋链路只属于一个生产单的业务规则。
- 页面仅保留袋码、已扫菲票数量、裁片数量和简短菲票编号，删除生产单、铺布单、面料、状态、记录、候选列表和说明卡。
- 装袋成功明确提示「装袋成功」，并在同一路由清空袋码、菲票、数量和上次扫码结果，立即进入新一轮。
- 「中转袋入仓」固定为「1 扫中转袋、2 扫库区库位、3 确认入仓」，不扫描或展示菲票。
- 入仓成功明确提示「入仓成功」，并清空袋码、库位进入新一轮。
- 装袋或入仓失败时保留本轮已扫数据，便于现场修正后重试。
- 当前为快速原型，仅使用页面本地 Mock 状态，不写事件账、不做跨模块一致性或真实持久化。

### 3.3 待后续更新

- 任务 3：补充「中转袋交出」操作页的最终结构、扫码、防错、异常和返回路径审查。
- 任务 4：补充 Web 页面、整体链路联调、浏览器分辨率与交互性能终验，并更新最终结论。

## 4. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色匹配 | 通过 | 页面面向裁床仓务一线员工，只保留当前动作和必要识别信息。 |
| 任务清晰度 | 通过 | 五入口名称直接；装袋和入仓均为三个短步骤，每页只有一个主要确认按钮。 |
| 信息架构与导航 | 通过 | 根页负责选择动作，操作页负责执行；旧入口可直达新页面，底部壳层和返回根入口保留。 |
| 页面模式 | 通过 | 使用 PDA 扫码执行页和简短结果区，不使用管理端表格或候选列表。 |
| 信息负荷 | 通过 | 删除生产单、铺布单、面料、阶段、历史、统计和说明卡等非执行信息。 |
| 文案 | 通过 | 使用「扫中转袋」「扫菲票」「确认装袋」「扫库区库位」「确认入仓」等短句动作词。 |
| 数量与状态 | 通过 | 菲票张数和裁片片数自动累计并带单位；入仓模式不展示无关菲票数量。 |
| 扫码与识别 | 通过 | Enter 优先完成扫描，并以约 150ms debounce 兼容无 Enter 扫码枪；识别成功自动加入，失败即时局部提示。 |
| 防错 | 通过 | 阻断未扫袋先加菲票、未知码、重复菲票、未打印、作废、未完成打编号、跨生产单及必填缺失。 |
| UI 样式 | 通过 | 低密度单列步骤、大输入框、唯一大号主按钮，颜色仅用于结果反馈。 |
| 组件交互 | 通过 | 输入使用 `data-skip-page-rerender`；菲票加入只局部更新扫码结果区。 |
| 协作关系 | 有条件通过 | 当前完成入口、装袋和入仓现场动作；交出责任转移由任务 3 补齐。 |
| 异常与追溯 | 有条件通过 | 当前失败可恢复且保留扫码数据；快速原型不做事件账，任务 3、任务 4 再审整体追溯。 |
| 现场设备可用性 | 有条件通过 | 代码层已控制低信息密度和局部刷新；实际 PDA、1024×768 与 200ms 浏览器测量由任务 4 终验。 |

## 5. 性能与可恢复性自查

- 高频扫码输入标记 `data-skip-page-rerender="true"`，避免每次输入触发整页重绘。
- 输入字符阶段仅更新本地值并重置 150ms debounce，不构造菲票候选投影；Enter 到达时取消等待并立即校验。
- 确认装袋先同步冲刷当前状态键的待处理扫描，再按最新状态确认；成功重置、模式切换和路由离开均取消 timer，轮次令牌阻断迟到回调。
- 菲票命中后只清空扫码输入并刷新已扫结果区域，不重新扫描整页图标。
- 「确认装袋」「确认入仓」只在提交结果变化时刷新当前路由页面，以便立即呈现新一轮。
- 失败结果通过本地状态保留袋码、菲票、数量或库位，不要求重新进入页面。
- 聚焦检查覆盖自动加入、连续扫码、成功清空、失败保留、两模式字段与唯一按钮。
- `npm run build` 已通过；实际设备响应时间和低分辨率视觉检查待任务 4 更新。

## 6. 问题标签

- `追溯不足`：仅指快速原型按需求不写事件账，非当前页面操作缺陷；任务 4 复核整体演示口径。

## 7. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 原页面字段、状态、历史和说明过多 | `字段过载`、`视觉干扰` | 裁床仓务操作员 | 任务 1 收口为五入口，任务 2 收口为两种三步执行页 | 否 |
| 菲票扫码后还需点击「加入菲票」或等待提交才知道扫错 | `点错风险` | 裁床仓务操作员 | Enter 或 150ms 停顿完成扫码后统一校验；成功自动加入，失败即时提示并清空输入；确认、重置和离开页面均收口 pending timer | 否 |
| 同一袋可能混入不同生产单菲票 | `选不对`、`点错风险` | 裁床仓务操作员 | 首张票锁定当前袋生产单，跨生产单扫码立即拒绝并保留已扫数据 | 否 |
| 入仓模式混入菲票扫描和列表 | `选不对`、`字段过载` | 裁床仓务操作员 | 入仓页仅保留袋码、库区库位和确认入仓 | 否 |
| 提交失败可能丢失已扫数据 | `点错风险` | 裁床仓务操作员 | 失败保留当前袋码、菲票、数量或库位 | 否 |
| 交出责任转移与 Web 整体口径尚未完成本轮审查 | `协作断裂`、`追溯不足` | 裁床仓务操作员、主管、管理人员 | 任务 3、任务 4 完成后继续更新本记录 | 是，当前不影响任务 1、任务 2 原型演示 |

## 8. 最终结论

结论：有条件通过

说明：

- 任务 1、任务 2 已实现范围通过角色、任务、PDA 信息负荷、文案、UI、防错、失败恢复和代码级性能自查。
- 当前例外是按快速原型范围仅保存页面本地 Mock 状态，不实现事件账、真实持久化或跨模块一致性。
- 整体方案尚待任务 3 的中转袋交出页面和任务 4 的 Web/浏览器终验；完成后继续更新本记录并给出最终结论。
