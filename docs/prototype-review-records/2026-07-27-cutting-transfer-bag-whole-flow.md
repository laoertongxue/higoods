# 裁床中转袋整袋流转原型累计审查记录

> 本记录覆盖裁床中转袋整袋流转整体方案，随任务推进持续更新。任务 1 至任务 4 已完成自动化复核；任务 5 第二轮真实浏览器对抗式审查发现的确认性能阻塞已完成修复与复测；2026-07-28 已补充 Web 待交出仓工作台与弹窗入口回归修复。

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-07-27 |
| 相关需求 / 任务 | 裁床中转袋整袋流转快速原型；覆盖任务 1 至任务 5 |
| 涉及系统 | FCS |
| 涉及页面路径 | Web：`/fcs/craft/cutting/warehouse-management/wait-handover`；PDA：`/fcs/pda/warehouse/wait-handover?scope=cutting`、`/fcs/pda/cutting/inbound/<taskId>`、`/fcs/pda/cutting/inbound/<taskId>?action=inbound-location`、`/fcs/pda/cutting/handover/<taskId>?action=transfer-bag-handover` |
| 端类型 | 员工执行端（PDA）；主管 / 文员执行端（Web） |
| 主要角色 | 印尼裁床仓务操作员；裁床仓管主管 / 文员 |
| 主要任务 | PDA 从五个明确入口进入现场动作；Web 通过编号输入完成菲票装袋、中转袋入仓和中转袋整袋交出 |

## 2. 参考规范

- `docs/higood-indonesia-factory-product-design-guidelines.md`
- `docs/higood-indonesia-factory-prototype-review-checklist.md`
- `AGENTS.md` 中 PDA 页面执行优先、中文化、轻交互与性能治理要求

## 3. 当前实施范围

### 3.1 任务 1：待交出仓五入口

已实现：

- 裁床待交出仓根页统一展示「菲票装袋」「中转袋入仓」「中转袋交出」「特殊工艺回仓」「菲票打编号」五个现场动作入口。
- 五个入口直接进入对应操作页，不在根页继续展示候选列表或说明性流程卡。
- 旧 `action` 地址自动进入新的对应操作页，避免旧入口形成死路。
- 「中转袋交出」使用独立入口名称和路由语义，并可返回五入口根页。
- 根路由识别旧 `action` 后立即跳转；已删除跳转之后不可达的旧候选、暂存袋、投影构建和子页渲染链，不改变五入口、旧地址兼容或现场操作结果。

### 3.2 任务 2：菲票装袋与中转袋入仓

已实现：

- 「菲票装袋」固定为「1 扫中转袋、2 扫菲票、3 确认装袋」。
- 菲票扫码识别成功后立即加入当前袋，可连续扫描，不再要求第二次点击「加入菲票」。
- Enter 扫描立即校验；无 Enter 的扫码枪输入停顿约 150 ms 后统一校验，输入过程中不重复构造候选数据。
- 扫码等待按任务、执行单和模式状态键分轮次管理；确认装袋前同步完成待处理扫描，成功清空或离开当前模式/路由时取消等待，旧轮次回调不得写回新一轮。
- 未知、未打印、作废或跨生产单菲票在扫描完成时立即局部提示，清空扫码框便于重扫，并保留袋码和已扫菲票。
- 同一袋首张菲票确定生产单，后续不同生产单菲票禁止加入，符合一个唛架和中转袋链路只属于一个生产单的业务规则。
- 页面仅保留袋码、已扫菲票数量、裁片数量和简短菲票编号，删除生产单、铺布单、面料、状态、记录、候选列表和说明卡。
- 装袋成功明确提示「装袋成功」，并在同一路由清空袋码、菲票、数量和上次扫码结果，立即进入新一轮。
- 「中转袋入仓」固定为「1 扫中转袋、2 扫库区库位、3 确认入仓」，不扫描或展示菲票。
- 入仓成功明确提示「入仓成功」，并清空袋码、库位进入新一轮。
- 装袋或入仓失败时保留本轮已扫数据，便于现场修正后重试。
- 装袋与入仓确认后只替换当前扫码工作区内容，成功和失败均立即显示反馈，不再触发应用根节点整页重绘。
- 局部替换后会聚焦新生成的真实输入框：成功进入下一轮袋码；袋码错误回到袋码；菲票错误回到菲票；库区库位错误回到库位，并使用 `preventScroll` 保持页面和工作区滚动位置。
- 页面本地 Mock 台账明确记录袋、菲票和库位状态：只有空袋可装袋、待装袋菲票可加入、已装袋待入仓袋可进入启用的裁床库位。
- 不存在袋、非空袋、同轮或跨轮重复菲票、已作废 / 已装袋菲票、空袋 / 已入仓 / 已交出 / 已作废袋、无效 / 停用 / 非裁床库位均被自动检查覆盖并阻断；成功后同步更新袋、菲票和库位归属，失败不修改台账。
- 当前为快速原型，仅使用页面本地 Mock 状态，不写事件账、不做跨模块一致性或真实持久化。

### 3.3 任务 3：中转袋整袋交出

已实现：

- 新 `transfer-bag-handover` 模式与旧交出装袋确认、特殊工艺回仓、默认通用交出分支隔离，不复用拆袋装袋页面。
- 历史深链 `action=handover-bagging-confirm` 在页面入口统一规范化为 `transfer-bag-handover`；实际渲染整袋交出页，旧逐菲票确认命令在该模式下不可执行，不再形成可达旧写入口。
- 页面固定为「1 扫中转袋、2 扫车缝任务、3 确认交出」，只保留一个主要按钮「确认交出」。
- 最小交出单位固定为整个中转袋，不扫描或选择菲票，不输入数量，不展示来源袋、目标袋或部分交出结构。
- 车缝任务扫码后自动带出车缝任务号、生产单号和接收工厂；工厂只读，不提供手选入口。
- 袋内菲票数量由袋码 Mock 数据只读带出，仅用于整袋识别，不允许选择袋内部分内容。
- 确认前允许改扫同一生产单且可接收的车缝任务，并覆盖任务号、生产单号和接收工厂草稿；跨生产单或不可接收任务仍被阻断并保留原有效草稿。
- 已有正式车缝任务草稿时改扫中转袋，同生产单仍可继续，跨生产单会立即阻断并保留原有效袋与任务草稿，不允许静默污染组合关系。
- 确认前手动清空车缝任务输入会清除任务号、生产单号、接收工厂和旧扫码反馈，但保留已识别袋及袋内菲票数量；随后必须重新扫描任务才能确认。
- 确认前手动清空中转袋输入会重置整轮，同时清除袋、车缝任务及其派生信息；随后必须从中转袋重新开始。
- 只有确认成功后才把最终任务写入袋台账的 `boundSewingTaskNo` 并形成真实绑定；已确认绑定阻断换绑，袋状态更新为「已交出」后同时阻断重复扫描和重复确认。
- 本页 Mock 袋记录生产单和「待交出 / 已交出 / 已作废」状态，车缝任务记录生产单、接收工厂和是否可接收；扫码与确认都会阻断袋非待交出、任务不可接收及生产单不一致。
- 确认成功后先把当前页袋状态改为「已交出」并记录唯一车缝任务，再清空页面进入新一轮；再次扫描或用旧状态重复确认同袋都会即时阻断。
- Enter 立即完成扫描；无 Enter 输入停顿约 150 ms 后完成。确认前同步冲刷最新等待扫描，旧轮次回调不能在确认或成功重置后写回。
- 袋码与车缝任务允许快速连续扫码；单字段扫描完成只校正本字段输入并刷新 live 派生区，不回写另一个尚待处理的输入框。主动清空袋码或确认后的整轮刷新仍按业务规则重建全部字段。
- 全局键盘事件已精确接入袋码和车缝任务扫码框，Enter 会把原始事件传给交出 handler；不匹配特殊工艺或默认交出字段。
- 路由离开清理已下沉到 `appStore.navigate` 和 `syncFromBrowser` 统一边界；菲票装袋 / 入仓与中转袋交出分别派发对应 leave 事件，main 导航、PDA 顶部待办、退出登录和浏览器前进后退均经过此边界，同 URL 不派发，且 main 不再重复派发。
- 交出成功明确提示「交出成功」，清空袋码、任务码和派生信息，并留在同一路由开始新一轮；失败即时提示并保留有效扫码数据。
- 中转袋交出工作区使用独立局部刷新标识；局部更新 helper 只有在工作区存在、内容已替换且新输入框可聚焦时才返回成功，handler 才返回 `handled-locally`。工作区缺失时返回普通 `true`，main 保留整页渲染回退；特殊工艺和既有 handover action 仍保持原事件与渲染路径。
- 局部替换后，成功进入下一轮袋码；袋码错误回到袋码；任务错误回到车缝任务码，均禁止聚焦引发自动滚动。
- 页面不展示生产现场全链路、铺布、面料、当前情况、最近记录或说明流程卡，返回路径固定为五入口根页。
- 当前为快速原型，仅使用当前页本地 Mock 状态，不写事件账、不做跨模块一致性、真实持久化或 API。

### 3.4 任务 4：Web 三操作与整体终验

已实现：

- Web 既有待交出仓菜单路由继续进入原仓库工作台，保留库存明细、菲票装袋、中转袋入仓、交出装袋确认、特种工艺回收入仓和库区库位等既有页面能力。
- 工作台顶部「菲票装袋」「中转袋入仓」「中转袋交出」三个按钮均在当前页面打开聚焦弹窗，不再替换为独立操作页面，也不改变当前 URL。
- Web 端统一使用编号输入和按钮确认，不使用「扫码」措辞，也不依赖摄像头、二维码组件或扫码设备。
- 「菲票装袋」只输入中转袋编号和菲票编号；菲票可逐张加入紧凑明细，不展示待入仓候选大列表、生产 / 铺布 / 面料或流程说明。
- 「中转袋入仓」只输入中转袋编号和库区库位，不包含菲票字段、菲票列表或菲票关系改写。
- 「中转袋交出」只输入中转袋编号和车缝任务号；生产单号与接收工厂由任务只读带出，不提供工厂选择、菲票、数量、部分交出或拆袋入口。
- Web 本地 Mock 覆盖同生产单、待交出、任务可接收、跨生产单、不可接收任务、已交出袋和重复交出门禁；与 PDA 一样按整袋确认，但不为快速原型引入跨端共享状态、事件账、API 或真实持久化。
- 菲票候选 Mock 与既有袋内菲票使用互不重复的编号；装袋时除菲票状态外还检查所有袋的实际归属，即使状态数据脏污也会阻断跨袋重复装入。
- 三个动作成功分别显示「装袋成功」「入仓成功」「交出成功」，局部清空本轮输入并停留在当前操作开始新一轮；失败只更新操作区反馈并保留有效输入。
- 高频输入框不绑定逐字符整页重绘；加入菲票、查询任务与确认动作均只更新当前弹窗工作区，不改写应用根节点、不丢失底层工作台。
- 弹窗内「加入」「查询任务」「确认」按钮均显式跳过页面级重绘；业务处理器局部更新弹窗后，主事件分发不得再次渲染整页并移除弹窗。
- `check:web-cutting-transfer-bag-actions` 同时执行命令处理器、候选状态更新、跨袋重复归属防错、成功清空 / 失败保留，并校验路由回到仓库工作台、三按钮弹窗触发与全局事件处理器真实接线；已纳入 `check:cutting:all`。
- `tests/cutting-wait-handover-web-modal.spec.ts` 使用真实 Chromium 验证原工作台能力仍可见，三个按钮逐个打开对应弹窗并执行装袋、入仓、查询任务与整袋交出；操作反馈始终留在弹窗内，关闭后弹窗移除且 URL 不变。
- 第二轮真实浏览器对抗审查曾实测装袋确认从 click 到「装袋成功」DOM 出现为 346.7 ms，超过 `< 200 ms` 门槛；根因为 `main.ts` 在页面 handler 更新状态后仍执行根节点整页重绘。本轮已改为工作区局部刷新并完成复测。

## 4. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色匹配 | 通过 | PDA 面向裁床仓务一线员工；Web 面向裁床仓管主管 / 文员，均只保留当前动作和必要识别信息。 |
| 任务清晰度 | 通过 | PDA 五入口名称直接；Web 从待交出仓工作台顶部直接打开三个聚焦弹窗；装袋、入仓和整袋交出各只有一个主要确认按钮。 |
| 信息架构与导航 | 通过 | PDA 根页负责选择动作、操作页负责执行；Web 保留原待交出仓工作台的信息查询与页签能力，三个新增操作不离开当前页面。 |
| 页面模式 | 通过 | PDA 使用扫码执行页；Web 使用管理工作台加短编号输入弹窗；弹窗不放候选大列表或管理宽表。 |
| 信息负荷 | 通过 | 各操作页仅显示当前动作所需信息；交出页只增加任务派生的生产单号和接收工厂防错信息，不展示铺布、面料、历史、统计或说明卡。 |
| 文案 | 通过 | PDA 使用「扫中转袋」等动作词；Web 使用「中转袋编号」「菲票编号」「车缝任务号」等输入文案；页面标题、按钮和结果统一为「中转袋交出」。 |
| 数量与状态 | 通过 | 装袋页自动累计菲票张数和裁片片数；入仓不展示菲票；交出仅只读显示整袋菲票张数，不输入或拆分交出数量。 |
| 扫码与识别 | 通过 | PDA 以扫描为主；Web 以编号输入、加入 / 查询按钮为主。两端识别失败均即时局部反馈。 |
| 防错 | 通过 | 阻断必填缺失、未知码、重复菲票、作废、跨生产单、交出袋状态异常、任务不可接收、同袋绑定多个任务和重复交出。 |
| UI 样式 | 通过 | PDA 为低密度单列步骤；Web 为工作台上的短表单弹窗；每个操作均保持唯一主要确认按钮，弹窗标题不重复，颜色仅用于结果反馈。 |
| 组件交互 | 通过 | 高频输入使用 `data-skip-page-rerender`；PDA 专用确认仅在 handler 明确返回 `handled-locally` 时跳过整页渲染，局部容器缺失会回退整页渲染；其他旧 handover action 不受影响。 |
| 协作关系 | 通过 | 交出页明确一个中转袋整体交给一个车缝任务，生产单与接收工厂由任务自动带出。 |
| 异常与追溯 | 通过 | 失败可恢复且保留本轮数据；本地 Mock、不做事件账 / API / 真实持久化是本次快速原型边界，不作为审查例外。 |
| 现场设备可用性 | 通过 | 390×844 PDA、1366×768 与 1280×720 Web 均完成真实 Chromium 复测且无页面横向溢出；PDA 七个确认场景还在非零页面和工作区滚动位置重复 3 轮，4× CPU 压力下结果反馈仍小于 200 ms，焦点正确且页面脚本无错误。 |

## 5. 性能与可恢复性自查

- 高频扫码输入标记 `data-skip-page-rerender="true"`，避免每次输入触发整页重绘。
- 输入字符阶段仅更新本地值并重置 150 ms debounce，不构造菲票候选投影；Enter 到达时取消等待并立即校验。
- 确认装袋先同步冲刷当前状态键的待处理扫描，再按最新状态确认；成功重置、模式切换和路由离开均取消 timer，轮次令牌阻断迟到回调。
- 菲票命中后只清空扫码输入并刷新已扫结果区域，不重新扫描整页图标。
- 「确认装袋」「确认入仓」只重绘带 `data-pda-cutting-inbound-workflow` 的当前工作区；成功清空、失败保留均不重绘应用根节点。
- 中转袋交出复用状态键、轮次令牌和确认前 `flush` 模式；成功后取消等待并重置当前轮。字段扫描只同步当前字段与 live 区，确认 / 主动清空仍可重建整个专用工作区，避免迟到袋扫码清除操作员已输入的任务码。
- 装袋 / 入仓与整袋交出工作区都缓存页面渲染时已解析的执行单编号；扫码与确认热路径直接复用工作区上下文，不再同步重建裁床执行上下文。裁床扫码输入、Enter 与三个确认动作均由已加载 handler 同步接收，避免冷启动动态模块加载或低性能设备下的重复计算。
- 聚焦检查执行统一路由离开 helper，并通过 PDA 顶部待办间接调用真实 `appStore.navigate`、浏览器同步调用 `syncFromBrowser`，实际捕获 `higood:pda-cutting-inbound-leave` 与 `higood:pda-cutting-handover-leave` 各一次；同 URL 不重复派发。全局 Enter 也通过可执行 helper 验证两个交出扫码框会派发、非 Enter 与特殊工艺字段不派发。
- 聚焦检查以真实历史深链渲染整袋交出工作区，并执行旧确认命令回归，确认旧逐菲票事件写入口不可达。
- 快速检查 `check:pda-cutting-wait-handover-entry-routing` 只验证 `routes-pda` 根路由 / 动态路由注册、五入口固定深链、不可达旧链清理和五个 legacy 解析目标，不执行页面渲染；冷启动约 0.58 秒，保留在 `check:cutting:all`。
- 真实集成检查 `check:pda-cutting-wait-handover-route-integration` 在同步 `window.location` 和 PDA 登录态后，逐条通过 `resolvePage` 验证根页、五个新深链、五个 legacy 跳转及跳转目标可渲染；legacy 跳转使用有界条件等待，同时确认浏览器与应用路由到达目标。
- 真实集成检查冷启动约 19–20 秒（本轮独立实测 20.05 秒，此前实测 18.55 秒），未加入日常 `check:cutting:all`；已进入 `check:cutting:release` 发布就绪序列并固定先于 `check:cutting:all` 执行。
- 本轮 `check:cutting:release` 实测为真实路由集成先通过，随后 `check:cutting:all` 在既有 `src/pages/process-factory/cutting/production-order-overview-view.ts: min-w >= 1600px` 门禁失败。本记录不宣称 `check:cutting:all` 全绿，且本轮未修改或掩盖该既有基线。
- 失败结果通过本地状态保留袋码、菲票、数量、库位或已识别车缝任务及派生信息，不要求重新进入页面。
- 聚焦检查覆盖自动加入、快速连续扫码、整袋交出精确字段与唯一按钮、任务派生工厂、同袋单任务、成功清空、失败保留和 timer/确认竞态。
- 整袋交出聚焦检查还覆盖袋状态异常、任务不可接收、已有任务后跨生产单换袋阻断并保留原草稿、成功后本地状态更新及同袋重复扫描 / 重复确认。
- Web 聚焦检查覆盖三模式字段与按钮、装袋逐票紧凑明细、成功清空、失败保留、入仓无菲票、交出无工厂 / 数量 / 菲票输入、任务派生、重复交出和高频输入不整页重绘。
- Web 三操作交互仅修改弹窗工作区内的输入、紧凑明细、只读派生结果和反馈节点，不触发应用根节点整页重绘，底层工作台与当前 URL 均保持不变。
- `tests/cutting-transfer-bag-confirm-local-refresh.spec.ts` 在 1280×720 真实 Chromium 中以 `performance.now()` 与 `MutationObserver` 从 click 计到结果反馈 DOM 出现，覆盖装袋成功、袋码失败、菲票失败、入仓库位失败、快速连续扫码交出成功、交出袋码失败和任务失败。
- 快速连续扫码场景在同一事件循环中先派发袋码 Enter，再立即输入并派发车缝任务 Enter，不等待袋 live 结果；随后必须识别任务并成功确认交出。
- 装袋缺袋、入仓错库位，以及交出成功 / 缺袋 / 缺任务五条关键确认路径均加入 4× CPU 限速和点击后 220 ms 同线程竞争；竞争发生在确认事件让出线程之后，可直接暴露异步热路径，未放宽 `< 200 ms` 断言。
- 使用 `--repeat-each=3 --workers=1` 完整复跑七场景共 21 项，21 项全部通过；最终实测 0.9–13.0 ms，全部低于 200 ms。
- 七个真实浏览器场景均在非零 `window.scrollY` 与非零工作区 `scrollTop` 下执行，断言替换后的 `document.activeElement` 精确落到袋码、菲票、库位或车缝任务输入框；同时验证 `#app` 首层外壳和 workflow 容器对象未替换、两级滚动位置不变。
- fake DOM 检查仅用于状态迁移、helper 返回值、聚焦目标选择和 main 回退契约，不作为真实 DOM 焦点或浏览器滚动的证明；真实焦点与滚动结论只来自上述 Playwright 用例。
- PDA 聚合分发把页面 handler 的 truthy 联合结果归一化为 boolean，保持原 `dispatchPdaPageEvent` 对输入、旧 action 与特殊工艺调用方的布尔契约。

## 6. 自动门禁复核

- 修改前运行 `npm run check:prototype-design-governance -- --all` 返回通过，输出为 `no prototype changes`；当时工作树只有未跟踪计划文档，因此该结果不能替代对既有提交和审查记录语义真实性的复核。本轮未修改治理脚本。
- `npm run check:pda-cutting-inbound-workflow`：通过。
- `npm run check:pda-cutting-wait-handover-entry-routing`：通过，快速契约不执行页面渲染。
- `npm run check:pda-cutting-transfer-bag-handover`：通过。
- `npm run check:web-cutting-transfer-bag-actions`：通过。
- `npx playwright test tests/cutting-transfer-bag-confirm-local-refresh.spec.ts --repeat-each=3 --workers=1`：21 / 21 通过，包含不等待 live 结果的袋码与任务快速连续 Enter 场景，以及装袋、入仓、交出成功 / 失败的 4× CPU 压力。
- `npm run check:special-craft-pda-warehouse-actions`：未通过；在进入 PDA 分发断言前即因既有动作 `SPECIAL_CRAFT_GARMENT_WAREHOUSE_OUTBOUND` 未注册而报错。本轮未修改特殊工艺交出 / 回仓或 process action 注册，不扩大范围修复该既有门禁。
- `npm run check:pda-cutting-wait-handover-route-integration`：通过，实测 20.05 秒。
- `npm run check:list-page-governance`、`npm run check:prototype-design-governance -- --all`、`npm run build`：通过。
- `npm run check:cutting:release`：未通过；真实路由集成通过后，日常聚合被未改动的 `production-order-overview-view.ts: min-w >= 1600px` 既有基线阻断。
- 自动化范围内无未关闭的产品问题标签；任务 5 的确认性能阻塞已关闭。

## 7. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 原页面字段、状态、历史和说明过多 | `字段过载`、`视觉干扰` | 裁床仓务操作员 | 任务 1 收口为五入口，任务 2 收口为两种三步执行页 | 否 |
| 菲票扫码后还需点击「加入菲票」或等待提交才知道扫错 | `点错风险` | 裁床仓务操作员 | Enter 或 150 ms 停顿完成扫码后统一校验；成功自动加入，失败即时提示并清空输入；确认、重置和离开页面均收口 pending timer | 否 |
| 同一袋可能混入不同生产单菲票 | `选不对`、`点错风险` | 裁床仓务操作员 | 首张票锁定当前袋生产单，跨生产单扫码立即拒绝并保留已扫数据 | 否 |
| 入仓模式混入菲票扫描和列表 | `选不对`、`字段过载` | 裁床仓务操作员 | 入仓页仅保留袋码、库区库位和确认入仓 | 否 |
| 旧交出装袋确认要求扫描单张菲票并拆分来源袋、目标袋 | `选不对`、`字段过载`、`协作断裂` | 裁床仓务操作员 | 新交出模式隔离为一个袋加一个车缝任务，整袋一次确认交出 | 否 |
| 收藏或外部链接仍可能直接进入旧 `handover-bagging-confirm` 写入口 | `点错风险`、`协作断裂` | 裁床仓务操作员 | 入口将旧 action 规范化为整袋交出，并在 handler 阻断该模式下的旧逐菲票确认命令 | 否 |
| PDA 顶部待办或退出通过 store 导航时可能遗留入仓扫码 timer | `点错风险`、`组件误用` | 裁床仓务操作员 | `appStore.navigate/syncFromBrowser` 统一派发入仓与交出 leave；移除 main 重复派发并保持同 URL 不清理 | 否 |
| 车缝接收工厂可能被现场人员另选，或确认前无法纠正扫错任务 | `选不对`、`点错风险` | 裁床仓务操作员 | 工厂由车缝任务只读带出；确认前同生产单可接收任务可覆盖草稿，清空任务保留袋并清除任务派生信息，清空袋则重置整轮；确认后才形成绑定并阻断换绑 | 否 |
| 非待交出袋、不可接收任务或跨生产单仍可能被确认 | `选不对`、`点错风险` | 裁床仓务操作员 | 本页 Mock 增加袋/任务生产单与可操作状态，扫码和确认双重阻断并保留有效数据 | 否 |
| 成功后同袋可能重复交出 | `点错风险` | 裁床仓务操作员 | 成功先更新本页袋为「已交出」并记录任务，后续扫描和重复确认均阻断 | 否 |
| 提交失败可能丢失已扫数据 | `点错风险` | 裁床仓务操作员 | 失败保留当前袋码、菲票、数量、库位或已识别任务及派生信息 | 否 |
| Web 原操作混入扫码措辞、菲票候选、操作人 / 备注和旧交出拆袋结构 | `字段过载`、`选不对`、`视觉干扰` | 裁床仓管主管 / 文员 | 三个顶部动作使用独立短表单弹窗，交出任务自动派生生产单和接收工厂；原工作台查询能力继续保留 | 否 |
| Web 三操作被错误改成独立页面，导致待交出仓原库存、页签和回仓能力不可见 | `协作断裂`、`组件误用` | 裁床仓管主管 / 文员 | 路由恢复 `warehouse-hub` 工作台；三个按钮在当前页面打开弹窗，并增加真实浏览器回归门禁 | 否 |
| 弹窗内业务按钮局部刷新后仍触发页面级重绘，导致弹窗被移除 | `点错风险`、`组件误用` | 裁床仓管主管 / 文员 | 所有弹窗业务按钮显式跳过页面级重绘；真实浏览器逐项提交并断言弹窗持续存在及成功反馈可见 | 否 |
| Web 原操作成功后关闭操作层并整页刷新，失败依赖系统 alert | `点错风险`、`组件误用` | 裁床仓管主管 / 文员 | 成功与失败均在弹窗内短反馈；成功局部清空，失败保留输入 | 否 |
| PDA 确认后全页重绘导致装袋反馈 346.7 ms，且 `innerHTML` 替换后焦点丢失 | `组件误用` | 裁床仓务操作员 | 专用确认改为当前 workflow 局部刷新；仅 `handled-locally` 跳过整页渲染；替换后按结果聚焦新输入框并使用 `preventScroll` | 否 |
| main 只按按钮 action 跳过整页渲染，工作区标识缺失时可能无反馈 | `组件误用` | 裁床仓务操作员 | update helper 返回精确布尔值；局部失败时 handler 返回普通 `true`，main 继续执行原整页渲染回退 | 否 |
| 袋扫码异步完成时统一回写两个输入框，可能清除刚输入但尚未处理的任务码 | `点错风险`、`组件误用` | 裁床仓务操作员 | 字段扫描完成只同步当前字段和 live 派生区；主动清空 / 确认保留全量重建；真实浏览器连续 Enter 连跑 3 次 | 否 |

## 8. 最终结论

结论：通过

说明：

- 任务 1 至任务 5 已通过角色、任务、Web / PDA 差异、信息负荷、文案、UI、防错、失败恢复、局部更新路径和真实浏览器交互性能复核。
- 袋 / 菲票 / 库位状态防错、仓管导航、纯五入口、确认前车缝任务改扫、手动清空任务 / 袋以及确认成功后清空，均有聚焦自动检查覆盖；真实路由集成约 19–20 秒，留在 release 门禁而不进入日常 `check:cutting:all`。
- `check:cutting:all` 仍被未改动的 `production-order-overview-view.ts: min-w >= 1600px` 既有基线阻断，不得写成全绿。
- 任务 5 第二轮对抗先后发现 346.7 ms 整页重绘、确认前动态加载、交出同步重建上下文及快速连续扫码输入竞态，均已修复；七个成功 / 失败与焦点恢复场景重复 3 轮共 21 项，最终为 0.9–13.0 ms，均满足 `< 200 ms`。
- 当前按快速原型范围仅使用本地 Mock，不实现事件账、真实持久化、API 或跨模块一致性；这是明确实施边界，不构成产品设计例外。
- Web 待交出仓以原工作台作为稳定页面主体，三个中转袋动作使用聚焦弹窗；独立页面替换工作台的方案已撤销，并由 Playwright 逐按钮防回归。
- 本轮代码质量清理不改变页面结构、文案、动作入口、扫码步骤或 Mock 业务结果，无新增设计例外。
- 特殊工艺交出 / 回仓不是本轮焦点与局部刷新修复范围；保留原渲染路径。既有 `check:special-craft-pda-warehouse-actions` 当前被未注册的 `SPECIAL_CRAFT_GARMENT_WAREHOUSE_OUTBOUND` 阻断，失败早于本轮涉及的 PDA 聚合分发断言，因此不把它写成已通过或由本轮修复。
- 前一性能修复提交正文含字面量 `\n\n`，仅属于提交说明格式瑕疵；本轮按追加提交要求不改写历史，对代码、运行时和审查结论无影响。
