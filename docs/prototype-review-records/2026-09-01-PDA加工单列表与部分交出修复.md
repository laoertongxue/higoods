# PDA 加工单列表与部分交出修复原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-09-01 |
| 相关需求 / 任务 | PDA 删除 Web 分页和全局悬浮搜索；执行页严格只保留“加工中／已完成”；确认接收和发起交出归入交接；扫加工单只定位，接收和交出改为逐张扫描面辅料标签、裁片／捆条菲票或加工后标签；扫码为默认主入口，手动输入为折叠兜底 |
| 记录模式 | 完整产品审查 |
| 涉及系统 | FCS |
| 涉及页面路径 | `/fcs/pda/exec`、`/fcs/pda/handover`、`/fcs/pda/handover/:handoverId`、`/fcs/pda/exec/SPECIAL_CRAFT/:workOrderId?surface=handover&handoverAction=receive|handout`、`/fcs/pda/exec/BINDING_PROCESS_ORDER/:workOrderId?surface=handover&handoverAction=receive|handout` |
| 端类型 | 员工执行端 |
| 主要角色与任务 | 辅助工艺、特殊工艺及捆条加工工厂操作员先扫码定位具体加工单，再逐张扫描本批实际接收／交出的标签或菲票；必要时手动输入完整码，或修改单张标签的部分数量 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：PDA 执行列表删除桌面分页器和全局悬浮查询；执行页仅保留加工状态。交接先通过加工单码定位，进入具体加工单后第二级扫码框默认聚焦：面辅料接收扫标签，裁片和盘扣投入接收扫菲票，裁片交出扫加工后菲票，其他产出交出扫加工后标签。手动输入收进“无法扫码？”折叠区并复用同一校验；旧通用整单接收／交出卡片和直达提交路径被过滤或硬阻断。

审查基线：

- `AGENTS.md` 第 4 节：员工端少读、少选、少填、扫码优先和数量带单位。
- `AGENTS.md` 第 5 节：PDA 小屏、真实图片和局部交互门禁。
- `AGENTS.md` 第 7 节：最后一次实质修改后的当前工作树验证。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 执行和交接均以具体加工单或交接对象为核心；任务不再占据 PDA 列表首屏。执行页与交接页统一为“状态标签 → 扫码 → 卡片”，但状态职责严格分开：执行仅“加工中／已完成”，交接负责“待接收／待交出／已完成”。 |
| 文案、状态、数量与单位 | 通过 | 接收和交出不再暴露自由整单数量框；每张已扫标签显示对象、输入方式、本次数量和单位，允许在标签可用量内做部分数量修改。 |
| 扫码、真实图片与对象识别 | 通过 | 第一级加工单码只定位，第二级逐张扫描真实交接对象；扫码框默认聚焦，手动输入默认折叠。原型可用码属于 Mock 测试数据，不表述为真实现场标签。 |
| 防错、危险确认与主管兜底 | 通过 | 空批次、错加工单、模糊／歧义、同批重复、单位不一致、零数、负数和超标签可用量均阻断；手工输入不能绕过。 |
| 交接、跨端事实与异常追溯 | 通过 | 每批写入具体 `workOrderId`，并保存扫码批次 ID、标签／菲票码、数量单位、输入方式、操作人、时间和实际业务记录 ID；旧通用入口在列表、详情和提交处理器三处收口。 |
| 低分辨率、PDA、弱网与上传恢复 | 有条件通过 | 当前工作树已在 360×640 浏览器尺寸核查列表、数量输入和确认按钮，无全局搜索遮挡；本轮没有现实 iData、软键盘和弱网证据。 |
| 命名路由、交互、图片大图与打印 | 本地双轮通过，现场阻塞 | 已覆盖辅助／特殊工艺和捆条的接收、交出扫码页及执行／交接职责边界；最后一次业务源码修改后的两轮全量结果见本记录第 7 节。本次不修改打印，现实 PDA 和实体打印扫码不可由浏览器证据替代。 |

## 4. 问题标签

- `视觉干扰`
- `组件误用`
- `字段过载`
- `协作断裂`
- `追溯不足`
- `对象串写`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| PDA 复用桌面分页器，占据小屏且空列表仍显示页码 | `组件误用`、`字段过载` | 现场操作员 | 删除 PDA 桌面分页模块；首批显示 10 条，存在剩余时才显示全宽“继续显示 N 条” | 否 |
| 全局悬浮“查生产”遮挡卡片和交出信息 | `视觉干扰` | 现场操作员 | PDA 路由不再挂载全局悬浮入口；保留当前模块内的扫码识别能力 | 否 |
| 确认接收把整单剩余量直接确认为已收，没有逐扫面辅料标签或裁片菲票 | `协作断裂`、`追溯不足`、`点错风险` | 加工工厂操作员、发料方 | 加工单码仅用于定位；接收动作内面辅料逐扫标签、裁片／盘扣投入逐扫菲票，空批次不能确认 | 否 |
| 发起交出按整单或自由数量提交，没有证明本批实际交出的实物 | `协作断裂`、`追溯不足`、`点错风险` | 加工工厂操作员、收货方 | 裁片逐扫加工后菲票，其他产出逐扫加工后标签；允许单张部分数量，提交批次绑定实际业务记录 ID | 否 |
| PDA 扫码与手工录入权重相同，手工路径可能绕过扫码防错 | `组件误用`、`点错风险` | 现场操作员 | 扫码框自动聚焦并作为唯一首屏输入；手动输入折叠为兜底，仍走精确码、错单、重复、单位和数量校验 | 否 |
| 旧通用交接卡片和直达详情仍可整单提交辅助／特殊工艺 | `协作断裂`、`对象串写` | 现场操作员、仓管 | 列表过滤旧特殊工艺整单卡；旧详情显示阻断提示，提交处理器再次拒绝，防止深链绕过 | 否 |
| 执行页在扫码区下方重复显示“工艺加工单／任务仅作来源追溯” | `字段过载` | 现场操作员 | 删除说明行；状态标签固定在扫码区上方，扫码区下方直接展示当前状态的加工单 | 否 |
| 执行页仍显示“待接收／待交出”标签和“确认接收／发起交出”动作 | `角色错位`、`组件误用` | 加工工厂操作员 | 辅助工艺、特殊工艺及捆条加工单的执行列表只保留“加工中／已完成”；扫描接收或交出阶段的加工单时不进入执行详情，明确提示前往“交接-待接收／待交出”；扫码值与列表筛选状态隔离，顶部数量不被清零 | 否 |
| 已取消捆条加工单被默认归入“加工中” | `状态误导`、`点错风险` | 裁床／捆条操作员 | 已取消加工单从执行列表和执行扫码候选中排除，不允许再出现“加工填报”动作 | 否 |
| 交接列表与详情暴露任务 ID、来源类型、交接范围、二维码和长内部记录号 | `字段过载`、`点错风险` | 现场操作员 | 列表收口为对象、去向、三项数量和一个当前动作；详情收口为摘要、当前动作、完成按钮和默认折叠的历史记录 | 否 |
| 数量已收齐但列表按钮仍显示“确认接收” | `状态误导` | 现场操作员 | 读取接收／交出完成门禁，满足时显示“完成接收单／完成交出单” | 否 |
| PDA 数量差异按钮跳转管理端异常页 | `角色错位` | 现场操作员、主管 | PDA 只显示等待主管处理；管理端处理后结果自动回写 PDA | 否 |
| 待交出仓 540 片在尚未操作时被显示为交出记录 | `追溯不足` | 操作员、仓管、管理人员 | 只有累计实际交出大于 0 或进入正式交出／实收状态才创建交出记录 | 否 |
| 现实 iData 的软键盘、扫码枪、反光和弱网尚未复测 | `点错风险` | 现场操作员 | 保留为现场验收门禁，不以浏览器截图冒充实机通过 | 是 |

## 6. 最终结论

结论：有条件通过。本地实现与双轮验证完成；现实 iData 和实体打印扫码仍阻塞，不能宣布“全部测试通过”。

说明：

- 当前工作树已实现两级扫码：先定位加工单，再逐张扫描本批真实交接对象。扫加工单本身不会产生接收或交出记录。
- 面辅料标签、裁片／捆条菲票和加工后标签均支持分批；单张可在其可用量内修改部分数量，合计即时更新。
- 扫码批次保存输入方式和实际业务记录 ID；手动输入只作折叠兜底，并与扫码使用同一门禁。
- 相关候选标签码和业务记录均为当前原型 Mock／隔离浏览器事实，不冒充生产业务记录。
- 本轮未在现实 iData 操作，不能据此宣布现实设备验收或原全链 2,592 条现场证据已经完成。
- 本次逐标签／逐菲票改造后的 360×640 页面证据已在最后一次业务源码修改后重新生成；它们仍只代表浏览器小屏，不代表现实扫码枪、软键盘、反光和弱网验收。

## 7. 变更覆盖与验证

### 受管文件

- `src/components/production-object-floating-entry.ts`
- `src/components/shell.ts`
- `src/data/fcs/process-warehouse-domain.ts`
- `src/data/fcs/process-action-writeback-service.ts`
- `src/data/fcs/pda-process-physical-scan.ts`（新增）
- `src/pages/pda-exec-detail.ts`
- `src/pages/pda-exec-progressive-list.ts`
- `src/pages/pda-exec.ts`
- `src/pages/pda-exec-pagination.ts`（删除）
- `src/pages/pda-handover.ts`
- `src/pages/pda-handover-detail.ts`

### 页面路由

- `/fcs/pda/exec?tab=IN_PROGRESS`
- `/fcs/pda/handover?tab=pickup`
- `/fcs/pda/handover?tab=handout`
- `/fcs/pda/handover/PKH-MOCK-SEW-400`
- `/fcs/pda/exec/SPECIAL_CRAFT/AUX-TASK-PO202603083-TING-02-6b1424b3?surface=handover&handoverAction=handout`
- `/fcs/pda/exec/BINDING_PROCESS_ORDER/:workOrderId?surface=handover&handoverAction=receive|handout`

### 验证命令

- `npm run check:aux-special-accessory:double`：通过；两轮均为每轮 1,390 项领域检查、181/181 个浏览器场景；每轮聚合 167 张加工单、167 张 Web 截图、162 张 360×640 PDA 截图，失败 0。
- `npm run check:pda-process-physical-scan`：通过，覆盖加工单／动作隔离、精确码、错单、同批重复、单位、数量和业务记录绑定。
- `npm run check:special-craft-web-mobile-action-dialog-and-layout`、`npm run check:special-craft-pda-single-fact`、`npm run check:button-loop-auxiliary-crafts`：通过。
- PDA 实物扫码专项首次完整运行 17/17 通过。额外独立重跑时出现过 Vite 冷启动后的 10 秒页面元素等待超时；失败截图显示目标页面随后已渲染，三个原始超时场景分别原样重放为 2/2 和 1/1 通过。该重试不替代上方两轮全量绿灯，也不记作现实设备证据。
- `npm run build`：通过，Vite 生产构建完成（2,372 个模块）。
- `npm run check:prototype-design-governance -- --all`：通过；覆盖 6 个用户可见文件、0 个纯技术文件和 1 份关联审查记录。

### 页面证据

- `output/playwright/pda-density-fix/exec-final-360x640.png`：APF 执行页，状态标签在上、扫码在下，无“工艺加工单／任务仅作来源追溯”、分页器和全局搜索。
- `output/playwright/pda-density-fix/handover-final-360x640.png`：APF 待接收列表，只显示对象、去向、应收／已收／待收和当前动作。
- `output/playwright/pda-density-fix/pickup-detail-final-360x640.png`：确认接收详情，只显示摘要、当前接收动作、完成按钮和折叠历史。
- `output/playwright/pda-density-fix/handout-created-final-360x640.png`：扫码并填写 1 片后生成部分交出记录，详情显示计划／已确认／待确认和折叠交出历史。
- `output/playwright/pda-density-fix/binding-receive-physical-scan-360x640.png`：捆条加工单接收逐标签扫码，扫码为主、手动输入折叠。
- `output/playwright/pda-density-fix/binding-handout-physical-scan-360x640.png`：捆条加工单交出逐加工后标签扫码。
- `output/playwright/pda-density-fix/special-craft-handout-physical-scan-360x640.png`：裁片加工单逐菲票部分交出，数量即时校验。
- `output/playwright/pda-density-fix/accessory-receive-scan-default-360x640.png`：辅料加工单逐标签接收，默认焦点位于扫码框。

### 真实图片验证

- APF 加工单卡片继续展示与 `SPU-DRESS-083` 对应的冻结款式图，360×640 截图中加载成功；本次没有修改图片源、失败态或大图交互。

### 例外

- 当前改动位于独立发布工作树；任务收据、CodeGraph 状态和最终差异检查在最后一次文档更新后单独记录，不能由历史回执替代。
- 该独立工作树未初始化 `.codegraph/`，`codegraph_status(projectPath=...)` 明确返回“not initialized”；未经用户同意不在工作树新建索引，因此不能宣称本次 CodeGraph 同步门禁通过。
- `npm run workflow:verify -- --output /private/tmp/higoods-pda-physical-scan-task-receipt.json --task-boundary ...`：未生成有效收据；验证器因该工作树缺少 CodeGraph `pendingChanges` 状态退出 1。业务专项和构建结果不受影响，但交付状态不能标记为 `verified`。
- 现实 iData 仍需按原现场证据门禁执行；本记录只确认当前工作树的代码和浏览器小屏结果。

## 8. 执行／交接职责边界追加验收

### 范围

- 适用：PDA 已实现的辅助工艺、特殊工艺和捆条加工单列表。
- 执行页：仅展示“加工中／已完成”，当前动作仅为加工填报、完成加工单或查看加工记录。
- 交接页：统一承接确认接收和发起交出。
- 不扩展：花边辅料加工单本阶段仍为 Web 链；毛织、KOL 整单、印花和染色的独立执行规则不在本次修改范围。

### 当前版本直接证据

- 360×640 打开 `/fcs/pda/exec`：顶部只有“加工中(0)”和“已完成(27)”，不存在“待接收／待交出”标签。
- 扫描 `AUX-PO14672-0001-01`：URL 保持 `/fcs/pda/exec`，提示“该加工单待接收，请到‘交接-待接收’处理”，不显示加工单候选或确认接收按钮；“已完成(27)”计数保持不变。
- 扫描 `AUX-PO14672-0002-02`：URL 保持 `/fcs/pda/exec`，提示“该加工单待交出，请到‘交接-待交出’处理”，不显示加工单候选或发起交出按钮；“已完成(27)”计数保持不变。
- 点击“已完成(27)”：卡片主动作均为“查看加工记录”，没有“确认接收”或“发起交出”。
- 打开 `/fcs/pda/handover`：顶部为“待接收／待交出／已完成”，待接收卡片提供“确认接收”，证明动作没有被删除，而是集中到交接模块。
- F090 捆条加工单在 360×640 下同样只有“加工中／已完成”；两张已取消单不再计入“加工中”，扫描 `BT-260309-082-01-14` 和 `BT-260302-004-01-01` 分别提示前往“交接-待接收”和“交接-待交出”。
- 浏览器控制台：0 Error、0 Warning。

### 追加页面证据

- `output/playwright/pda-exec-stage-boundary/exec-two-tabs-360x640.png`：执行页只有两个执行状态标签。
- `output/playwright/pda-exec-stage-boundary/scan-waiting-receipt-360x640.png`：待接收单扫码引导到交接。
- `output/playwright/pda-exec-stage-boundary/scan-waiting-handout-360x640.png`：待交出单扫码引导到交接。
- `output/playwright/pda-exec-stage-boundary/completed-records-only-360x640.png`：已完成卡片仅提供查看加工记录。
- `output/playwright/pda-exec-stage-boundary/handover-owns-receipt-and-handout-360x640.png`：交接页保留接收和交出状态及动作。
- `output/playwright/pda-exec-stage-boundary/binding-exec-two-tabs-360x640.png`：F090 捆条执行区只有“加工中／已完成”，已取消单未显示。
- `output/playwright/pda-exec-stage-boundary/binding-scan-waiting-receipt-360x640.png`：待接收捆条单引导到交接。
- `output/playwright/pda-exec-stage-boundary/binding-scan-waiting-handout-360x640.png`：待交出捆条单引导到交接。

### 追加验收结论

- 浏览器小屏：通过。
- 代码边界：通过，执行页不会把接收或交出加工单路由进执行详情。
- 现实 iData：未执行，仍不得表述为现场设备通过。
