# 列表治理与印花平台局部分页原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-08-11 |
| 相关需求 / 任务 | 修复 WLS 面料需求看板未迁移标准列表，以及印花平台切换每页 20 条触发整页重绘 |
| 记录模式 | 完整产品审查 |
| 涉及系统 | WLS、FCS |
| 涉及页面路径 | `/wls/fabric-demand-board`、`/fcs/process/print-orders` |
| 端类型 | 管理端 |
| 主要角色与任务 | 仓储、计划和工艺管理人员查看面料需求；平台管理人员筛选和翻页查看印花加工单 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：面料需求看板由手写表格迁移为标准列表，新增标准分页、三态排序、列显示／顺序／冻结设置和局部交互；原有筛选、统计、目标面料、原料、多仓库存、印花、染色、采购和异常预警内容保持。印花平台页仅修正分页控件事件标记，切换每页条数后列表局部更新，不再替换整页 DOM，业务字段和数据不变。

当前审查依据：

- `AGENTS.md` 第 4 节：印尼工厂现场产品设计基线。
- `AGENTS.md` 第 5 节：UI、列表和真实图片专项门禁。
- `AGENTS.md` 第 7 节：分层验证和证据新鲜度。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 两个页面均为管理端标准数据列表；WLS 页面已声明并使用标准列表模式。 |
| 文案、状态、数量与单位 | 通过 | WLS 原有筛选、8 项统计、7 类业务列、六类异常及 Yard／卷口径由专项契约逐项校验；印花平台业务字段未变。 |
| 扫码、真实图片与对象识别 | 通过 | WLS 目标面料名称、SPU、SKU 与其既有实拍类 Mock 物料图仍在同一列；缩略图增加加载失败态和高清大图弹窗。扫码不适用于本次管理看板。 |
| 防错、危险确认与主管兜底 | 通过 | 异常预警列设为必需列，不能被列设置隐藏；本次不涉及危险写操作。 |
| 交接、跨端事实与异常追溯 | 通过 | 多仓库存、印花／染色待接收与待入库、责任人和解除条件保持可见，未改变跨端事实。 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 1366×768 下页面主体无横向溢出，宽表只在表格容器内滚动；PDA、上传和弱网不适用于本次管理列表。 |
| 命名路由、交互、图片大图与打印 | 通过 | 两个命名路由均完成浏览器验收；分页、筛选、排序、列设置均保持页面根节点稳定，图片大图支持按钮、遮罩及 Esc 关闭；打印不适用于本次变更。 |

## 4. 问题标签

- `组件误用`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 面料需求看板仍使用手写表格，无法满足全仓列表治理 | 组件误用 | 仓储、计划、工艺管理人员 | 迁移至标准列表页、共享列表控制器、标准表格和标准分页，并移除对应历史基线项 | 否 |
| 印花平台页切换每页条数后目标控件被局部替换，外层事件继续触发整页渲染 | 组件误用 | 平台管理人员 | 启用列表控制器的局部事件标记，让分页、排序和列设置动作在事件分发前明确跳过整页重绘 | 否 |

## 6. 最终结论

结论：通过

说明：两个点名缺陷均有直接自动化和命名页面证据；WLS 原有业务信息没有因标准列表迁移而减少。

## 7. 变更覆盖与验证

### 受管文件

- `src/pages/wls-fabric-demand-board.ts`
- `src/pages/process-print-orders.ts`

### 页面路由

- `/wls/fabric-demand-board`
- `/fcs/process/print-orders`

### 验证命令

- `npm run check:list-page-governance:static`：通过，扫描 355 个页面，历史基线剩余 18 项。
- `node --import tsx scripts/check-wls-fabric-demand-board.ts`：通过。
- `node --import tsx scripts/check-platform-process-order-events.ts PRINT`：通过，含平台印花页和工厂印花页回归。
- `PLAYWRIGHT_REUSE_EXISTING_SERVER=false CUTTING_E2E_PORT=4215 npx playwright test tests/wls-fabric-demand-board-standard-list.spec.ts --workers=1 --reporter=line`：通过，1 项。
- `npm run check:list-page-governance`：通过，含静态扫描、标准列表 Chromium 模板和全量原型治理。
- `npm run build`：通过。

### 真实图片验证

- 图片来源：WLS 面料需求 Mock 行既有 `materialImageUrl` 实拍类图片，不作为线上真实库存证明。
- 对象对应：缩略图与面料名称、SPU、SKU 在“面料信息”同一单元格；大图标题同时显示名称与 SKU。
- 缩略图与失败态：每行均显示缩略图；加载失败时显示“图片失败”。
- 大图弹窗：点击缩略图打开大图，支持关闭按钮、遮罩和 `Esc`；大图保持宽高比且不溢出 1366×768 视口。

### 例外

- 无。
