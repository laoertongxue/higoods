# 裁片单关联补料联动原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-07-22 |
| 相关需求 / 任务 | 裁片单查看、筛选并完成关联补料单；补料管理新增补料入口收敛 |
| 涉及系统 | FCS |
| 涉及页面路径 | 裁片单；补料管理 |
| 端类型 | Web 管理端 |
| 主要角色 | 裁床主管、生产管理人员 |
| 主要任务 | 按裁片单识别补料事实，查看或单选完成一张补料单，并在两个菜单页面共享完成结果 |

## 2. 参考规范

- `docs/higood-indonesia-factory-product-design-guidelines.md`
- `docs/higood-indonesia-factory-prototype-review-checklist.md`
- `docs/prototype-review-record-template.md`
- 项目标准列表页、局部刷新、中文化、低分辨率与交互响应门禁

## 3. 业务与交互结论

- 补料单挂在裁片单下，一张裁片单允许多次补料；每张补料单独立显示“第 N 次”标签，状态仅为“未完成 / 已完成”。
- 操作栏只允许 radio 单选一张未完成补料单，未选中前禁用确认；标签详情也可完成当前这一张。完成一张不连带完成其余补料单，也不改变裁片单主状态。
- 已完成补料单不再展示完成入口；完成前有确认层。重复完成、记录缺失均给出中文反馈；弹层支持 Escape、Tab 圈定和关闭后焦点恢复。行仍存在时回到当前行操作；“有未完成”筛选下末单完成导致行消失时回到“补料完成状态”筛选。
- 新增补料弹窗只选择裁片单；所属生产单作为只读上下文展示，不再提供“按生产单 / 按裁片单”双 Tab，也不保留说明区。
- “无补料”只匹配零张补料单，并排除“有未完成 / 全部已完成”等完成类筛选；`ALL_COMPLETED` 定义为至少有一张补料单且每张均已完成，混合状态不属于全部已完成。
- 两个菜单页面读取同一补料登记事实；首次访问顺序不影响稳定数据，同一 SPA 完成后跨页共享，冷启动幂等且不会生成第 4 次重复记录。
- 历史裁片单缺少可信数量时统一显示“未提供”，不把未知值伪装为真实 0，也不臆造任务边界。

## 4. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色匹配 | 通过 | 面向裁床主管和生产管理人员的 Web 管理端，列表密度与追溯信息符合角色。 |
| 任务清晰度 | 通过 | 每张标签明确次数和状态；操作栏一次只选一张，详情只作用于当前单。 |
| 信息架构与导航 | 通过 | 裁片单与补料管理位于两个业务菜单页面，但共享同一补料事实并支持同 SPA 往返。 |
| 页面模式 | 通过 | 两页均使用标准列表；详情、选择、确认采用短弹层；新增补料只保留裁片单选择。 |
| 信息负荷 | 通过 | 移除无业务作用说明区和双 Tab；列表显示摘要，详细字段进入详情。 |
| 文案 | 通过 | 页面状态、反馈、错误和未知历史值均为中文业务表达，无英文状态码。 |
| 数量与状态 | 通过 | 一单多次、每单独立状态；`ALL_COMPLETED` 与无补料边界明确；未知数量显示“未提供”。 |
| 扫码与识别 | 通过 | 本次为管理端单据关联与完成，不涉及现场扫码。 |
| 防错 | 通过 | 已完成无入口；单选前禁用确认；二次确认；重复/缺失反馈；不改变裁片单主状态。 |
| UI 样式 | 通过 | 复用企业后台标准列表、状态标签和弹层，1366×768、1280×720 下无页面或主体横向溢出。 |
| 组件交互 | 通过 | 操作列固定；宽表只在容器内横滚；分页、三态排序、列显示/顺序/冻结/每页条数按契约工作。 |
| 协作关系 | 通过 | 裁片单与补料管理共享同一完成事实，生产单只作为只读归属上下文。 |
| 异常与追溯 | 通过 | 记录缺失、重复完成、历史信息未知均有明确表达，不伪造来源或数量。 |
| 现场设备可用性 | 通过 | 两档验收分辨率路由可达、无空白/死链/明显遮挡，弹层、列设置、Escape 与焦点路径可操作。 |

## 5. 问题标签与处理

当前无未关闭问题标签。

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 两页分别维护稳定补料数据可能产生数量与状态偏差 | `算不准` | 裁床主管、生产管理人员 | 收敛为单一共享 fixture，统一幂等登记与单张完成规则 | 否 |
| 快速重复确认可能覆盖首次成功反馈 | `点错风险` | 裁床主管 | 使用单一事件通道；重复触发只完成目标单并保留首次成功结果 | 否 |
| 历史裁片单数量或任务边界被当成已知事实 | `追溯不足` | 生产管理人员 | 只投影有稳定补料事实的裁片单，未知数量和边界显示“未提供” | 否 |
| 行保留或因筛选消失后焦点丢失 | `点错风险` | 裁床主管 | 分别回到当前行操作或“补料完成状态”筛选 | 否 |

## 6. 自动检查与浏览器验收

| 命令 / 场景 | 真实结果 |
| --- | --- |
| `node --import tsx --test tests/supplement-order-registry.test.ts` | 15 项通过，0 失败 |
| `npx playwright test tests/supplement-management-list-template.spec.ts --workers=1` | 24 项通过，0 失败；详情完成实际 DOM 响应 124.0ms；1366×768、1280×720 均无页面横向溢出 |
| `npx playwright test tests/cut-order-supplement-linkage.spec.ts --workers=1` | 24 项通过，0 失败；详情完成 17.2ms，操作栏完成 16.0ms；两档分辨率均无页面/主体横向溢出，操作列固定可见，宽表仅容器横滚 |
| `npm run check:list-page-governance` | 通过；扫描 319 个页面、基线 29；标准列表模板检查通过 |
| `npm run check:prototype-design-governance` | 通过；staged 范围无原型变更 |
| `npm run check:prototype-design-governance -- --all` | 通过；all 范围无原型变更 |
| `npm run check:third-party-cutting-task-boundaries` | 通过 |
| `npm run build` | 通过；Vite 转换 2246 个模块并完成产物构建 |

浏览器验收还覆盖：两页路由可达；无空白页、死链、英文状态或明显遮挡；标签详情、单选完成、列设置、Escape、焦点恢复、筛选结果、跨页共享和冷启动幂等均与业务规则一致。操作栏完成从确认点击起由 `MutationObserver` 等待标签、所在行和统计区完成更新，低于 200ms；`main`、筛选区和分页区根对象保持，页面纵向滚动与表格横向滚动保持。分页内容因筛选模型变化时允许在稳定分页根内局部更新。

## 7. 最终结论

结论：通过

说明：

- 本次无业务例外；未扩展真实后端、权限或裁片单主状态机。
- 非阻断观察：首次生成历史投影沿用既有原型数据构建方式；本次完成补料后的交互均为局部刷新。
- 额外执行 `npm run check:cutting-clean-mainline` 时发现两个不在本次分支差异中的既有残留：`marker-spreading.ts` 的“铺布列表”旧口径、`production-order-overview-view.ts` 的 `min-w >= 1600px`。该全局检查退出 1；两文件自基线 `6d1a683e` 至本分支未发生变化，不作为本次补料联动业务例外，也未越界修改。
