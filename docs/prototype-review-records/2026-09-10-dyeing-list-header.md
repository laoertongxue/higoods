# 染色列表表头调整

## 1. 基本信息

- 日期：2026-09-10；完整产品审查；PFOS 管理端。
- 依据：用户本轮标注截图。需求编号 UI-001～003；范围为移除顶部整行、移动批量打印按钮。
- 版本：main / f6f3f945a46711265ad6f0eaf7086e9533134fcb 的本次工作区差异。
- 服务：/Users/laoer/Documents/higoods，Vite 0.0.0.0:5188；实际 PID 95759 的 cwd 已核对。

## 2. 影响判定

- 用户可见影响：有
- 判定依据：删除顶部重复标题与两个交出按钮，批量打印移入列表表头。共享列表组件增加显式关闭顶部标题行的选项，默认行为不变。
- 采用 AGENTS.md 第 4、5、7 节基线。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 仍为标准管理列表，工厂 Tab 位于内容顶部 |
| 文案、状态、数量与单位 | 通过 | 批量按钮仍显示选中数，0条禁用、2条启用 |
| 扫码、真实图片与对象识别 | 通过 | 图片及对象资料未改动；本次只移动入口 |
| 防错、危险确认与主管兜底 | 通过 | 原批量打印处理器保留，未选择不可点击 |
| 交接、跨端事实与异常追溯 | 通过 | 不修改接收、加工、交出规则或记录 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 1366×768 页面宽度1366；PDA、弱网、上传不适用布局调整 |
| 命名路由、交互、图片大图与打印 | 通过 | 点击两条批量打印进入 DWO-001,DWO-002 预览；本次未改打印模板 |

## 4. 问题标签

- 无

## 5. 主要问题与处理

- 按用户标注移除顶部整行。通过组件现有结构增加一个可选开关，其他调用默认继续显示标题；不复制列表模板、不用 CSS 隐藏旧操作。
- 本轮前的工作区修改均属于同一染色对齐任务，全部保留；收据工具要求绑定全部累计差异，不能按文件子集生成。本轮源码增量以 `/tmp/higoods-dye-header-before.ts`、`/tmp/higoods-list-page-before.ts` 与当前文件对比核对，仅为开关及按钮位置。

## 6. 最终结论

结论：通过。命名页面的本轮布局及按钮行为已实测；技术检查结果以本轮收据为准。此前系统打印分页待人工确认的范围未扩大为本轮已完成事实。

## 7. 变更覆盖与验证

### 受管文件

- `src/components/ui/list-page.ts`
- `src/pages/process-factory/dyeing/work-orders.ts`

### 页面路由

- `/fcs/craft/dyeing/work-orders`
- `/fcs/print/task-route-card?sourceType=DYEING_WORK_ORDER&sourceId=DWO-001%2CDWO-002`

### 验证命令

- `git diff --check`：通过；本轮增量仅两处页面配置和共享标题开关。
- `npm run workflow:verify`：失败（首次 --paths 子集被收据工具拒绝）；最终使用本任务累计范围重跑，结果见 [本轮收据](/tmp/higoods-dyeing-header-20260910/task-receipt.json) 和 [日志](/tmp/higoods-dyeing-header-verify.log)。

### 浏览器证据

- 当前服务、1366×768：`[data-standard-list-page] > header` 不存在；页面“待交出列表 / 交出单据”按钮数为0。
- 批量打印按钮仅1个，位于 `[data-standard-list-table-section] > header`，列设置左侧；截图保存在本任务工具输出。
- 初始0条禁用；勾选 DY-20260328-001 和 DY-20260328-002 后按钮文字为“批量打印流程卡（2）”且启用；点击打开包含对应 sourceId 的流程卡预览。

### 真实图片验证

- 图片资源与渲染逻辑没有变化，沿用前次审查的物料和商品对应关系。本轮列表截图缩略图正常。

### 例外

- 无本轮例外。本轮不对系统打印面板或真实出纸作新增验收声明。
