# PFOS 后道工厂菜单更名原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-08-08 |
| 相关需求 / 任务 | PFOS 后道一级菜单统一命名为“后道工厂管理” |
| 记录模式 | 完整产品审查 |
| 涉及系统 | PFOS |
| 涉及页面路径 | `/fcs/craft/workbench/overview`、`/fcs/craft/post-finishing/tasks` |
| 端类型 | 管理端 / 主管端 |
| 主要角色与任务 | 中央工厂管理人员、后道主管从 PFOS 侧栏识别并进入后道工厂业务页面 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：PFOS 侧栏分组标题和后道入口标题统一显示“后道工厂管理”；路由、子菜单、业务状态、数量、图片、Mock 数据和交互保持不变。

当前审查基线：

- `AGENTS.md` 第 4 节：印尼工厂现场产品设计基线。
- `AGENTS.md` 第 5 节：UI、列表和真实图片专项门禁。
- `AGENTS.md` 第 7 节：分层验证和证据新鲜度。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 更名后菜单名称与中央工厂体系中的后道工厂定位一致，不改变角色和任务入口。 |
| 文案、状态、数量与单位 | 通过 | 仅统一菜单名称；后道业务状态、数量和单位不变。 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 本次只改变桌面侧栏短文案，不影响 PDA、弱网或上传能力。 |
| 命名路由、交互、图片大图与打印 | 通过 | 当前分支在 1366×768 验收工作台与后道任务路由；侧栏新名称完整显示，原有页面与导航可用，控制台 0 错误、0 警告。本次不涉及图片和打印。 |

## 4. 问题标签

- 无

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 无 | 无 | 无 | 无 | 否 |

## 6. 最终结论

结论：通过

说明：

- 文件级替换、专项检查、全项目旧文案扫描、命名页面验收和全工作区治理检查均已通过。

## 7. 变更覆盖与验证

### 受管文件

- `src/data/app-shell-config.ts`

### 页面路由

- `/fcs/craft/workbench/overview`
- `/fcs/craft/post-finishing/tasks`

### 验证命令

- `npm run check:process-factory-tabs-and-post-finishing`：通过
- `rg -n --hidden -g '!.git' -g '!node_modules' '<替换前菜单名称>' .`：通过，零匹配
- `npm run check:prototype-design-governance -- --all`：通过
- 命名页面浏览器验收：通过；当前分支在 1366×768 验收 `/fcs/craft/workbench/overview`、`/fcs/craft/post-finishing/tasks`，侧栏新名称完整显示，原导航可用，控制台 0 错误、0 警告

### 例外

- 无
