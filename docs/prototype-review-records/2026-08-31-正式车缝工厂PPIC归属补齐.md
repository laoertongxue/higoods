# 正式车缝工厂 PPIC 归属补齐原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-08-31 |
| 相关需求 / 任务 | 正式三方车缝工厂不得缺少有效 PPIC；补齐当前原型档案并阻止后续保存空归属 |
| 记录模式 | 完整产品审查 |
| 涉及系统 | FCS |
| 涉及页面路径 | `/fcs/factories/profile` |
| 端类型 | 管理端 |
| 主要角色与任务 | PPIC 团队负责人、调度人员查看正式车缝工厂归属，并确保工厂分配后能确定唯一任务 PPIC |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：正式车缝工厂档案“跟进 PPIC”列由空值变为现有启用的原型 PPIC；新增或更新正式车缝工厂时，缺少有效 PPIC 将被阻断。页面结构、路由、图片、其他字段和交互不变。

本次审查依据：

- `AGENTS.md` 第 4 节：印尼工厂现场产品设计基线。
- `AGENTS.md` 第 5 节：UI、列表和真实图片专项门禁。
- `AGENTS.md` 第 7 节：分层验证和证据新鲜度。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 管理端工厂档案继续使用现有标准列表，仅补齐 PPIC 责任字段 |
| 文案、状态、数量与单位 | 通过 | “跟进 PPIC”显示明确人员名称，不再显示空值；没有新增数量口径 |
| 防错、危险确认与主管兜底 | 通过 | 正式车缝工厂写入时缺少 PPIC 会被阻断，旧样例读取时统一补齐 |
| 交接、跨端事实与异常追溯 | 通过 | 工厂档案继续作为选择工厂后确定任务 PPIC 的唯一主数据来源 |
| 命名路由、交互、图片大图与打印 | 通过 | 路由和交互未变；本次不涉及款式、物料图片或打印 |

## 4. 问题标签

- `协作断裂`
- `追溯不足`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 核查时原型 21 家正式车缝工厂样例全部缺少 PPIC，分配后无法可靠确定责任人 | 协作断裂、追溯不足 | PPIC 团队负责人、调度人员、PPIC | 对旧样例统一补入现有启用默认 PPIC；正式车缝工厂写入时增加 PPIC 必填门禁；增加专项检查 | 是，当前为模拟人员和工厂，真实归属仍需业务名单替换 |

## 6. 最终结论

结论：通过

说明：

- 原型层面消除正式车缝工厂空 PPIC，并建立后续写入门禁。
- 本次不虚构真实 20 多名 PPIC 与 600 多家工厂的归属；现有默认 PPIC 仅用于原型旧数据迁移，不能视为生产分工。

## 7. 变更覆盖与验证

### 受管文件

- `src/data/fcs/factory-master-store.ts`

### 页面路由

- `/fcs/factories/profile`

### 验证命令

- `node --experimental-strip-types --experimental-specifier-resolution=node scripts/check-factory-onboarding-step11-ppic-assignment.ts`：通过；正式车缝工厂 21 家，PPIC 缺失 0 家，并验证空 PPIC 写入会被阻断
- `/fcs/factories/profile` 页面验收：通过；当前工作树、1366×768、首屏成衣／车缝能力工厂 PPIC 非空，空值行均为不具备车缝能力的辅助工艺、印花、染色或裁床工厂；截图 `output/playwright/ppic-factory-profile.png`
- `npm run check:prototype-design-governance`：通过；默认模式未检测暂存文件
- 针对 `src/data/fcs/factory-master-store.ts` 与本记录直接运行 `validatePrototypeReviewCoverage`：通过；1 个用户可见文件由本记录覆盖
- `npm run build`：通过；2374 个模块完成转换，仅保留既有大分包提示
- `codegraph sync`：通过；索引已是最新
- CodeGraph 状态检查：通过；1563 个文件、47846 个节点、163195 条边，无待同步文件

### 例外

- 当前原型只有少量启用 PPIC 模拟选项，因此旧档案统一迁移到现有默认 PPIC。真实人员与工厂映射不在仓库中，本次不臆造；取得业务名单后必须整体替换，并保留后续任务责任移交规则。
