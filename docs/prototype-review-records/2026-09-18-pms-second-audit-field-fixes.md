# PMS 迁移第二轮复查即时修复（费用项对齐与关联采购单）原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-09-18 |
| 相关需求 / 任务 | 对 SRM 未覆盖页面做第二轮字段级复查后的即时修复：物流对账 7 项费用与 SRM 财务事实源对齐；物流对账详情补“关联采购单”只读表与“查看头程完整信息”跳转。缺口登记见 `docs/product-design/采购管理系统PMS迁移逐页功能验证清单.md` 第 15 节 |
| 记录模式 | 完整产品审查 |
| 涉及系统 | PMS |
| 涉及页面路径 | `/pms/logistics-reconciliations`、`/pms/first-leg-shipments` |
| 端类型 | 管理端（1366×768 验收，1280×720 兜底） |
| 主要角色与任务 | 财务：按头程运费/头程物流费/关税/增值税/清关费/附加费/报关费分项核对与确认；追溯该头程关联的面辅料采购单并跳转头程详情 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：物流对账的 7 项费用名称由“运费/报关费/清关费/仓储费/派送费/关税/其他”改为与 SRM 财务事实源一致的“头程运费/头程物流费/关税/增值税/清关费/附加费/报关费”，同步影响列表费用列、详情分项、导入模板与导出/预览；详情新增“关联采购单”表与“查看头程完整信息”按钮（跳转并定位头程单号）。

完整产品审查的当前基线：

- `AGENTS.md` 第 4 节：印尼工厂现场产品设计基线。
- `AGENTS.md` 第 5 节：UI、列表和真实图片专项门禁。
- `AGENTS.md` 第 7 节：分层验证和证据新鲜度。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色、任务与页面模式 | 通过 | 保持标准列表与详情抽屉模式，未引入新页面结构 |
| 文案、状态、数量与单位 | 通过 | 费用为中文业务名称并带币种；预计/实际/差异仍由系统重算 |
| 扫码、真实图片与对象识别 | 通过 | 关联采购单表展示物料名称与编码，不新增图片对象；既有图片能力未变 |
| 防错、危险确认与主管兜底 | 通过 | 分项确认门禁与账单金额校验未变；跳转按钮仅在详情内出现 |
| 交接、跨端事实与异常追溯 | 通过 | 关联采购单来自同一面辅料采购事实源，无第二套口径 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 1366×768 验收；关联表在容器内部滚动；无 PDA |
| 命名路由、交互、图片大图与打印 | 通过 | 跳转到命名路由 `/pms/first-leg-shipments` 并自动定位单号 |

## 4. 问题标签

- `算不准`（已处理：费用项与财务事实源不一致会误导预计/实际核对口径）
- `点错风险`（已处理：头程与采购单缺少可追溯入口）

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 物流对账费用项名称与 SRM `types/finance.ts` 不一致 | 算不准 | 财务 | 统一为头程运费、头程物流费、关税、增值税、清关费、附加费、报关费；模板/导入/校验/单测/e2e 同步 | 否 |
| 物流对账无法查看关联采购单、无法跳转头程详情 | 点错风险 | 财务 | 详情新增关联采购单只读表（物料/款式/数量/单价金额/供应商/状态）与“查看头程完整信息”跳转定位 | 否 |

## 6. 最终结论

结论：通过（第二轮复查即时修复批次）

说明：

- 两项即时修复已完成并补充断言；第二轮复查剩余字段级缺口（G1–G11）已登记在逐页核对清单第 15 节，留待用户确认批次后实施。

## 7. 变更覆盖与验证

### 受管文件

- `src/data/pms/reconciliations.ts`
- `src/pages/pms/logistics-reconciliations.ts`

### 页面路由

- `/pms/logistics-reconciliations`
- `/pms/first-leg-shipments`（跳转目标，未修改页面本体）

### 验证命令

- `npm run build`：通过
- `npm run check:pms-purchase-chain`：通过（分项确认、导入模板、生成请款等断言随新费用项通过）
- `npx tsx --test tests/unit/pms-settlement-flow.test.ts`：通过（6 项，含新费用键断言）
- `CUTTING_E2E_USE_PREVIEW=true npx playwright test tests/pms-*.spec.ts --workers=1`：通过（35 项，含物流对账导入新表头与分项确认）
- `npm run check:menu-routes`、`npm run check:list-page-governance`、`npm run check:prototype-design-governance`：通过
- `npm run workflow:verify -- --output /private/tmp/pms-second-audit-task-receipt.json --task-boundary "PMS 第二轮字段级复查即时修复（费用项对齐与关联采购单）"`：通过（收据见 `/private/tmp/pms-second-audit-task-receipt.json`）

### 性能证据（生产预览，Chromium，1366×768，2026-09-18）

- P3b 对账与请款用例复测原始样本：站内切换 22–41ms；物流对账（含新关联采购单渲染）27–39ms；面辅料对账 28–36ms；请款 28–36ms，全部 `<200ms`。
- 原始样本见测试输出 `pmsSettlementPerf.*`。

### 真实图片验证

- 本次不新增款式/物料对象；关联采购单表只展示名称与编码，物料图片能力保持既有实现。

### 例外

- 无
