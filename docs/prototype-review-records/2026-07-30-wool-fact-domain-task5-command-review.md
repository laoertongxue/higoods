# 毛织事实领域任务 5 命令原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-07-30 |
| 相关需求 / 任务 | 毛织事实工作流任务 5：可重试事实命令、原子库存与公共仓库位置校验 |
| 涉及系统 | PFOS / WLS |
| 涉及页面路径 | 毛织加工单、毛织仓库及下游接收的数据命令；本次不修改页面或路由 |
| 端类型 | 管理端 / 主管端 |
| 主要角色 | 毛织主管、毛织仓管、下游仓管、设备主管 |
| 主要任务 | 接收纱线、领用退回、加工填报、发起交出、下游确认、库存调整与转移、修改数量、确认完成 |

## 2. 参考规范

- `docs/higood-indonesia-factory-product-design-guidelines.md`
- `docs/higood-indonesia-factory-prototype-review-checklist.md`

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色匹配 | 通过 | 命令按毛织主管、仓管、下游仓管和设备主管的责任事实分开记录 |
| 任务清晰度 | 通过 | 接收、领退、填报、交出、下游接收、调整、转移、修改和完成均为独立动作 |
| 信息架构与导航 | 通过 | 本次只调整领域命令，不调整导航和页面入口 |
| 页面模式 | 通过 | 本次不调整页面模式 |
| 信息负荷 | 通过 | 未增加页面字段；公共库位身份由目标仓库与目标库位共同确定 |
| 文案 | 通过 | 校验错误、原因和操作记录均使用中文业务语义 |
| 数量与状态 | 通过 | 正数、整数、累计上限、库存余额、退回上限和修改差额均有命令级校验 |
| 扫码与识别 | 通过 | 本次不调整扫码；库存转移读取公共仓库位置主数据的稳定身份 |
| 防错 | 通过 | 停用仓库、区域、货架或库位均不可转入；同名库位按所属仓库消除歧义 |
| UI 样式 | 通过 | 本次不调整 UI |
| 组件交互 | 通过 | 本次不调整组件或交互渲染 |
| 协作关系 | 通过 | 交出与下游确认分离，确认差异不反向篡改毛织来源数量 |
| 异常与追溯 | 通过 | 命令 ID 可重试，原因、操作人、时间、目标仓库与目标库位进入事实或操作日志 |
| 现场设备可用性 | 通过 | 完成命令保留设备解除记录；本次不调整设备端交互 |

## 4. 问题标签

- `算不准`
- `选不对`
- `点错风险`
- `协作断裂`
- `追溯不足`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 重试可能重复新增事实或库存流水 | `算不准` | 毛织主管、毛织仓管 | 所有新增事实命令使用稳定 `commandId` 返回首次结果 | 否 |
| 业务事实与仓库流水分次写入会形成半成品记录 | `协作断裂` | 毛织主管、下游仓管 | 在同一次存储提交中原子写入业务事实、库存流水和下游投影 | 否 |
| 数量可越过计划、库存或已确认事实边界 | `算不准` | 毛织主管、毛织仓管 | 增加加工 150% 上限、库存余额、累计退回和下游确认锁定校验 | 否 |
| 毛织模块自建目标库位白名单会遗漏公共仓库新增库位 | `选不对` | 毛织仓管 | 每次转移实时读取公共仓库位置主数据，不维护毛织专属白名单 | 否 |
| 不同仓库复用相同库位编号可能误判停用状态 | `点错风险` | 毛织仓管 | 转移命令同时接收目标仓库 ID 与库位 ID，并逐级检查仓库、区域、货架和库位启用状态 | 否 |
| 库存调整、转移和数量修改缺少审计上下文 | `追溯不足` | 毛织仓管、毛织主管 | 强制填写原因并记录前后数量、目标位置、操作人和时间 | 否 |

## 6. 最终结论

结论：通过

说明：

- `commands.ts` 已覆盖可重试事实命令、数量防错、原子库存、下游锁定和人工完成。
- `queries.ts` 统一按数量修改链计算接收、填报、交出和库存有效数量。
- `scripts/check-wool-fact-workflow.ts` 覆盖重复命令、边界数量、原子写入、停用公共库位拒绝及运行时新增启用库位可用。
- 本次未修改页面、UI、组件、文案展示或路由，不存在页面治理例外。

## 7. 变更覆盖与验证

### 受管文件

- `src/data/fcs/wool-domain/commands.ts`
- `src/data/fcs/wool-domain/queries.ts`
- `src/data/fcs/wool-domain/types.ts`

### 检查脚本

- `scripts/check-wool-fact-workflow.ts`

### 页面路由

- 领域数据层更新，无新增或调整页面路由。

### 验证命令

- `npm run check:wool-fact-workflow`：通过。
- `npm run check:prototype-design-governance`：通过。
- `npm run check:prototype-design-governance -- --all`：通过。
- `git diff --check`：通过。
- `codegraph sync && codegraph status`：通过。

### 例外

- 无。
