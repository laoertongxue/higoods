# 单张染色加工单线上对齐原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-07-16 |
| 最近复核日期 | 2026-07-17 |
| 相关需求 / 任务 | 单张染色加工单与业务认可的线上页面基本一致 |
| 涉及系统 | PFOS / PDA |
| 涉及页面路径 | `/fcs/craft/dyeing/work-orders`；染色 PDA 执行页 |
| 端类型 | 管理端 / 主管端 / 员工执行端 |
| 主要角色 | 染厂主管、业务人员、染厂操作员 |
| 主要任务 | PFOS 查询和人工维护染色加工单；PDA 按现场动作推进同一状态 |

## 2. 参考规范

- `docs/higood-indonesia-factory-product-design-guidelines.md`
- `docs/higood-indonesia-factory-prototype-review-checklist.md`

## 3. 分批审查状态

已完成列表结构、查看/编辑/日志弹窗、中印双语流程卡、PDA 接单至交出、入库闭环、真实下载与批量打印、进行中改派的 Chromium 浏览器验收。任务 10 已补齐最终产品口径和验收缺口。

| 检查项 | 当前结论 | 说明 |
| --- | --- | --- |
| 角色匹配 | 通过 | PFOS 面向主管和业务人员，允许较高信息密度与人工改状态 |
| 信息架构与导航 | 通过 | 单张染色加工单列表按线上结构展示，不混入合并染色列、徽标或历史；合并染色仍是独立菜单和对象，执行与分配历史在该对象内永久保留 |
| 页面模式 | 通过 | 使用标准列表页、标准表格和分页组件 |
| 信息负荷 | 通过 | 线上业务字段较多，使用字段分组、列设置和表格内横向滚动承载；摘要卡在窄屏自动换行，但每张卡仍保持 48px 单行内容 |
| 文案 | 通过 | 页面字段与状态均使用中文业务词汇 |
| 数量与状态 | 通过 | 数量均展示单位，状态统一为七个线上业务状态 |
| UI 样式 | 通过 | 摘要卡保持 48px 单行布局，操作列固定右侧 |
| 组件交互 | 通过 | 筛选、分页、排序、列显示和冻结复用公共组件，局部刷新工作区 |
| 异常与追溯 | 通过 | 日志弹窗分页展示永久状态记录，高风险人工状态变更需要二次确认 |
| 现场设备可用性 | 通过 | PFOS 已在 1366×768、1280×720 验收，PDA 接单、开工、完工和交出已在 390×844 视口验收；入库闭环及改派后的新旧染厂权限均有浏览器证据 |

## 4. 当前风险与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 线上字段多、宽表信息密度高 | `字段过载` | 染厂主管、业务人员 | 按业务字段组展示，支持列显示、顺序、冻结和分页；摘要卡在窄屏按完整卡片换行 | 否 |
| PDA 与 PFOS 必须显示同一状态 | `协作断裂` | 染厂主管、染厂操作员 | PDA 接单保持“等待处理”，开工、完工、交出依次推进“染色中、染色完成、待审核”；累计实收不足进入“部分入库”，达到应收进入“已完成”。PDA 展示同一状态，但主按钮继续由实际工序节点决定 | 否 |
| PDA 染色任务此前被待接单范围排除，且接单只保存在页面临时状态 | `协作断裂`、`权限与状态冲突` | 染厂操作员、染厂主管 | 真实浏览器验收中恢复生产单来源染色任务的接单入口，并将接单结果写入统一 PDA 任务 Mock；印花任务仍按既有范围排除 | 否 |
| 从业务列表进入独立打印预览时仍保留后台壳 | `操作成本高`、`协作断裂` | 业务人员、染厂主管 | 全局导航识别普通后台壳与独立打印壳的切换边界，切换时执行完整壳渲染；后道打印回归同时同步到当前“扫码收货”口径和当前 Mock 单据 | 否 |
| 线上流程卡为中印双语，通用任务卡字段和版式无法直接承接 | `信息不足`、`操作成本高` | 染厂主管、印尼现场操作员 | 染色加工单使用独立“染整生产流程卡”模板，展示平台加工单号、生产单/备货来源、原料与颜色资料、二维码和八个现场签认工序；批量打印按勾选顺序逐张分页 | 否 |
| PFOS 将加工单改为“取消”后，PDA 仍可能执行已加载的旧动作 | `状态冲突`、`协作断裂` | 染厂主管、染厂操作员 | PFOS 与 PDA 共用动作预校验；页面按钮与写回服务双层阻断，且阻断发生在底层加工单、任务和日志变更之前 | 否 |
| 人工调整染厂只改线上展示，真实加工单与 PDA 任务归属可能分叉 | `协作断裂`、`权限与状态冲突` | 业务人员、染厂操作员 | PFOS 保存染厂时同步真实染色加工单和同 ID PDA 任务；变更归属时重置未执行任务的接单事实 | 否 |
| 实际数量为 0 时被演示兜底数量覆盖，正式生产单又可能按数组序号拼接假业务事实 | `算不准`、`信息失真` | 业务人员、染厂主管 | 数量仅在缺失时回退，明确的 0 原样展示；生产单、商品、面料和颜色只读取真实快照或显式演示资料，缺失显示“—” | 否 |
| 多条操作日志、宽表冻结和流程卡图片此前缺少真实验收 | `追溯不足`、`操作成本高` | 业务人员、染厂主管 | 日志按每页 10 条分页；真实横向滚动验证冻结列与右侧操作栏；流程卡输出卡序号、布料样品 SPU、批号及两张资料图 | 否 |
| 印花列表操作列被压缩后，查看与打印按钮的点击区域发生重叠 | `易误操作` | 工艺工厂操作员 | 通用印花操作链接禁止换行，恢复真实可点击宽度，并以鼠标点击验收查看入口进入 Web 详情而非打印预览 | 否 |
| 入库确认、PDA 通用开工、接单初始化和加工中改派可能产生跨端事实分叉 | `状态冲突`、`协作断裂`、`权限与状态冲突` | 仓管、染厂操作员、染厂主管、业务人员 | 领域检查覆盖入库原子性、明确接单、开工同步和改派重置；浏览器上下文提交部分/足额接收事实并验证 PFOS 状态，真实页面验证改派后旧厂失去执行入口、新厂重新接单后继续原状态 | 否 |
| 三种导出和批量打印缺少真实下载与多选顺序浏览器证据 | `协作断裂`、`追溯不足` | 业务人员、染厂主管 | Chromium 已验证三种文件真实下载、未勾选阻断、按勾选顺序打印和逐单分页 | 否 |
| 待分配工厂只能选择当前列表已经出现过的染厂，补料、差异和库存风险表达不完整 | `信息不足`、`信息失真`、`操作成本高` | 业务人员、染厂主管 | 工厂选项读取有效染色能力主数据，接收人允许直接填写；列表明确展示“补料”，差异改为“多 / 少 / 一致”，库存仅在不足时标红 | 否 |
| 各页面分别拼装计划、执行、交出和入库 Mock，可能出现状态正确但数量事实互相冲突 | `算不准`、`信息失真`、`协作断裂` | 业务人员、染厂主管、染厂操作员、仓管 | 统一以加工单、执行节点、交出和收货事实生成 PFOS 与 PDA 展示；自动检查逐单校验投入、产出、损耗、交出、实收和待回货守恒，并覆盖待分配、备货、补料、部分入库、取消、完成、超期、水溶及合并染色成员 | 否 |
| 已完成订单把计划量与实际产出的差额显示为待染数量，导致损耗被误认为仍需生产 | `算不准`、`信息失真` | 业务人员、染厂主管 | 染色完成、待审核、部分入库、取消和已完成统一显示待染数量为 0，差额只保留在损耗或收货差异字段；浏览器用已完成单验证 | 否 |

## 5. 最终结论

结论：通过。

- PFOS 列表在 1366×768 和 1280×720 下无页面级横向溢出；宽表仅在表格容器内滚动。
- 查看、编辑、日志弹窗均为局部更新；人工改状态后列表滚动位置和工作区 DOM 身份保持不变，未出现整页闪烁。
- 1280×720 下编辑弹窗和高风险状态二次确认完整可见，保存、取消与确认按钮均可达。
- 列显示和每页条数按路由持久化；离开后重新进入列表时页码恢复第 1 页、排序恢复默认。
- 中印双语染整生产流程卡可从实际行操作打开，平台加工单号、二维码、八个现场签认工序和签字区完整展示，页面无横向截断。
- PDA 使用真实“接单 → 开工 → 完成染色 → 发起交出”按钮推进，同一加工单在 PFOS 依次显示“等待处理 → 染色中 → 染色完成 → 待审核”。
- 关键按钮预热后均在 200ms 内给出可观察响应；浏览器控制台无错误。
- 任务 8 已修正两处旧回归基线：合并染色检查和浏览器用例不再要求单染色列表展示合并投影；PDA 返回 PFOS 前预热异步事件处理器，确保验证真实状态而非开发服务器首次编译时序。
- 任务 9 已关闭对抗式审查缺口：取消状态在 PDA 页面和写回层同时阻断；工厂归属同步真实加工单与 PDA；零数量、正式快照、日志分页、流程卡资料、列表冻结与跨端同号均有专项断言。
- 任务 10 已完成：移除需求单号；工厂分配与七个执行状态分维度；已完成设为终态；高风险人工状态调整二次确认；进行中改派保留执行状态、重置接单并转移执行权限。
- 任务 11 已按统一事实型 Mock 收口：生产单快照决定计划量，执行节点决定实际投入和产出，交出与收货事实决定待审核、部分入库和已完成；不再通过列表状态覆盖伪造业务进度。
- 真实浏览器按加工单号逐条核对补料染色中、部分入库剩余量、超期未完结、待分配工厂、备货来源和已完成数量；并验证已完成单待染数量为 0。筛选结果均为单条，控制台无错误。
- Chromium 已验证三种文件真实下载、未勾选打印阻断，以及按勾选顺序生成两张逐单分页流程卡。
- 入库验收在浏览器上下文中依次提交部分与足额接收事实，并验证 PFOS 显示“部分入库”与“已完成”；该证据不表述为仓库端 UI 点击验收。
- 进行中改派通过真实 PFOS 编辑完成：旧染厂执行页被阻断，新染厂重新接单后可从原“染色中”状态继续。
- 同一正式生产单染色加工单在平台加工单列表、PFOS 和 PDA 任务中均使用平台加工单号，不生成工厂侧第二套编号。
- 统一加工单旧验收已迁移到当前列表内查看、编辑、日志和打印口径；同时修复印花列表查看/打印操作区域重叠。
- 新鲜验证结果：染色专项 Playwright 6/6、合并染色 Playwright 1/1、统一加工单 Playwright 4/4；统一事实场景、染色专项、统一加工单、生产单生成与变更、染色/合并染色、水溶 PDA、列表页治理、原型设计治理及生产构建全部通过。本轮未重跑与此次事实源无直接关系的后道打印专项，不沿用旧结果冒充新鲜证据。
