# 后道交接 SKU 明细与已交出原型审查记录

## 1. 基本信息

| 项目 | 内容 |
|---|---|
| 记录日期 | 2026-09-03 |
| 需求编号 | `PDA-SKU-001`～`PDA-UI-002` |
| 端与角色 | 后道工厂 PDA；交接员、后道操作员、复检员 |
| 命名路由 | 待接收详情、交接已交出、后道加工、后道复检 |
| 审查状态 | 本地原型已验证，待产品接受 |

## 2. 影响判定

- 用户可见影响：有
- 判定依据：接收确认区和展开记录新增逐 SKU 识别卡；后道交接的“待交出”替换为与 Web 后道出货单同源的“已交出”；后道加工与复检页新增返回按钮。

审查依据：`AGENTS.md` 第 4 节员工执行端、交接与图片基线，第 5 节 UI 与真实图片门禁，第 7 节分层验证和证据新鲜度。

## 3. 自查结论

| 检查项 | 结论 | 说明 |
|---|---|---|
| 角色、当前任务与主动作 | 通过 | 待接收仍以当前 SKU 确认为主动作；已交出仅查看 |
| 信息密度与导航 | 通过 | 明细默认折叠；两个主页签；操作页有返回 |
| 文案、状态、数量和单位 | 通过 | 区分后道已交出数量与成衣仓已确认数量 |
| 扫码、真实图片与对象识别 | 通过 | 接收和出货 SKU 均从对应产品事实解析图片，保留失败态与大图入口 |
| 防错、危险确认与主管兜底 | 通过 | 本次不改变原接收差异、驳回和授权规则 |
| 交接、跨端事实与异常追溯 | 通过 | PDA 已交出不新建单据，直接读 Web 后道出货单 |
| 低分辨率、PDA、弱网与上传恢复 | 通过 | 390×844、480×800 自动化无溢出已通过；本次不改弱网和上传 |
| 命名路由、交互、图片大图与打印 | 通过 | 待接收、已交出、后道加工和返回动作均已本地目检；打印不受影响 |

## 4. 问题标签

- `追溯不足`
- `协作断裂`
- `缺导航恢复`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
|---|---|---|---|---|
| 接收只看到汇总数量 | 追溯不足 | 交接员 | 当前和历史均补充逐 SKU 识别与数量 | 否 |
| 后道工厂可重复发起交出 | 协作断裂 | 后道操作员 | 改为只读“已交出”并共用 Web 出货单 | 否 |
| 加工和复检页缺返回 | 缺导航恢复 | 后道操作员、复检员 | 标题栏增加返回执行工作台 | 否 |

## 6. 最终结论

结论：通过（本地原型范围）

说明：页面专项、旧交接回归、两遍业务追踪和 3×5×5 全链路均已通过；物理 PDA、真实扫码枪和生产环境不在本次本地原型证据内，仍待现场验收。

## 7. 变更覆盖与验证

### 受管文件

- `src/pages/pda-handover-detail.ts`
- `src/pages/pda-handover.ts`
- `src/pages/pda-post-finishing-flow.ts`
- `tests/post-finishing-pda-role-boundary.spec.ts`

### 页面路由

- `/fcs/pda/handover?tab=pickup`
- `/fcs/pda/handover/PKH-POST-WOOLHO202605004`
- `/fcs/pda/handover?tab=shipped`
- `/fcs/pda/post-finishing/execute`
- `/fcs/pda/post-finishing/recheck`

### 验证命令

- `PLAYWRIGHT_REUSE_EXISTING_SERVER=false CUTTING_E2E_PORT=4193 npx playwright test tests/post-finishing-pda-role-boundary.spec.ts --reporter=line`：通过（6/6）
- `PLAYWRIGHT_REUSE_EXISTING_SERVER=false CUTTING_E2E_PORT=4194 npx playwright test tests/pda-handover-pickup-record-card-compact-layout.spec.ts tests/pda-handover-pickup-p1-record-card.spec.ts tests/pda-handover-pickup-list-and-detail.spec.ts tests/pda-processing-mobile-density.spec.ts --reporter=line`：通过（9/9）
- `PLAYWRIGHT_REUSE_EXISTING_SERVER=false CUTTING_E2E_PORT=4198 npx playwright test tests/post-finishing-pda-role-boundary.spec.ts tests/pda-handover-pickup-record-card-compact-layout.spec.ts tests/pda-handover-pickup-p1-record-card.spec.ts tests/pda-handover-pickup-list-and-detail.spec.ts tests/pda-processing-mobile-density.spec.ts --workers=1 --reporter=line`：通过（最终联合回归 15/15）
- `tests/post-finishing-full-flow-cross-terminal.spec.ts` 第一遍：通过（1/1，3×5×5 完整链路）。
- 同一全链路第二遍使用项目外运行期证据目录：通过（1/1，6.1 分钟）；项目内直接写运行期 JSON 曾触发 Vite 热重载并使瞬时成功提示丢失，已确认是测试证据路径干扰而非业务失败，最终通过后再归档证据。
- `npm run build`：通过（最终改动后重新构建，Vite 2395 个模块转换完成）。

### 真实图片验证

- 待接收按交接记录反查 SPU/SKU 产品资料图；本地目检 3 个 SKU 均有图。
- 已交出优先使用出货单行图片，历史行缺失时按 SPU/SKU 反查同一产品图；本地目检 2 个 SKU 均有图。
- 图片按钮共用 PDA 大图层，保留加载失败文案。

### 例外

- 本次仅代码原型与本地浏览器 PDA 验收；不代表物理 PDA、真实扫码枪或生产环境已验收。

### 落地证据

- `output/playwright/post-finishing-handover-sku-shipped-20260903/cross-terminal-evidence.json`：15 条完整链、75 条 SKU 回货明细、各类业务单据和操作日志汇总。
- `output/playwright/post-finishing-handover-sku-shipped-20260903/final-screenshots/`：49 张过程与页面截图。
- 需求追踪矩阵：10/10 条已验证，并记录第一遍正向追踪和第二遍反向追踪结论。
