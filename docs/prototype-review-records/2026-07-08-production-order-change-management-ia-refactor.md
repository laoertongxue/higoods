# HiGood 原型审查记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-07-08 |
| 相关需求 / 任务 | Task 3：生产单变更分层页面状态与路由导出 |
| 涉及系统 | FCS |
| 涉及页面路径 | `/fcs/production/changes`、`/fcs/production/changes/new`、`/fcs/production/changes/orders/:id`、`/fcs/production/changes/:id/edit`、`/fcs/production/changes/:id` |
| 端类型 | 管理端 |
| 主要角色 | 跟单、买手、工艺、计划履约、结算 |
| 主要任务 | 将生产单变更拆成列表、新增、编辑、变更单详情和生产单关系详情路由，后续页面实现可按对象分层承接。 |

## 2. 参考规范

- `docs/higood-indonesia-factory-product-design-guidelines.md`
- `docs/higood-indonesia-factory-prototype-review-checklist.md`

## 3. 自查结论

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色匹配 | 通过 | 本次只补 FCS 管理端路由，不下放给一线员工判断。 |
| 任务清晰度 | 有条件通过 | 新增页占位保留 6 步入口，明确由系统反推变更类型；后续任务会替换完整页面。 |
| 信息架构与导航 | 通过 | 路由拆分为变更单、生产单关系、新增和编辑，避免继续用单一路径混合不同对象。 |
| 页面模式 | 有条件通过 | 当前是最小占位，不做 UI 重构；完整表格、详情和交互由后续任务补齐。 |
| 信息负荷 | 通过 | 占位只展示必要标题、步骤和 Tab 名称。 |
| 文案 | 通过 | 使用中文业务文案，不展示英文状态码。 |
| 数量与状态 | 通过 | 本次不新增数量计算或状态判断。 |
| 扫码与识别 | 通过 | 本次不涉及扫码。 |
| 防错 | 有条件通过 | 通过路由分层减少对象误判；具体表单防错待后续实现。 |
| UI 样式 | 通过 | 复用现有 Tailwind 字符串模板风格。 |
| 组件交互 | 通过 | 本次不引入新交互。 |
| 协作关系 | 有条件通过 | 路由为后续按变更单和生产单关系分别追溯打基础。 |
| 异常与追溯 | 有条件通过 | 详情占位包含处理记录入口，完整追溯由后续任务实现。 |
| 现场设备可用性 | 通过 | 本次为管理端页面，不改变现场执行端。 |

## 4. 问题标签

- `状态抽象`
- `追溯不足`

## 5. 主要问题与处理

| 问题 | 标签 | 影响角色 | 处理方式 | 是否仍有风险 |
| --- | --- | --- | --- | --- |
| 旧路由把生产单关系详情和变更单详情混在 `/fcs/production/changes/:id`。 | 状态抽象 | 跟单、工艺、计划履约 | 增加 `/orders/:id` 承接生产单关系详情，`/:id` 承接变更单详情。 | 否 |
| 当前新增、编辑、详情页面仍为占位。 | 追溯不足 | 跟单、结算、计划履约 | 本任务只保证状态与路由可达，后续任务补齐完整页面内容。 | 是 |

## 6. 最终结论

结论：有条件通过

说明：

- 本次只做页面状态和路由导出，不做 UI 重构和数据层修改。
- 例外：新增、编辑和变更单详情为最小占位，后续任务替换完整实现。

## 7. 追加审查记录：列表页与待处理来源池

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色匹配 | 通过 | `/fcs/production/changes` 仍为 FCS 管理端，服务跟单、工艺、计划履约和结算查看变更单与待处理来源。 |
| 信息架构与导航 | 通过 | 列表页拆为「变更单」和「待处理生产单」两个 Tab，不再把变更单详情和生产单关系判断混在首屏。 |
| 信息负荷 | 通过 | 移除场景覆盖面板和 80 个场景展示，列表只保留检索、统计、变更单列表和待处理来源池。 |
| 文案 | 通过 | 操作改为「新增变更」「查看」「继续处理」「撤回」「日志」「发起变更」「查看关系」，不把版本补丁动作直接放在变更单行。 |
| 防错 | 有条件通过 | 来源池发起变更会预填生产单并进入新增流程；撤回暂为原型提示，真实状态机留给后续任务。 |

结论：通过。

## 8. 追加审查记录：列表页统计联动与旧内嵌详情清理

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 信息架构与导航 | 通过 | 列表页继续只保留变更单列表和待处理生产单来源池，不恢复旧内嵌详情。 |
| 信息负荷 | 通过 | 删除旧内嵌详情死代码和旧选中 action，避免后续任务 5/6 与列表页职责混淆。 |
| 组件交互 | 通过 | 搜索单张变更单时，统计卡、变更单列表和关联影响统计使用同一过滤结果。 |

结论：通过。

## 9. 追加审查记录：新增 / 编辑分步骤表单

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 角色匹配 | 通过 | 新增和编辑页仍定位为 FCS 管理端，由跟单、工艺、计划履约或结算录入变更意图，不下放给一线员工判断技术包概念。 |
| 任务清晰度 | 通过 | 表单拆成选择生产单、填写变更内容、系统计算影响、确认单据处理、料工费与时效、提交审核 6 步，先让业务说清变了什么，再由系统反推结果。 |
| 信息负荷 | 通过 | 影响、单据、料工费和时效步骤只保留轻量说明，不恢复旧内嵌详情表格，完整详情留给变更单详情页任务。 |
| 文案 | 通过 | 页面使用中文业务文案，明确“系统反推，不要求业务人员先选版本关系或补丁”，避免把系统分类前置给业务人员。 |
| 防错 | 有条件通过 | 提交审核沿用数据层校验，变更原因为空时会展示红色错误；保存草稿用默认原因兜底，后续真实草稿状态由正式业务系统定义。 |
| 异常与追溯 | 通过 | 提交后生成变更单并跳转详情，系统同步生成影响、单据、成本和时效子记录，便于后续追溯。 |

结论：通过。
