# 实施计划

依据：同日设计第 1—4 节。基线 main 75f85bad；分支 codex/dyeing-dispatch-pages；同工作区服务 5188。

1. REMOVE：旧批次模块 → 菜单/路由/事件/领域/投影/样例、检查及历史规范 → 删除完整对象和引用，保留水溶及正式生产变更保护 → 全库扫描、类型、加工单和生产变更契约 → 无残留入口、死代码或旧规范。
2. DOMAIN：交出规则 → dyeing-task-domain.ts、接收衔接 → 打印门槛、草稿占用、扫描记录、实际交出及演示单据 → 专项契约 → 列表、加工单、下游事实一致。
3. PAGE：独立页面 → output-documents.ts、菜单/路由/事件 → 标准列表、筛选、分页/列设置、建单及加入、详情和扫描 → 浏览器第一轮 → 正常路径完成且 Mock 值具体可读。
4. PRINT：打印 → 交出打印模板 → 线上字段组合、单位分列、完整来源、A4 分页 → 打印截图 → 内容正确且无截断。
5. VERIFY：矩阵/审查记录 → 删除检查、数量专项、两轮浏览器、构建/治理/收据及 CodeGraph → 当前分支证据闭环后交付本地结果。
