# AGENTS.md

## 1. 项目定位与技术边界

这是一个 **HiGood 产品原型仓库**，不是可交付的生产业务系统。目标是快速、准确地生成和迭代接近 Axure 输出的高保真页面原型，并通过 GitHub 自动部署到 Vercel 演示。

固定技术栈：

- Vite、TypeScript、Tailwind CSS。
- Vanilla TypeScript 字符串模板渲染：页面函数返回 HTML 字符串，经 `renderAppShell(state, pageContent)` 生成外壳并写入 DOM。
- React 只用于二维码等少量隔离挂载点，不得把页面迁移成 React 组件体系。

默认实现页面结构、真实感公开演示数据和必要交互。所有后续需求及功能新增、调整必须按第 2.4 节接入 Supabase 共享持久化与附件存储，免登录公开访问；这是本原型明确授权的数据能力。不擅自增加真实身份鉴权、无关后端框架、复杂上传 / 图像处理、离线队列或自动重试基础设施。

禁止引入 Next.js、shadcn/ui、Radix、Redux、Zustand、MobX、Provider、React Query、SWR、复杂表单 / 图表框架，以及为未来扩展预建的 store、API、领域层或大型通用抽象。页面应“看起来像真实系统”，但不得把原型演示表述为真实业务已经发生或生产能力已经完成。

---

## 2. 执行边界与事实优先级

默认目标是 **在最小必要范围内把当前需求做干净**。

### 2.1 任务边界

- 开始时确定本次需求、页面 / 路由、验收点和任务负责文件；最终只汇报这些改动，不吸收用户已有的无关工作区差异。
- 模块干净、改动小时局部 patch；继续叠补丁会增加分支和兼容层时，允许在当前需求直接相关模块内干净重写。
- 重写必须保持路由、数据格式和事件入口等外部接口兼容，不得顺手修改无关模块、全局布局、菜单、脚手架或部署配置。
- 优先复用现有样式、布局、组件和 Mock 数据；不主动升级依赖、重构无关目录或修改无关检查脚本。
- 用户未明确要求时，不新建大型组件体系，不抽象“未来可能复用”的工具层，不把原型改造成工程化样板。

涉及代码或运行页面时，开始核查前必须确认当前分支、Git HEAD、实际运行服务 / 工作树、命名路由和验收设备。不得在一个工作树修改、用另一个版本的页面作为验收证据。

### 2.2 事实优先级

证据冲突时按以下顺序处理：

1. 用户本次明确确认的业务事实和范围。
2. 当前现场证据、实际运行页面、打印结果或可复现行为。
3. 当前代码、数据、路由和共享事实源。
4. 本文件的项目治理规则。
5. 历史文档、旧测试、旧截图和旧计划。

必须区分“代码已确认”“现场已确认”“基于证据推断”“待用户确认”。旧测试或历史文档与已确认业务冲突时，它们是待更新对象，不是最终事实。Mock 数据只代表原型演示场景，不得在设计文档或最终回复中表述为真实工厂事实。

### 2.4 Supabase 公开演示数据接入硬边界

自 2026-09-24 用户确认起，后续所有需求和功能新增、调整均按本节执行。Supabase 是被修改业务范围的共享持久化事实源；免登录、公开访问，匿名访问者可按页面业务规则读取和操作公开演示数据。不得自行改成必须登录，也不得把演示操作人标签当作真实身份鉴权。

#### 接入范围

- 覆盖新增或调整涉及的列表、详情、创建、编辑、复制、删除/作废、提交、审核、状态、数量、金额、配置、关系、操作记录，以及 Web/PDA/打印/导入导出对这些事实的读取。
- 同一业务动作涉及主单、明细、物料、费用、样衣安排、上下游单据、分配、执行和交接时，必须逐项登记同一事实的所有读写入口。由本次功能生成或修改的关联数据也在接入范围内，不能只迁移主页面而让下游继续写浏览器存储。
- 设计稿、物料/款式图片、纸样、样衣照片和其他附件进入 Supabase Storage，业务记录保存对象路径及元数据；禁止把 Base64、Blob 字节或仅当前浏览器有效的 blob URL 作为持久化附件。
- 纯文案、样式或无持久化状态的交互变更无需制造数据库表，但必须核验受影响页面确实读取相应 Supabase 事实，并在任务记录说明不产生新增持久化数据的依据。
- 既有未迁移模块在下一次新增/调整涉及该业务时一并迁移所需读写链路；不能以“旧代码”豁免当前变更。此规则不要求无关模块在每次任务中全面重写。

#### 接入规则

1. 浏览器持久化仅保留第 2.5 节允许的少量界面偏好及非业务迁移标记；业务记录、草稿和附件不得持久化到浏览器，未提交输入仅保留在当前页面内存。成功保存须得到 Supabase 确认，并可在独立浏览器重新读取。
2. 缺少 Supabase 配置、网络失败或写入被拒绝时明确报错并保留可恢复输入；不得静默切回本地保存，不得显示虚假保存成功。公共演示种子通过可控初始化写入，禁止每个浏览器启动时覆盖共享数据。
3. 仅浏览器可公开的 publishable/anon key 可进入 VITE 环境变量；service_role、数据库密码等特权凭据不得进入前端、仓库、日志或答复。数据库启用 RLS，逐表、RPC 和 Storage 路径明确匿名权限；“公开”不等于关闭权限校验或开放管理权限。敏感测试站资料不得复制到公开演示库。
4. 在 Supabase 服务端校验必填字段、对象归属、状态、数量和业务前置条件；不能只依赖前端校验。多记录动作必须事务提交；采用并发版本校验和持久请求标识，防止覆盖同事修改和重复建单。响应丢失时核对真实结果，不能直接宣称回滚。
5. 删除/作废、附件替换、并发修改和上传失败需要明确恢复行为；附件复制复用引用，不能误删仍被引用的文件。旧浏览器数据迁移须有显式来源、重复导入防护和核验，未确认前保留原数据。
6. 开工先列业务对象 → Supabase 表/Storage → 所有读写入口 → 原子动作/并发/恢复 → 验收证据，纳入第 3.1 节矩阵。既有测试站数据库仍严格只读；新配置的公开演示 Supabase 与测试站不得混同。
7. 验收至少覆盖无登录的新浏览器读取、两端修改与冲突、刷新/直达深链接、附件上传及跨端预览、空/满浏览器存储、拒绝/断网/响应丢失、重复操作和下游结果一致性；SQL/RLS/RPC 需直接请求验证，不能仅用模拟 fetch 的单测证明服务器规则已生效。
8. 未迁移入口、旧本地写入、待应用 SQL、缺失环境配置或未通过场景均列为未完成；不得把“已安装集成”“Storage 可上传”“构建通过”表述为业务已完整接入。发布沿用第 8.2 节，核对对应提交的 Vercel 部署和线上 Supabase 实际读写。

### 2.5 浏览器业务存储退出、迁移清理与交付硬门禁

本节由用户于 2026-09-24 明确确认，长期适用于后续任一页面的功能新增、调整和修复，以及由此涉及的共享数据、上下游、Web、PDA、打印和导入导出入口。与旧本地存储、缓存或临时输入规则冲突时，以本节为准。目标是：**浏览器中的业务存储可以被彻底清空；清空后已保存业务不丢失，后续业务不依赖浏览器持久化，也不会重新积累进去。** 本节要求免登录公开访问，沿用第 2.4 节匿名权限和服务端业务约束。

#### 2.5.1 最终数据归属与范围

- 任务、已保存草稿、明细、物料、数量、费用、样衣安排、配置、关系、状态、执行、交接、接收和操作记录统一保存到 Supabase 数据库。点击“保存草稿”后，须能在另一台设备读取；未保存输入不能冒充已保存草稿。
- 设计稿、款式／物料图片、纸样、样衣照片及其他业务附件统一保存到 Supabase Storage；数据库只保存文件标识、对象路径、大小、类型、校验信息和业务引用等元数据。
- 当前页面编辑值和待上传文件仅保存在内存，明确未保存状态及离开提醒；失败时保留当前页面可重试输入，不得承诺刷新或关闭后仍能恢复未提交内容。
- localStorage、sessionStorage、IndexedDB 及应用自行管理的 Cache Storage 不得新增或持续保存业务记录、整包快照、草稿或附件副本，不得用“缓存”“离线草稿”换名规避。仅允许少量、有界且可丢弃的界面偏好及非业务迁移标记；读写失败必须使用默认值，不能阻断业务。浏览器自动管理的 HTTP 静态资源缓存不作为业务事实源，无需全站禁用。
- 调整任一页面时，必须同时完成该业务动作依赖的全部读写链路迁移，不能只接入主页面、列表或上传接口。纯文案／样式调整无需新建数据表，但仍须检查受影响页面的数据来源并收口依赖的旧本地业务存储。无业务数据的页面记录不适用依据。
- 不要求顺手重写无关模块；但尚未迁移的其他模块必须登记，不能以局部通过宣称全站存储问题解决。每个被调整模块均以本节为完成门禁。

#### 2.5.2 封闭旧读写与云端业务提交

1. 先登记并关闭旧写入，再执行清理。删除页面启动、模块导入、读取、查询和初始化时写入浏览器快照的逻辑；删除保存、复制、提交、执行、交接和附件处理中的本地业务持久化入口。
2. 删除 Supabase 缺配置／失败时回退本地保存的分支，以及旧浏览器记录覆盖云端数据的恢复逻辑。公开演示种子仅通过受控初始化进入云端，不能在每次访问时覆盖或重复导入。
3. 按业务对象、所属任务和所需字段查询，列表分页；按记录／业务动作保存。不得长期将整个共享工作区作为一个不断扩大的快照整体读写。存量整包方案在受影响范围拆分并验收后才能关闭迁移。
4. 只有获得服务端提交确认才显示保存成功。多记录业务动作通过数据库事务原子提交，服务端校验对象归属、必填、状态、数量、金额和前置条件，禁止留下半套关联单据。
5. 每次业务动作使用持久唯一请求编号及记录版本控制；重试沿用同一请求编号。重复点击不重复建单，同事并发修改不静默覆盖。请求超时／响应丢失时查询提交回执，明确成功、失败或结果待确认，禁止未经核对自动回滚或生成新请求重复提交。
6. 直接访问下游链接、刷新、首次打开和跨端操作均须独立读取云端事实，不得依赖先访问另一个页面形成内存或本地存储状态。

#### 2.5.3 文件上传、业务关联与空间回收

- 顺序固定为：选择文件与内存临时预览 → 唯一路径上传 Storage → 业务事务登记附件引用 → 云端提交确认 → 释放临时预览和不再使用的内存引用。禁止 Base64、Blob 字节或 blob URL 持久化到业务记录或浏览器存储。
- 替换文件使用新对象路径；复制业务记录可复用既有附件引用。删除前必须检查全部有效引用，不得误删其他任务、版本或归档仍使用的文件。
- Storage 上传与数据库提交不能假定天然原子。必须记录待关联、已关联及可清理状态，允许业务保存失败后的同次重试复用已上传文件，避免重复上传。
- 实现受控的服务端孤立文件清理。任务须明确保留时长、重试窗口、引用核验、执行入口及清理证据；超过约定窗口且无有效业务／迁移引用的文件才可删除。不得把“以后人工清理”作为已完成。
- 管理数据库、文件容量和上传量，设定并记录与当前套餐匹配的容量阈值和管理员处理入口；管理过期演示归档及无引用文件。云端达到限制时明确失败并保留当前未保存输入，禁止转存浏览器冒充成功。不得承诺免费云端容量无限。

#### 2.5.4 一次性旧数据归档、迁移与清除

1. **清单与隔离**：列明受影响 localStorage／sessionStorage 键、IndexedDB 数据库和对象仓库、应用业务缓存、附件实体、共享存储项及全部旧读写入口；区分独占与共用归属。禁止无差别清空全站或其他模块数据。
2. **先停止旧写入**：版本切换期间暂停受影响业务编辑，协调同浏览器标签页、关闭数据库连接并提示旧页面刷新。旧版本无法可靠停止时保留源数据并报告阻断，不能仅凭删除前重读一次认定并发安全。处理 IndexedDB 删除被旧连接阻塞的情况，并验证旧标签页不会重新写回。
3. **建立可恢复归档**：需要保留的本地独有、冲突、旧版本记录和附件先上传到独立云端迁移归档区。记录批次、来源存储项、原始内容、附件、数量、校验值、迁移版本和完成状态，支持中断恢复和幂等重试。归档只接受已确认可公开的演示数据，沿用第 2.4 节数据边界；不得把敏感测试站资料上传公开库。
4. **归档与正式业务分离**：旧数据不能因页面访问自动覆盖正式云端记录。相同编号不代表内容一致；文件本地格式与云端路径不同也不代表未迁移。通过迁移凭据、版本和内容校验确认归档，正式恢复／导入使用明确入口、来源映射、去重和冲突处理，并保留结果。
5. **校验后清除**：云端读回校验通过、附件完整且旧写入已停止后，删除已归档的本地源数据，不必等待其全部合并到正式业务列表。独占项可整体删除；共用项只删除本业务数据，失败不得损失其他记录。分别处理附件实体与元数据，不能仅删除附件引用就宣称文件空间已释放。
6. **丢弃边界**：默认归档后清除。只有用户明确授权丢弃具体范围的旧数据时才可跳过归档；要求彻底解决存储问题本身不等于授权删除未同步数据。
7. **按设备完成**：每台设备下次访问时执行本设备迁移，服务端不能直接清除未访问设备的浏览器数据。迁移有明确的发现、归档中、待校验、待清理、完成或失败状态，展示进度和恢复入口；不得只写控制台日志。迁移标记不是唯一完成证据，须核对云端回执与源数据清理结果。
8. **有界执行**：按版本执行一次性迁移，分批处理大记录和文件，避免每次页面加载全量扫描。归档／校验失败保留源数据并可重试；未完成数据不能永久静默保留。记录迁移前后存储占用及剩余项，不能只凭记录条数声称释放了全部空间。

#### 2.5.5 异常与恢复要求

| 场景 | 必须行为 |
| --- | --- |
| 缺配置、网络失败、服务拒绝、云端容量限制 | 明确失败与重试方式，保留当前内存输入，不回退本地业务保存 |
| 保存中、重复点击、响应丢失 | 显示真实进度，防重复提交，按持久请求编号核对结果 |
| 同事并发修改 | 检测版本冲突，重新读取并明确处理冲突，不静默覆盖 |
| 上传成功但业务保存失败 | 保持待关联记录供重试，按规则回收最终无引用文件 |
| 旧数据归档失败、连接占用、旧版本仍写入 | 保留源数据，说明迁移阻断及恢复动作，不标记完成 |
| 浏览器偏好存储已满、被禁用或抛错 | 使用默认偏好，所有已迁移业务仍可正常读取、保存及上传 |

#### 2.5.6 实施顺序与强制验收

固定顺序：**登记对象和入口 → 封闭旧写入 → 完成全链云端读写及事务／并发／幂等 → 完成附件关联与回收 → 执行归档校验和本地清除 → 验证浏览器业务存储不再被使用 → 新一轮对抗式审查 → 授权发布及 Vercel 核验。** 不得以无关重构扩大任务，亦不得省略本业务的下游。

每次功能调整按第 3.1 节登记适用要求、实现与当前证据；至少覆盖下列场景，不适用须逐项说明依据：

- localStorage 满额，以及 localStorage／IndexedDB 不可用时，读取、保存、复制、提交、附件上传和相关执行动作仍正常；偏好降级不影响业务。
- 在隔离验收浏览器中清空业务存储后重新打开，云端已保存任务、状态、明细及附件完整恢复；连续创建、复制、上传和执行后，浏览器没有新增业务持久化记录或文件。同时检查源码入口和实际运行写入，不能只捕获异常。
- 无登录新浏览器及两台设备读取同一事实；并发修改不覆盖，重复提交不重建，提交成功但响应丢失后可准确确认结果。
- 旧数据混合新旧版本、同编号冲突、Base64／文件实体时，归档完整；迁移中断后恢复不丢失、不重复导入；共用存储中的其他模块记录不受损。
- 多标签页和旧版本标签页不误删新修改、不重新写回；IndexedDB 连接阻塞能够提示和恢复；清理成功须实际核验存储项及附件实体。
- 上传成功后取消或业务失败产生的孤立文件能实际回收，仍被引用的文件不被删除；归档有实际可恢复／导入证据。
- 命名业务从主单到下游执行、交接和完成的完整流程在 Web／PDA／关联页面一致；直达、首次打开和刷新不依赖访问顺序。
- SQL、RLS、RPC、Storage 权限和清理规则须在目标演示环境通过必要的直接请求核验；模拟请求、单测和构建不能替代实际云端验证。
- 按第 7.2 节保留原始耗时、真实 Supabase 等待和客户端净耗时。客户端迁移解析、校验、序列化、计算和渲染不能扣除；迁移等待不能包装为页面性能通过。
- 最后一次实质修改后进行新一轮对抗式审查，主动验证数据丢失、重复提交、并发覆盖、旧数据回流、本地回退、附件泄漏和下游断链；发现问题须修复并重测受影响项，存在未解决项不得结束为完成。

完成必须同时满足：**旧写入已封闭、适用链路全部接入、旧数据迁移清理已在验收设备闭环、其他设备具备可执行迁移路径、上述适用场景通过、当前证据齐全且新一轮对抗式审查通过。** 规则写入、代码存在、云端集成安装或构建成功不等于业务迁移完成。发布仍须获得用户授权；每次推送 GitHub 后按第 8.2 节核验该提交对应 Vercel 部署完成及线上实际读写，不得以 GitHub 已接收代替交付。

### 3.1 需求追踪与交付闭环

当任务满足以下任一条件时，必须建立“总体设计／需求 → 实施计划 → 原子需求追踪矩阵 → 实现与验证证据”的闭环，不得只依靠长篇设计文档或口头确认判断完成：

- 用户明确要求“不得遗漏”“全部实现”“逐项交付”或同等严格口径。
- 需求影响 3 个以上模块、共享事实源、公共组件、路由总入口、Web／PDA／打印闭环、迁移删除或高风险发布。
- 同时包含角色、状态、数量、权限、上下游、版本、时效或多个专业团队协作。
- 需要分阶段实施并由研发、测试、产品共同验收。

小范围文案、静态布局或单一 Bug 修复不强制新建矩阵，但仍需按第 3 节和验证规则形成直接证据。

#### 3.1.1 四类文档职责

1. **总体设计／需求文档**：定义业务事实、范围、对象、角色、状态、规则、页面和验收口径，是“做什么”的权威来源。
2. **实施计划**：把需求按依赖拆成工作包，写明业务目标、文件／符号、修改、验证证据和完成条件，是“怎么做”的执行依据。
3. **需求追踪与交付矩阵**：把所有规范性条款拆成原子需求，逐条绑定来源、工作包、实现位置、自动化证据、页面／设备证据、状态和确认人，是“是否漏做、是否完成”的控制面。
4. **原型审查记录／任务收据**：保存当前版本真实执行过的验证和版本信息，是“本次交付是否有证据”的回执。

四者不得互相替代：计划不是完成证明，构建不是业务验收，页面截图不是领域规则证据，矩阵中出现需求编号也不代表已经实现。

#### 3.1.2 原子需求拆分规则

每条原子需求只表达一个可以独立判断通过或失败的结果，并使用稳定编号。编号按业务域组织，例如 `MASTER-001`、`TASK-001`、`BOM-001`、`PAGE-001`、`PERF-001`。

拆分时必须覆盖：

- 范围与非范围、删除和保留边界。
- 业务对象、唯一性、来源和上下游关系。
- 角色、负责人、操作身份和真实动作权限。
- 状态、进入／退出事件、完成条件、返工和不可逆规则。
- 数量、单位、金额、公式、精度和时间口径。
- 正常场景、阻断／边界场景和恢复动作；用户明确排除异常模块时，不擅自扩展异常流程，但保留必要防错门禁。
- 页面、路由、字段、按钮、Mock、真实图片、设备、打印和性能。
- 删除代码、旧逻辑收口、历史兼容或明确不兼容范围。
- 自动化检查、命名页面验收、治理记录和交付证据。

禁止把“完成某页面”“完成某模块”作为唯一需求条目；必须继续拆到对象、动作、规则和可观察结果。相同规则在文档中重复出现时可以合并为一条原子需求，但必须列出全部来源章节；不同角色、不同状态或不同完成条件不得为减少行数而合并。

#### 3.1.3 追踪矩阵必填字段

矩阵至少包含以下字段。字段可以放在一张宽表中，也可以拆成以同一需求编号关联的“需求登记表”和“交付证据表”；无论采用哪种形式，最终必须能按需求编号完整联查：

- 需求编号。
- 来源文档和章节。
- 原子需求。
- 对应实施工作包。
- 实现文件／符号或明确的待绑定位置。
- 自动化验证。
- 页面、PDA、打印或性能验证；不适用时明确写“不适用”及理由。
- 当前状态。
- 证据链接或证据位置。
- 产品确认人和确认版本。

状态统一使用：`待实施`、`实施中`、`已实现待验证`、`已验证`、`已阻塞`、`不适用`。不得使用含糊的“基本完成”“大致通过”。

#### 3.1.4 实施和变更规则

1. 开工前完成总体设计章节与原子需求编号的双向覆盖检查：每个规范性章节至少映射一条需求，每条需求必须能回到原文。
2. 每个研发任务、提交或 PR 必须声明负责的需求编号；不得吸收未声明编号的业务变更。
3. 代码实施后补齐实际文件／符号和证据，不得长期保留“待绑定”。
4. 需求发生变化时先修改总体设计，再同步实施计划和矩阵；保留变更记录，不直接在代码或测试中形成隐性新口径。
5. 删除需求必须同时追踪菜单、路由、页面、处理器、数据／Mock、测试、文案和兼容入口；仅隐藏菜单不算完成。
6. 同一事实只保留一组充分证据；不得用多个重复测试制造已覆盖假象。
7. 发现设计、计划、矩阵、代码或页面冲突时，按项目事实优先级修正低优先级对象，并重新执行受影响验证。
8. 用户确认业务对象或模块更名时，默认执行完整迁移，不得只增加用户可见别名。菜单、页面标题、页面地址、路由键、文件／符号、类型、字段、关联 ID、事件与状态来源、Mock、持久化键、导入导出、测试和文档必须统一使用新名称；旧地址、旧路由注册和兼容跳转必须直接删除，不得保留旧对象、双写或两套事实源。只有用户明确要求保留外部兼容入口时，才可另行记录例外、期限和移除条件。

#### 3.1.5 完成与交付门禁

原子需求只有同时满足以下条件才能标记“已验证”：

1. 实现位置已经存在且由当前版本读取。
2. 相关自动化契约通过；纯视觉需求可标记自动化不适用，但必须有浏览器证据。
3. 适用的命名页面、PDA、图片、大图、打印或性能场景已经在当前分支和工作树验收。
4. 最后一次实质修改后证据重新生成。
5. 没有与该需求冲突的遗留入口、旧文案、Mock 或测试。
6. 证据位置、验证版本和确认人已写入矩阵。
7. 涉及页面或交互的需求必须通过第 7.2 节性能门禁及其中有明确授权的例外；超时或缺少有效证据时，不得标记“已验证”或宣称完成。

阶段只有在负责的全部需求均达到约定状态后才能关闭；总体交付只有在矩阵不存在无说明的 `待实施`、`实施中`、`已实现待验证` 或 `已阻塞` 条目时才能宣称完整。`implemented`、`verified`、`delivered`、`accepted` 仍按项目交付定义使用，不得把矩阵勾选替代远端提交或产品接受回执。

#### 3.1.6 审查方法

最终审查必须同时做两次追踪：

1. **正向追踪**：从总体设计逐条走到矩阵、实施位置和证据，检查是否漏做。
2. **反向追踪**：从代码、页面、路由、Mock 和测试回到需求编号，检查是否越界、曲解或加入未确认能力。

可以使用脚本检查编号唯一性、章节覆盖、必填字段、状态值和证据空缺，但语义是否忠实仍必须由主代理和产品负责人结合原文审查，不能只凭编号数量或构建绿灯判断。

---

## 3. Sol 工作方式

### 3.1 Superpowers 项目级默认禁用

- 本项目默认禁用所有 Superpowers 技能和工作流：不主动读取、加载、调用或执行任何 Superpowers `SKILL.md`，也不因插件注入、会话钩子、技能清单或“可能适用”提示而自动触发。
- 只有用户明确指定使用某个技能（点名该技能，或明确要求按它的流程执行）时才可启用，且范围严格限于被点名的技能，不顺带加载其他技能、阶段轨迹或完整工作流。
- 用户讨论、引用或 @ 提及 Superpowers 插件不等于指定使用某个技能；任务看起来契合某技能同样不等于指定。要求“全部启用／长期启用”时必须先修改本节，再执行流程。
- 不创建 Superpowers 阶段轨迹、技能证据、专用计划目录或多阶段代理链路；用户指定使用某技能时，其产出仍按第 3.4 节和第 7 节的项目验证口径交付。
- 有价值的方法已内建在第 3.4 节。与外部插件、技能元数据或自动触发说明冲突时，以本节为准。

### 3.2 高效执行

质量以“需求是否形成直接证据闭环”衡量，不以技能、代理、工具或检查数量衡量。

任务按三类执行：

1. **只读核查 / 解释**：确认相关业务、代码、路由或页面事实后直接回答；不修改文件，不运行构建、全量治理或任务收据。
2. **设计 / 文档**：核对命名范围内的事实和上下游，建立需求与证据对应关系；不把设计验证扩大成代码实现验证。
3. **代码 / 页面实现**：完成定位、修改、相关专项检查和命名页面验收；只有共享组件、路由、构建配置受影响或准备正式交付时，才增加项目级检查。

强制规则：

- 同一事实由可信证据确认后，不得换工具或代理重复证明。
- 用户要求“再次核查”时优先核对上次结论后的差异、遗漏和未验证项；只有明确要求全部重查时才全量重读。
- 长任务维护简短的“需求 → 实现位置 → 验证证据”清单，每项需求只保留一组充分证据。
- 工具失败时修正参数或切换直接证据；同一路径不得无变化重复尝试。
- 过程更新只报告新事实、风险和结果；不得以节省时间或 Token 为由跳过业务正确性、真实图片、命名页面、打印或用户明确验收。

### 3.3 Sol 子代理决策与调用

Sol 自主判断是否使用 Codex 原生子代理。默认使用 0 个；子代理只用于缩短真正独立任务的墙钟时间，不替代主代理判断。

启动条件：

- 主代理工作之外至少存在 1 个无先后依赖、文件 / 事实源不重叠且无需频繁交换结果的旁路任务，形成至少 2 条可并行工作流，并能明显缩短时间。
- 每个子任务都能限定单一目标、读写范围、交付物和停止条件，主代理可一次整合和验收。
- 单页、单函数、单一 Bug、少量 patch、同文件协作、串行依赖、重复审查、一次 CodeGraph 可回答的问题、运行或等待命令等场景禁止启用。

数量和告知：

- 1 个独立旁路任务用 1 个子代理，2 个独立模块用 2 个；只有 3 个完全独立工作流时才用 3 个，并发上限为 3。
- 同一需求的独立审查最多 1 个子代理；禁止子代理继续创建下级子代理，除非用户明确要求。
- 启动前用一条 commentary 说明原因、数量、每个子代理的目标与读写范围 / 交付物、主代理职责和整合方式。

主代理必须检查实际产出和 diff，解决共享事实、接口及页面一致性，完成最终验证；不得直接采信子代理的“已完成”。发现越界、重复或冲突时立即停止派发并收回处理。

### 3.4 项目内建方法

#### 3.4.1 需求说明文档

用户要求需求说明、产品方案、业务设计或 PRD 时，先核查代码、实际路由 / 页面、上下游数据和现场事实，再用至少一个正常场景和一个异常 / 边界场景验证角色、对象、数量、状态和交接。

文档默认包括：背景与目标 / 非目标、角色与现场条件、业务对象与关系、数据来源与上下游、当前与目标流程、状态和时序、页面与关键动作、Web / PDA / 打印分工、真实图片要求、异常防错与恢复、数量单位与权限、验收标准、假设和待确认项。

用户要求“仅业务语言”时，正文不得出现文件名、函数名、接口、数据库、实现步骤或技术状态码。需求确认前不得把设计表述为已经实现。

#### 3.4.2 正式实施计划

用户明确要求，或任务影响 3 个以上模块、共享事实 / 事件账 / 公共组件 / 路由总入口、Web + PDA + 打印闭环、迁移删除和高风险发布时，需要正式实施计划；小范围页面 patch 不创建计划。

计划必须写明目标与完成标准、事实依据、范围 / 非范围、上下游影响和依赖顺序。每一步使用“业务目标 → 文件 / 符号 → 修改 → 验证证据 → 完成条件”，并覆盖数据 / Mock、旧逻辑处理、页面 / PDA / 图片 / 打印验收、风险和待确认问题。计划是实施依据，不是完成证明；用户要求先出计划时，确认前不得实现。

#### 3.4.3 系统化调试

1. 在用户点名的分支、服务地址、路由和设备条件下复现原始问题。
2. 区分代码、版本 / 工作树、数据、渲染分支和环境问题。
3. 一次验证一个根因假设，根因未确认前不得连续试改。
4. 建立最小失败复现或专项契约，实施最小修复。
5. 重放原始失败场景和直接相邻回归路径。

构建通过不能代替页面、PDA、打印或性能问题已经修复。

#### 3.4.4 核心逻辑测试与高风险审查

- 数量公式、状态推导、事件投影、库存守恒、历史兼容或打印映射变化时，优先先写能复现错误的最小专项检查。
- 纯文案、静态布局和视觉微调不强制测试先行，但必须验收真实页面。
- 测试验证业务结果；旧测试与确认后的业务冲突时同步更新契约，不为简单原型引入新测试基础设施。
- 共享事实源、公共 UI、事件账、Web / PDA 共用逻辑、历史兼容或合并 `main` 前，由主代理对最终完整 diff 做一次高风险审查；只有范围完全独立时才按第 3.3 节使用最多 1 个只读子代理。

---

## 4. 印尼工厂现场产品设计基线

本节是现场产品设计基线，与第 5 节专项 UI 门禁、第 7 节验证规则共同生效。以下长文档保留为历史详细依据，日常任务不主动读取；只有用户要求追溯历史依据、修改本节治理基线或本节没有覆盖争议项时才读取：

- `docs/higood-indonesia-factory-product-design-guidelines.md`
- `docs/higood-indonesia-factory-prototype-review-checklist.md`

### 4.1 角色与任务

- **管理端**：面向中国管理、计划、跟单、财务和运营，可展示全链路、统计、筛选和追溯。
- **主管端**：面向印尼主管、组长、仓管和质检负责人，突出待处理、异常、差异、超时和兜底动作。
- **员工执行端**：面向印尼一线员工，遵循“少读、少想、少算、少选、少填，系统多判断，主管可兜底”。Web 也可能属于员工执行端。

员工页围绕“当前任务、当前对象、当前动作、当前结果”组织，首屏只有一个明显主动作；复杂动作拆成 3～5 个短步骤，每步只问一个问题。员工优先从待办、工作台或扫码进入，不要求找菜单、理解模块或状态机。

PDA 和员工页面可以简化表达，但不得扭曲真实业务对象关系。复杂层级只保留防错所需归属；单一对象不制造分组，多对象分组只为避免选错。

### 4.2 识别、数量与文案

- 能扫码的对象优先扫码，扫码后立即展示识别结果；编号必须结合真实图片、颜色、尺码、数量、袋号或库位。
- 所有数量带现场单位；应领、已领、剩余、应交、已交和差异由系统计算，差异直接表达“少 / 多多少”。
- 员工端状态翻译成当前动作，不暴露写回、投影、执行对象、来源记录、状态流转、链路等系统词。
- 按钮使用明确动作词；错误文案说明哪里错、怎么改、何时叫主管，不责备用户。
- 超量、扫错、重复提交、必填缺失和状态不允许必须阻断；删除、作废、差异确认、代确认、库存调整和结算推进必须二次确认并保留操作事实。

当前原型默认使用中文业务文案和中文状态，不得直接显示英文技术状态码或乱码。员工端文案必须短句化、动作化并便于翻译；用户明确要求印尼语或中印双语时允许使用对应语言，不视为违反中文化要求。

### 4.3 协作、异常与恢复

- 交接必须表达谁把什么交给谁、应交与实交、差异、确认责任和扫码 / 图片证据，不能只展示状态或单据字段。
- Web、PDA、仓储、质检和结算读取同一业务事实和数量口径；管理端能追溯现场记录。
- 异常结构化记录操作人、时间、原因、差异数量和必要图片，并明确后续处理人；员工无法判断时提供“叫主管处理”“上报差异”或拍照说明。
- 弱网、上传失败和重复点击必须展示是否已保存、如何重试 / 重拍和何时找主管，但不要求真实离线基础设施。

### 4.4 页面模式与设备

- 管理端允许较高信息密度和标准表格；主管端保持中等密度并突出异常；员工端优先卡片、步骤和扫码结果，避免复杂表格、多 Tab、横向滚动和大量手填。
- 仓储页围绕“物在哪里、谁收发、数量差多少”；交接页围绕责任转移；质检页围绕质量事实；技术资料页围绕防止做错。
- 红色只用于错误、危险和阻断，黄色用于待处理 / 风险，绿色用于完成 / 一致，蓝色用于主动作 / 信息；图标不能替代关键文字。
- 管理端标准列表按 1366×768 验收、最低 1280×720；主管端和员工执行 Web 在 1024×768 可完成主要任务；PDA 按项目实际小屏验收。

### 4.5 Mock 数据与自查

Mock 数据必须足以支撑阅读和交互，并体现真实字段组合、上游来源、下游去向、多角色、多状态、差异、异常、扫码 / 图片证据和可追溯操作记录；不能只有空壳或一两条同质数据。

页面完成后只对本次适用项形成一次自查证据：

1. 角色、端类型、当前任务和主动作。
2. 信息密度、页面模式和导航。
3. 文案、状态、数量、单位和差异。
4. 扫码、真实图片和对象识别。
5. 防错阻断、危险确认和主管兜底。
6. 交接责任、跨端事实、异常和追溯。
7. 低分辨率、PDA、扫码枪、弱网和上传失败。
8. 命名路由、关键交互、图片大图和打印效果。

### 4.6 用户可见影响与治理记录

只有会改变用户看到、理解或操作结果的变更才需要完整原型审查记录，包括页面结构 / 样式 / 文案、字段、状态、数量、图片、可见 Mock 数据、路由 / 导航、按钮 / 交互、防错 / 异常路径，以及会改变上述结果的共享组件、数据或事件处理。

纯内部重构、类型 / 命名整理、内部工具调整、性能实现替换、测试维护等变更，如果能证明页面、业务事实、路由和交互行为不变，不需要填写完整自查矩阵；只需按 `docs/prototype-review-record-template.md` 的“无用户可见影响”简版填写受管文件、判定依据和技术验证。无法可靠判断时按“有用户可见影响”处理。

治理范围为 `src/pages/`、`src/components/`、`src/data/`、`src/router/`、`src/main-handlers/`。记录放在 `docs/prototype-review-records/`：

- **有用户可见影响**：填写完整审查记录、自查结论、页面路由、验证结果、例外和最终结论。
- **无用户可见影响**：填写简版影响声明，不要求角色、页面矩阵、两份历史长文档引用或产品结论。
- **仅文档 / 治理变更且未修改上述目录**：不创建记录或影响声明。

提交或交付前运行 `npm run check:prototype-design-governance`；仅在有意审计整个工作区时使用 `-- --all`。不得把真实的文案、Mock、图片、状态或交互变化伪装成内部重构。

---

## 5. UI 与交互专项规则

管理端保持清晰、稳定、克制的企业后台风格；主管端突出异常和待处理；员工执行端应像操作台，不照搬管理表格。不做复杂动画或营销视觉。

### 5.1 组件与渲染

- 新增或重写页面优先使用 `src/components/ui/` 当前已有的按钮、徽章、弹窗、抽屉、表格、表单、分页、筛选、卡片和反馈组件；按实际目录核查，不维护易失效的逐文件清单。
- 通用 UI 能力不足时在组件库补充最小能力；强业务且只出现一次的结构可在页面内拆成清晰 `renderXxx()`，不得堆进巨型模板。
- 不为统一 UI 迁移 React，也不在大型页面继续堆大量内联按钮、筛选、表格或弹窗模板。
- 页面加载和点击 / 输入操作响应默认严格低于 500ms（`< 500ms`）。用户明确允许时，少数较重页面可按第 7.2 节记录不超过 1s 的例外，不为此反复过度优化。验收以目标内容完成渲染、结果可用且可继续操作为准，计时与完成门禁见第 7.2 节；不得仅以按钮变色、出现 Loading、骨架屏、占位内容或“处理中”提示作为达标。Supabase 请求耗时按第 7.2 节扣除，其余上传处理、打印生成等客户端耗时不得自动豁免。
- 弹窗、抽屉、Dropdown、行操作、Tab、展开等使用局部 DOM 更新，不触发 `root.innerHTML` 整页重绘；只有路由或页面主体数据整体变化时允许整页渲染。
- 搜索、数量、备注和筛选输入使用局部更新或 debounce；一次 `input` 不得整页重绘。图标 hydration 只扫描新插入区域。
- 列表限制首屏渲染量，优先分页、分批加载或懒渲染，确有大量数据时再考虑虚拟化；不得为了性能引入新框架。

### 5.2 标准管理列表页

本节只适用于管理端 / 主管端桌面标准数据列表，不适用于 PDA、员工任务队列、固定摘要、步骤页和扫码结果页。

- 新增或调整的标准列表页在页面顶部声明 `// @page-pattern: list`，通过 `npm run check:list-page-governance`，并使用 `renderStandardListPage`、`renderStandardListTable`、`renderTablePagination`。
- 管理端标准列表固定按“页面标题与主操作 → 查询卡片 → 统计卡片 → 数据列表卡片 → 分页”排列。页面标题位于左侧，新建、查看当前业务单据等页面级主操作位于右侧；不得把查询条件散放在页面空白区域。
- 查询条件必须完整包在一个大卡片内。第一行放当前页面的高频条件，第二行固定提供可实际使用的“查询、重置、导出”；确有更多条件时在同一卡片内展开“更多筛选”，不得为筛选另开页面或多层弹窗。
- “查询”同时刷新列表、统计和总条数；“重置”清空全部条件并恢复完整结果；“导出”导出当前查询条件下的全部匹配记录而不是只导出当前页，且不得包含操作列。无可导出数据时必须给出明确反馈，不得放置无行为按钮。
- 统计卡片紧邻查询卡片下方，并与当前查询结果使用同一数据范围。状态类统计可以作为快捷筛选，但选中状态必须与查询条件同步。
- 数据列表必须独立成卡。列表标题和记录数位于左侧，批量操作按业务需要放置，“列设置”固定在列表表头最右侧；表格与分页位于同一卡片内。
- 不得为了套用参照页增加不存在的业务条件或切换 Tab。需要工厂切换等真实业务维度时可以放在查询卡片之前；没有该维度的页面不得制造工厂 Tab。生产准备管理下的所有标准列表明确不设工厂切换 Tab。
- 验收基准为 `/fcs/craft/cutting/supplement-management`；公共能力不足时扩展 `src/components/ui/`，不得复制整套页面模板。
- 无工厂切换 Tab 的管理端列表，以 `/fcs/craft/dyeing/work-orders` 的“查询卡片、操作按钮、统计卡片、列表表头、表格与分页”层级作为结构参照，但不得照搬染色业务字段、批量打印或专用导出动作。
- 摘要使用 48px 单行布局；列表必须分页，并明确当前页、每页条数和总数。
- 宽表支持列显示、顺序、排序和冻结；冻结普通列固定左侧，必需 / 防错列不可隐藏，操作列固定右侧。
- 列显示、顺序、冻结和每页条数按路由持久化；当前页和排序不持久化。
- 页面主体不得横向溢出，宽表在表格容器内部滚动；按第 4.4 节分辨率验收。
- `scripts/standard-list-page-baseline.json` 只保留未迁移且未变化的历史页面哈希，不得修改基线或检查脚本绕过门禁；业务例外写入完整原型审查记录。

### 5.3 款式与物料真实图片硬门禁

任一列表、详情、卡片、表单、弹窗、抽屉、PDA 或打印预览出现款式 / 物料时，必须满足：

- 每一个款式、每一种物料都有与对象真实对应的实拍图、效果图或正式物料图，不得只为部分记录配图。
- 表格缩略图与款式名称 / 款号、物料名称 / 编码在同一单元格、同一列组合展示；非表格页面放在同一信息块，不得让图片与对象标识分离。
- 缩略图可点击，弹窗展示原始或高清大图，支持关闭按钮、遮罩和 `Esc` 关闭；大图保持宽高比，在低分辨率下不溢出。
- 图片提供可理解的替代说明；加载中和失败状态必须可见，失败时不得静默显示破图或用无关占位图冒充。
- 禁止使用纯色块、图标、首字母、渐变、线框、无关网络图或通用占位图冒充真实图片；图片资源须在原型环境稳定可访问。
- 缺少真实图片时页面不得按完成交付，必须记录缺失对象和素材阻塞。
- 完整原型审查记录逐项说明图片来源、对象对应关系、缩略图位置和大图弹窗验证结果。

---

## 6. 模块例外

字典页默认按结果展示页处理，不做重后台维护感。除非用户明确要求，不开放新增、批量操作或复杂编辑，优先展示结果、来源和引用关系。

---

## 7. 验证原则

验证使用“需求 → 实现位置 → 当前证据”，按风险逐层增加：

1. **文件级**：检查任务 diff、语法、类型和直接契约。
2. **功能级**：运行命名页面、路由、组件或业务链路的最小专项检查。
3. **运行时级**：页面 / 交互变更在已确认分支、服务和命名 Web / PDA / 打印路径真实验收；视觉风险较高或用户点名时保留截图。款式 / 物料同步验收缩略图、失败态和大图。
4. **项目级**：共享基础能力、路由总入口、构建配置变化或准备提交 / 交付时，才运行构建、完整治理和任务收据。

最后一次实质修改后，受影响的专项检查和浏览器验收结果自动失效，必须重新执行。历史绿灯、计划或其他分支的页面不能作为当前通过证据；构建也不能代替命名页面交互和打印验收。

工作区有用户其他修改时只验证本任务范围，不覆盖、暂存、搬移或让任务收据吸收无关差异。全量收据无法限定任务文件时，使用独立工作树或明确说明未运行。

交互改动还要检查整页闪烁、滚动位置、输入卡顿、局部更新、首屏渲染量和图标扫描范围。

### 7.1 本地运行查看效果

用户要求“本地运行查看效果”时，必须让同一局域网设备可访问：

- 使用 `npm run dev -- --host 0.0.0.0 --port <port>`，优先复用当前演示端口。
- 获取局域网 IP，并以当前分支和工作树验证 `http://<局域网IP>:<port>/<path>` 返回 `200`。
- 最终给出局域网地址；无法访问时提示同一网络和 macOS 防火墙检查。

---

### 7.2 页面加载与交互性能硬门禁

本节是所有页面 / 交互任务的强制完成条件。性能未达标就是任务未完成，不允许“功能已完成、性能后续优化”“有条件通过”或仅记录超时后关闭任务。

1. **适用范围**：覆盖本次新增、修改及受共享代码影响的 Web / PDA 页面，包括首次进入与冷启动、整页刷新、站内路由切换，以及页面所有可交互入口；按钮、Tab、弹窗、抽屉、展开、查询、重置、筛选、输入、分页、排序、列设置、图片预览、保存及确认等不得漏测。不能只测最容易通过的少数动作。
2. **时限与明确例外**：默认每次页面加载和操作响应的门禁耗时（按第 3 项扣除 Supabase 请求耗时后）须严格低于 500ms；任一样本 `>= 500ms` 即该项失败。用户明确允许少数页面不超过 1s 时，在任务文档中列明具体页面、加载场景、上限和授权时间；这些样本 `<= 1000ms` 可通过，其余仍按 `< 500ms`。不得扩大为所有页面统一 1s，也不得隐去原始慢样本。不得用平均值、分位数、最快一次、剔除慢样本或四舍五入后的数值掩盖失败。
3. **完整计时**：首次进入 / 整页刷新从浏览器导航开始计时；站内切换、点击和输入从事件发生时计时，包含排队等待、模块加载、数据初始化与计算、DOM 更新和实际绘制。终点是目标页面首屏或本次操作应展示的内容、数量、状态及必要图片就绪且可交互；保存 / 确认还须对应结果确实可读。不得只计事件处理函数、模块已加载后的执行时间，或以 HTTP 200、DOM 已插入替代可用结果。
   **Supabase 请求耗时扣除（用户于 2026-09-24 明确授权）**：门禁耗时 = 原始端到端耗时 − 本次结果就绪所必需的 Supabase 请求等待区间的并集时长。数据库/REST/RPC/Storage 上传下载及必要附件加载均适用，按请求发出至响应体接收完成计时，并裁剪到本次操作起止区间；并发请求重叠区间只能扣除一次。只扣除真实等待网络的区间，不扣除与请求重叠执行的客户端计算、主线程工作、渲染及绘制；不能通过挂起无关请求冲抵客户端耗时。请求前序列化/压缩、响应 JSON 解析、数据归一化、状态计算、DOM 更新和绘制仍计入门禁。必须同时记录原始端到端耗时、每个相关 Supabase 请求区间、合并扣除值和门禁净耗时；失败/超时请求不能据此判为通过，也不能把整个异步处理函数耗时当作网络耗时。非 Supabase 请求仍计入原门禁；无法可靠分离时标为证据不足，不主观估减。此项长期规则无需逐次申请性能例外。
4. **真实环境与首次操作**：必须在已确认的当前分支、HEAD、工作树、实际服务地址和第 4.4 节适用设备尺寸下测量，记录浏览器、设备、数据量及缓存条件。冷启动、刷新、站内切换分别出具结果；不得预热模块、减少验收数据或换服务后把热态结果冒充首次结果。清缓存或重置数据只能在隔离的验收浏览器环境进行，不得清除用户工作数据。
5. **逐项证据**：每条受影响路由的上述加载场景及可交互入口必须有可复现的浏览器测量；每项至少 5 次，包含首次操作及适用的正常 / 边界场景。保留每次原始耗时、Supabase 请求明细与合并扣除值、门禁净耗时及其最大值、通过 / 失败、测量脚本、版本和环境；用户阅读、填写及等待确认的时间不计入，但确认后的系统响应必须重新计时。新增或修改后重测受影响项，不得引用旧版本结果。
6. **失败处理**：超时、页面错误、内容未就绪或证据缺失都必须判定失败，继续定位修复并重测；不得通过延迟显示、仅显示忙碌状态、跳过冷启动、跳过重操作或降低判定标准规避。只有真实结果达标才可关闭对应需求。
7. **完成与交付**：任一适用项失败，相关需求、阶段和本次总体任务均不得标记“已验证”“完成”或 `verified` / `delivered` / `accepted`；可以如实记录实现存在和未达标项，但记录本身不构成豁免。构建、单测、治理脚本、技术收据通过，以及提交 / 推送成功，均不能替代本节验收。既有检查脚本尚未覆盖本门禁时，必须补充浏览器性能证据，不能只凭脚本退出码或收据状态宣称完成。
8. **记录与结论**：适用的需求矩阵、原型审查记录、任务收据配套证据和最终答复必须明确本节是否通过及证据位置；未通过时必须明确“未完成”，不能将未达标项列成“后续优化”后宣称整体完成。纯文档 / 治理修改且未改变运行行为时注明不适用，不要求为文字修改重跑页面性能。

---

## 8. CodeGraph 与交付

代码核查、设计、修改和实现确认前优先使用 CodeGraph：先查看 `codegraph status`，只在未初始化、存在待同步文件或索引明显落后时同步。结构问题优先 `context` / `explore`，流程问题用 `trace`，影响面用 `impact`；中文文案、路由和状态使用 `rg` 精查。同一事实不得重复查询。

代码修改后运行一次 `codegraph sync` 和 `codegraph status`；只读核查、文档或 `AGENTS.md` 修改无需同步。最终只声明实际执行过的同步状态。

### 8.1 任务收据与交付状态

本次修改 `src/`、`scripts/`、依赖或构建配置，并准备声明为 `verified`、提交或交付时，运行：

```bash
npm run workflow:verify -- --output <临时目录>/task-receipt.json --task-boundary "<本次任务边界>"
```

收据绑定当前 Git HEAD、任务差异、受影响检查、退出结果和 CodeGraph 状态；实质修改后旧收据失效。只读任务、仅文档 / 审查记录 / `AGENTS.md` 修改，或存在无法隔离的无关工作区修改时，不自动运行收据。

本项目默认禁用 Superpowers（见第 3.1 节），不运行 `workflow:stage`，不声明 `--required-skills` 或两阶段技能证据；用户点名使用某个技能时按其单步执行，仍不启用上述收据阶段机制。

交付状态：

- `implemented`：实现存在，最终验证未闭环。
- `verified`：最终改动、相关检查和 CodeGraph 状态闭环；涉及页面 / 交互时，第 7.2 节性能门禁及其中有明确授权的例外必须通过，技术收据通过不能代替该条件。
- `delivered`：在 `verified` 基础上，GitHub API 确认提交存在且目标分支指向该版本，并按第 8.2 节核验该提交对应的 Vercel 部署已成功完成。
- `accepted`：在 `delivered` 基础上，GitHub API 确认有权限的指定接受人明确接受该版本。

没有 provider 回执时不得把本地验证或助手交接表述为远端交付成功。

### 8.2 GitHub 推送后的 Vercel 部署完成门禁

- 每次推送 GitHub 后，必须继续核验本次推送提交对应的 Vercel 部署。只有部署成功完成，本次发布任务和对话才算完成；不得在推送成功后以“Vercel 尚未核验”或“等待自动部署”为由结束任务。
- 核验必须绑定本次推送的完整 commit SHA、目标分支和对应部署环境。推送 `main` 时核验正式环境，不得用其他提交、旧部署或预览环境的成功状态代替。
- 使用 Vercel 部署状态，或 GitHub 上由 Vercel 提供且绑定该 SHA 的部署 / 状态回执确认完成。成功依据为 Vercel `READY`，或明确说明部署已完成的 Vercel `success` 回执；本地构建通过、GitHub 接收推送、构建开始、站点能打开均不能代替部署完成证据。
- 部署仍在排队或构建时，继续等待并以合理间隔查询，期间报告有意义的进展。部署失败或取消时，核查原因并处理本次改动导致的问题；无法继续时明确说明阻塞原因和所需信息，不得声明任务完成。
- 最终回复必须给出提交 SHA、Vercel 部署结果和对应部署链接。若修复后再次推送，必须重新核验最新提交，不能沿用前一次部署回执。
